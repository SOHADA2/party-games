# 숙소에서 하기 좋은 게임 모음 (party-games)

친구들과 숙소(펜션·MT)에서 하는 **오프라인 게임의 진행·순위 도구**.
게임 자체는 현실에서 하고, 폰은 방 코드 접속 · 역할 뽑기 · 순위 입력 · 종합 리더보드만 담당한다.

- **저장소**: https://github.com/SOHADA2/party-games (public)
- **라이브**: https://sohada2.github.io/party-games/
- **현재 버전**: v0.32.0 (`index.html`의 `APP_VERSION`)

---

## ⚠️ 새 세션 / 다른 컴퓨터에서 시작할 때

```bash
git pull                 # ① 항상 먼저. 여러 기기에서 main 에 직접 푸시한다
node -v                  # ② Node 18+ 만 있으면 된다 (npm install 없음, 의존성 0)
node tools/check.mjs     # ③ 받은 코드가 멀쩡한지 30초 확인
node serve.mjs 8080      # ④ 작업 시작 (같은 와이파이의 폰에서도 열린다)
```

⑤ 이 문서 아래 **「세션 이력」** 을 읽고 현재 상태·미해결 항목을 파악한다.
⑥ 작업 후 **버전 올리고**(`APP_VERSION`) **세션 이력 갱신**하고 **바로 푸시**한다.

### 다른 기기로 이어서 작업하기 ★
- **필요한 건 저장소 전부다.** 빌드도, 설치도, 비밀키도 없다. Firebase 설정은 `index.html`
  안에 그대로 있고(공개 RTDB · 친구끼리 전제), 검증 도구는 `tools/`에 들어 있다.
  → `git clone` 하고 Node만 있으면 이 문서의 모든 명령이 그대로 돈다.
- **끝내면 바로 푸시할 것.** 커밋만 하고 두면 다른 기기에서 그 작업이 안 보이고,
  같은 파일(`index.html` 단일 파일이라 거의 항상 겹친다)을 고쳐 충돌한다.
- **충돌이 났다면** `git pull --rebase` 로 풀고, `index.html`은 한 파일에 전부 들어 있어
  자동 병합이 위험하다 → 충돌 구간은 손으로 고른 뒤 **반드시 `node tools/check.mjs`** 로
  끊어진 참조를 확인하고 푸시한다.
- `_*.html`(검증 사본)·`node_modules/`는 gitignore 대상이다. 커밋되면 안 된다.

---

## 기술 스택 / 구조

- **순수 HTML/CSS/JS 단일 파일** (`index.html`) — 프레임워크·빌드 없음. aram과 같은 방식.
- Firebase Realtime Database (CDN v11.0.0, ES module import)
- 배포: GitHub Pages (`main` 브랜치 자동 배포, 1~3분)

| 파일 | 역할 |
|---|---|
| `index.html` | 앱 전체 (디자인 토큰 + CSS + 게임 레지스트리 + 동기화 + 렌더) |
| `manifest.json` / `icon.svg` / `icon-192.png` / `icon-512.png` / `apple-touch-icon.png` | PWA — 홈 화면 추가 시 앱처럼 실행 |
| `sw.js` | 앱 셸 network-first + **폰트 CDN 캐시-우선**. Firebase 요청은 절대 가로채지 않음 |
| `serve.mjs` | 로컬 테스트 서버 (배포물 아님) |
| `tools/` | 검증 도구 — 문법·끊어진 참조 검사, 검증 사본 생성, 헤드리스 스샷 (배포물 아님) |

폰트는 **Pretendard Variable 동적 서브셋**(jsdelivr CDN). 숙소 와이파이가 끊겨도
`sw.js`의 `ASSET_CACHE`에 한 번 받아두면 유지된다. ⚠️ `sw.js`의 캐시 이름(`party-shell-v3`
/`party-asset-v3`)을 바꾸면 옛 캐시가 정리된다 — 셸 목록/캐시 전략을 고칠 때 **버전을 올릴 것**.

### PWA 아이콘 ★함정
- ⚠️ **iOS는 `apple-touch-icon`에 SVG를 지원하지 않는다.** PNG가 없으면 아이폰 홈 화면 아이콘이
  페이지 스크린샷/빈 사각형으로 깨진다. → `apple-touch-icon.png`(180×180) 필수.
- 안드로이드/크롬은 manifest의 SVG도 받지만, **maskable 대응을 위해 PNG 192/512를 같이** 둔다.
- PNG는 `icon.svg`에서 생성한다(이 PC에 cairosvg 없음 → **Edge 헤드리스로 렌더 후 Pillow로 축소**):
  ```
  # 512px로 렌더할 임시 html(img를 512×512로) → --window-size=512,512 --screenshot
  # 이후 Pillow LANCZOS로 192 / 180 생성
  ```
- 아이콘을 바꾸면 **PNG 3개를 다시 생성**하고 `sw.js` 캐시 버전도 올릴 것.

### 앱 설치 유도 (`installBannerHtml`)
홈·대기실 상단에 「📲 앱으로 설치하기」 배너. 크롬은 `beforeinstallprompt`를 잡아뒀다가 띄우고,
iOS는 설치 API가 없어 `showIosInstall()` 안내 모달(공유 → 홈 화면에 추가)을 띄운다.
이미 설치됨(`isStandalone`)·설치 불가 환경·✕로 끈 경우(`pg_install_off`)엔 아예 렌더하지 않는다.
⚠️ 배너 ✕는 버튼 **안에** 있어서, `bind()`의 `stopPropagation()`이 없으면 설치 팝업까지 같이 뜬다.

---

## 배포

```bash
git add -A && git commit -m "..." && git push origin main
```

→ GitHub Pages 자동 빌드. 확인:

```bash
gh run list --repo SOHADA2/party-games --limit 3
curl -s https://sohada2.github.io/party-games/ | grep -o "APP_VERSION = '[^']*'"
```

⚠️ **캐시 주의**: `sw.js`가 앱 셸을 캐싱한다. network-first라 보통 즉시 반영되지만,
안 바뀌면 시크릿 창 또는 새로고침 2번.

### ⚠️ Pages 배포가 안 올라갈 때 (실제로 겪음, v0.20.0)
푸시는 됐는데 라이브 버전이 그대로면 **GitHub 쪽 문제일 수 있다.** 순서대로 본다:

```bash
gh run list --repo SOHADA2/party-games --limit 3     # 실패/queued 인지
gh run view <ID> --repo SOHADA2/party-games --log-failed
curl -s https://www.githubstatus.com/api/v2/summary.json   # Pages/Actions 상태
```

- `Failed to get ID Token … Request timeout` → **OIDC 토큰 발급 타임아웃(일시 장애)**.
  내 코드와 무관하다. 직전 빌드가 같은 설정으로 성공했는지 보면 바로 구분된다.
- `gh run rerun` 을 해도 **queued 에서 안 움직이는 경우가 있다**(정상은 20~25초).
  이때 `gh run cancel` 은 "이미 완료됨"이라 나오고 `gh run view` 에는 잡이 비어 있다 —
  큐가 꼬인 상태다.
- **가장 확실한 복구는 새 커밋을 푸시해 빌드를 새로 거는 것.** 같은 SHA 재푸시는 트리거되지 않는다.
⚠️ 이때 **배포가 안 됐는데 "배포 완료"라고 보고하지 말 것.** 라이브 `APP_VERSION` 을
   직접 `curl` 로 확인한 값만 믿는다.

---

## 로컬 테스트

```bash
node serve.mjs        # http://localhost:8080
node serve.mjs 8123   # 포트 지정
```

같은 와이파이의 폰에서는 서버가 출력하는 `http://<PC IP>:포트` 로 접속.
⚠️ **`file://` 로 직접 열지 말 것** — 서비스워커·일부 기능이 동작하지 않는다.

---

## 검증 레시피 (커밋 전 필수)

1. **문법 + 끊어진 참조**
   ```bash
   node tools/check.mjs          # 실패하면 exit 1
   ```
   단일 HTML이라 `node --check index.html` 은 안 된다(`ERR_UNKNOWN_FILE_EXTENSION`).
   이 도구가 `<script type="module">` 을 뽑아 검사하고 **원본 줄번호로 되돌려** 알려준다.
   ⚠️ 문법 검사만으로는 **지운 함수를 아직 호출하는 것**을 못 잡는다(화면을 갈아엎을 때
   가장 자주 나는 사고). 그래서 선언 없이 호출되는 이름도 같이 훑는다.

2. **검증용 사본 만들기 — 진짜 DB를 건드리지 않는다**
   ```bash
   node tools/demo.mjs tools/scenarios/smoke-e2e.js  _s.html      # 게임 5종 + 백본 전 구간
   ```
   **firebase import 를 통째로** 가로채 `initializeApp/getDatabase/ref/set/update/remove/
   get/onValue`를 스텁으로 바꾼다. 그래서 사본에서는 무슨 짓을 해도 RTDB에 못 쓴다.
   ⚠️ 예전엔 호출부 7군데를 문자열로 패치했는데, 화면을 고칠 때마다 그 문자열이 사라져
   **조용히 무력화**됐다(= 진짜 DB에 씀. 유령 방 4821 사고). 호출부는 절대 패치하지 말 것.
   ⚠️ **`initializeApp`까지 막는 이유** — 예전엔 `firebase-app` import를 남겨뒀는데,
   `gstatic.com`이 막힌 환경(원격 컨테이너·기내 와이파이)에서는 그 import가 실패해
   **`<script type="module">`이 통째로 안 돈다.** 시나리오가 실행조차 안 되는데 에러도 안 난다
   → "다 통과한 줄 알았는데 애초에 안 돌았다"가 된다. 지금은 네트워크 없이도 돈다.

3. **자동 점검 / 스크린샷** (`serve.mjs` 를 먼저 띄운다)
   ```bash
   node serve.mjs 8080 &
   node tools/shot.mjs --dom "/_s.html?demo=1"                     # E2E 결과, 실패 시 exit 1
   node tools/shot.mjs "/_demo.html?demo=1&v=games" -w 440 -h 1500 # 한 화면
   node tools/shot.mjs --grid games,game,board -w 400 -h 1300      # 여러 화면 한 장에
   ```
   화면 스냅샷용 시나리오는 `tools/scenarios/cho.js` 가 본보기다
   (`?demo=1&v=<단계>` 로 원하는 단계까지 진행시켜 놓는다). 게임을 추가하면 같은 형식으로
   하나 만들어 둘 것 — `--grid` 로 단계별 화면을 한 장에 나란히 볼 수 있다.
   ⚠️ **합격 판정에 평범한 낱말을 넣지 말 것.** `shot.mjs` 가 예전에 `/문제|ERR |‼/` 로
   판정했는데, 초성 퀴즈 라벨에 "문제"가 들어가자 **전부 통과인데 exit 1** 이 됐다.
   지금은 시나리오가 찍는 표시(`‼` · `✕FAIL` · 맨 앞 `ERR `)로만 판정한다.

   **★ 헤드리스 함정 — 플래그를 지우지 말 것 (`tools/shot.mjs`가 이미 처리한다)**
   - `--no-sandbox --user-data-dir=<새 폴더>` 를 빼면 **그냥 멈춘다**(프로필 잠금).
     "이 PC에서 스크린샷이 안 된다"의 정체는 거의 항상 이것이었다.
   - 브라우저는 **Edge → 맥/리눅스 크롬 → Playwright 크로미움** 순으로 찾는다.
     못 찾으면 `EDGE` 또는 `CHROME` 환경변수로 경로를 준다.
   - 출력 png는 **Temp**에 쓴다(스크래치패드는 쓰기 거부 0x5).
   - `file://` 로 열지 말 것. 하네스와 대상이 **다른 출처면 가상시간이 전파되지 않아**
     애니메이션 도중에 찍힌다 → 항상 `localhost` 로.
   - 모바일 폭 단일 스샷의 **우측 잘림은 캡처 아티팩트**다(실제 오버플로 아님).
     정확히 보려면 `--grid` 를 쓴다 — iframe마다 폭을 주므로 잘리지 않는다.

4. **검증이 끝나면 사본을 지운다**: `rm -f _*.html` (gitignore 대상이지만 남겨두지 말 것)
   ⚠️ **더미 데이터에 사람 이름을 쓰지 말 것**(아래 원칙 참조).

### 도구 (`tools/` · 의존성 0)

| 파일 | 역할 |
|---|---|
| `tools/check.mjs` | 문법 + 끊어진 참조 + 죽은 코드. 실패 시 exit 1 |
| `tools/demo.mjs` | Firebase 쓰기를 막은 검증 사본 생성 (import 가로채기) |
| `tools/shot.mjs` | Edge 헤드리스 스크린샷 / DOM 덤프. Edge 경로 자동 탐색. `--dom` 도 `-w/-h` 를 주면 그 크기로 띄운다(레이아웃 측정) |
| `tools/scenarios/smoke-e2e.js` | **게임 10종 + 방·점수 백본 전 구간 자동 점검(283항목).** 게임을 추가·삭제하면 반드시 먼저 돌린다 |
| `tools/scenarios/song.js` | 노래 맞히기 화면 스냅샷(`v=setup run reveal done`) |
| `tools/scenarios/yut.js` | 윷놀이 화면 스냅샷(`v=setup board halt phone`) |
| `tools/scenarios/cho.js` | 초성 퀴즈 화면 스냅샷(`v=setup run reveal done` · `cat=proverb` 로 최악 케이스) |
| `tools/scenarios/quiz.js` | 상식 퀴즈 화면 스냅샷(`v=setup run reveal done` · `q=long` 으로 가장 긴 문제) |
| `tools/scenarios/relay.js` | 한 단어 릴레이 화면 스냅샷(`v=setup ready run turnEnd done` · `long=1` 로 가장 긴 제시어) |
| `tools/scenarios/landscape.js` | **가로 화면 점검 — 다 같이 보는 화면이 스크롤 없이 들어가는지(17항목).** ⚠️ `-w 1024 -h 768` 처럼 **크기를 줘야** 의미가 있다 |
| `tools/scenarios/yut-verify.js` | **윷놀이 깊은 검증 — 자동 대국 66판·이동 전수·데드락·레이아웃·말 모양·지적 도달성·던지기 우선(70항목).** 태블릿 크기로도 한 번 돌릴 것(81항목) |

⚠️ 도구는 **음성 테스트를 통과한 상태다**(일부러 버그를 심으면 exit 1). 고칠 때도 그 성질을
유지할 것 — 절대 실패하지 않는 검사기는 없느니만 못하다.

### ⚠️ 이름 원칙 (사장님 지시)
**실존 인물로 읽힐 수 있는 사람 이름을 예시·더미·주석 어디에도 쓰지 않는다.**
테스트 봇 이름은 누가 봐도 가짜인 **동물 이름**(`BOT_NAMES`), 코드 주석 예시는 사물 단어를 쓴다.
스크린샷용 데모 데이터도 마찬가지.

---

## 동기화 모델 — 호스트가 진실원 ★핵심

방 코드로 여러 폰이 접속하지만, **점수·팀·순번의 기준은 호스트(진행자) 기기**다.

- **호스트**: 로컬 `S.room`을 고치고 즉시 렌더 → `pushHostState()`로 RTDB write-through
  (실패해도 무시). 서버 스냅샷에서는 **`players`만 병합**해서 받는다(다른 사람 입장 감지).
- **참가자**: 서버 스냅샷을 그대로 반영. 자기 `players/{pid}` 노드만 쓴다.

→ **숙소 인터넷이 끊겨도 호스트 화면에서 진행·점수 입력이 그대로 되고,
복구되면 다음 쓰기 때 권위 필드 전체를 다시 밀어 자동으로 맞춰진다.**

⚠️ 이 구조 때문에 **호스트가 앱을 닫으면 기준 기기가 사라진다.** 설정 화면에 경고 있음.
⚠️ 권위 필드(`teams`/`scores`/`rotation`/`status`)를 참가자가 쓰게 만들면 이 모델이 깨진다.

---

## ⚠️ 경고창 — 되돌릴 수 없는 것은 먼저 물어본다 (v0.31.0)

`confirm()` 을 **앱 어디에서도 쓰지 않는다.** PWA 로 띄우면 시스템 창이라 앱이 깨진 것처럼 보이고,
「무엇이 사라지는가」를 여러 줄로 적을 수가 없다. 대신 상태 하나로 도는 인앱 경고창을 쓴다.

```js
S.ask = { emoji, title, lose:[...], safe, ok, go, d }   // go = 「예」를 눌렀을 때 부를 액션
```

- `vAsk()` 가 그리고 **`render()` 가 뷰 HTML 뒤에 이어 붙인다** — `bind()` 가 `#view` 만 훑으므로
  그 안에 있어야 버튼이 먹는다. `position:fixed` 라 어느 컨테이너에 있든 화면 전체를 덮는다.
  ⚠️ **`.two` 같은 래퍼 안에 넣으면 안 된다** — 그래서 뷰 함수가 아니라 `render()` 에서 붙인다.
- ⚠️ **`renderSig()` 에 `S.ask?.go` 가 있어야 한다.** 빼면 창이 뜨지도 닫히지도 않는다.
- ⚠️ **`go` 는 다시 묻지 않는 액션이어야 한다**(`leave` → `leave-go`). 안 그러면 창이 다시 뜬다.
- ⚠️ **`await` 하지 않는다.** `act()` 는 switch 를 동기로 들어가므로 `ask-yes` 가 `act(q.go)` 를
  부르면 상태 변경이 즉시 일어난다 — 검증 시나리오가 다음 줄에서 바로 상태를 볼 수 있다.
- `go()` 가 `S.ask = null` 로 지운다(화면이 바뀌면 열려 있던 창은 의미가 없다).
- `lose[]` 와 `safe` 는 **상수라 raw 삽입**된다(`<b>` 를 쓰려고). 사람 이름 같은 값은 `h()` 를 거칠 것.

**⚠️ 「물어본다」만으로는 경고가 아니다 — 무엇이 사라지는지 적어야 한다.**

| 어디 | 사라지는 것 |
|---|---|
| 방 나가기 · 진행자 | 순위를 저장할 기기가 없어짐 · 방 코드를 잊으면 점수로 못 돌아옴 |
| 방 나가기 · 참가자 | **다시 들어오면 새 `pid` 라 지금까지 쌓은 내 점수가 통째로 끊긴다** (실제 점수를 숫자로 보여준다) |
| 게임 그만두기 | 진행 중인 기록·점수가 저장되지 않음 |
| 점수 전부 지우기 | 저장한 모든 판의 순위 · 되돌릴 수 없음 |
| 제시어 기록 초기화 | 이미 나온 제시어가 다시 나올 수 있음 |
| 윷판 정리 | 말 위치와 판 · 순위 미반영 |

⚠️ **참가자의 「점수가 끊긴다」는 진짜다.** `joinRoom()` 이 매번 새 `pid` 를 발급하고
   `clearSession()` 이 복구용 세션까지 지우므로, 나갔다 들어오면 옛 `pid` 의 `scores` 기록이
   **고아가 되어 `calcPoints()` 에 안 잡힌다.** 경고 문구를 고칠 때 이 사실을 지우지 말 것.

---

## 데이터 구조

Firebase RTDB — **aram 프로젝트 재사용**(`aramchaos-ca022`), `party` 노드로 격리.
crumble-bot이 `/crumble`을 쓰는 것과 같은 패턴. RTDB 규칙이 공개라 인증 불필요.

```
/party/rooms/{4자리코드}
  createdAt, touchedAt, host, status
  players/{pid}   : { name, joinedAt, seen }        // seen = 20초 하트비트, 70초 넘으면 오프라인 표시
  teams           : { count, assign:{pid:팀번호} }
  scores/{sid}    : { gameId, mode, order[], weight, assign, at }
  rotation/{게임id}: [pid...]                        // 이미 술래/사회자 한 사람
  used/{게임id}    : [제시어...]                     // 이미 나온 제시어(판 사이 중복 방지)
```

- `order` = 순위 배열 (개인전 = pid, 팀전 = 팀 인덱스)
- **점수는 저장하지 않고 매번 계산**(`calcPoints`) → 배점(weight)을 바꾸면 과거 기록에도 즉시 반영
- 팀전 기록은 **그 시점 `assign`을 스냅샷으로 함께 저장** → 나중에 팀을 다시 짜도 과거 점수가 안 깨짐
- **`used`** = 이미 나온 제시어. `rotation` 과 **똑같은 자리·똑같은 방식**(호스트만 씀).
  `S.play` 는 판이 끝나면 `null` 이라 기억이 없으므로 방 노드에 둔다. 비밀이 아니다(다 같이 본 단어).
  남은 게 이번 판을 채우기에 모자라면 **자동으로 비우고 처음부터** 돈다(멈추면 안 되니까).
  진행자가 「🔄 초기화」로 직접 비울 수도 있다(`used-reset`).
- 방 코드: 4자리 숫자. **48시간 이상 손대지 않은 방은 재사용**(`newCode()`)
  ⚠️ **12시간이 아니라 48시간인 이유** — 숙소는 하룻밤을 잔다. 밤에 놀고 다음 날 오후에
  이어 하면 12시간이 쉽게 넘고, 그 사이 남이 같은 코드를 받아가면 그 방 점수가 통째로 날아간다.
- `pid`는 세션마다 새로 발급. `localStorage.partygames_session`에 `{code,pid,isHost,name}` 저장 → 재접속 복구
- ⚠️ **지워진 게임의 옛 `scores` 기록이 남아 있을 수 있다.** `gameId`가 `GAMES`에 없으면
  리더보드는 🎲 기본 아이콘 + 원래 id로 표시하고 점수는 그대로 반영한다
  (`sc.weight`를 저장해두므로 `G()`가 없어도 계산이 된다). 진행자가 ✕로 지울 수 있다.
  게임을 삭제할 때 **이 폴백을 깨지 말 것** — `G(sc.gameId)`를 옵셔널 체이닝 없이 쓰면 화면이 죽는다.

---

## 점수 체계 — **개인 랭킹 하나로 모인다** ★ (사장님 확정, v0.32.0)

```
1등 10 · 2등 7 · 3등 5 · 4등 3 · 5등 2 · 이하 1     (RANK_POINTS / rankPt)
× 게임별 배점(weight)
팀전 = 팀 순위 점수를 팀원 **전원에게 동일 지급** → 그대로 개인 점수가 된다
```

게임마다 점수 단위가 다르므로(걸린 시간/맞춘 개수/승패) **전부 "그 판의 순위"로 환산**해서 합산한다.
이 설계 덕분에 게임을 몇 개를 추가하든 리더보드가 안 깨진다.

**최종 랭킹은 언제나 개인전이다.** 팀은 랜덤으로 다시 짜도 되고, 팀전을 몇 판 하든
그 점수는 개인 누적으로 들어간다. 그래서 「오늘의 1등」이 항상 한 사람으로 나온다.

- **`scorePt(sc, pid)`** — 한 판이 그 사람에게 준 점수. `calcPoints()` 와 **같은 규칙**을 쓴다.
  리더보드의 게임 기록에 `+15` 처럼 표시해서 **팀전이 내 점수로 들어온 게 눈에 보이게** 한다.
- ⚠️ **팀전은 그 시점 `assign` 스냅샷으로 판단한다.** 현재 `S.room.teams` 를 보면 안 된다 —
  팀을 다시 짜는 순간 과거 점수가 통째로 뒤바뀐다. E2E 에 못을 박았다.
- **점수에 따른 보상·벌칙은 앱이 다루지 않는다**(사장님: "점수에 따라서 뭘 받거나 하는 거는
  나중에 개별적으로 정하면 되는 거니까"). 앱은 순위까지만 낸다.

## 🗂 지난 기록 — 방이 사라져도 다시 본다 (v0.32.0)

사장님: "만약 방에서 모두 나가더라도 해당 점수들은 다시 확인할 수 있도록
메인화면에 시간과 일자 등을 표시해주는 게 맞을 것 같아."

방 노드는 **언젠가 사라진다**(모두 나가고 48시간이 지나면 코드가 재사용된다).
그래서 최종 순위를 **`localStorage`(`partygames_rooms`)** 에 따로 갈무리한다 — RTDB 가 아니다.

```js
{ code, at, last, rows:[{n:이름, p:점수}], games:[{g:게임id, m:모드, top:[상위3]}] }
```

- **`snapRoom()`** 이 `render()` 끝에서 돈다. ⚠️ **서명이 바뀔 때만 쓴다** —
  `render()` 는 스냅샷마다 불리고 `localStorage` 는 동기 I/O 다.
- 홈 화면 `histHtml()` 이 **날짜·시각·1등·인원·판 수**를 줄로 보여주고, 누르면 `vHist()` 상세.
- ⚠️ **`vHist()` 는 `S.room` 을 안 본다.** 저장된 값만으로 그린다 —
  그래야 방이 없어도 열린다(그게 이 기능의 전부다). E2E 가 `S.room=null` 로 확인한다.
- ⚠️ **지금 들어가 있는 방의 기록은 지울 수 없다.** 살아 있는 값이라 지워도 다음 렌더에
  다시 쌓인다. 그 줄엔 ✕ 를 아예 안 그리고 「지금 방」 칩을 붙인다.
- 최대 30개. 넘으면 오래된 것부터 밀려난다.

---

## 디자인 시스템 ★

**컨셉 = 심야 파티 / 네온 사인.** 잉크빛 자정 배경 + 코랄→앰버 히어로 그라데이션 +
게임별 고유 액센트. 술 마시며 봐도 읽히게 대비·글자 크기를 크게 잡았다.

- **전부 `:root` CSS 변수로 통제**한다. 색을 하드코딩하지 말고 토큰을 쓰거나 토큰을 추가할 것.
  - 배경 `--ink/--ink2` · 표면 `--surf/--surf2/--surf3` · 선 `--line/--line2`
  - 글자 `--tx/--dim/--dim2` · 액센트 `--hot/--amber/--elec/--mint/--rose/--sky`
  - `--hero` (메인 그라데이션) · `--sh1~3` (그림자) · `--lift` (카드 위쪽 1px 하이라이트)
  - 반경 `--r-s/m/l/xl` · 이징 `--ease`(감속) `--spring`(탄성)
- **깊이 표현**: 카드는 `box-shadow: var(--sh1), var(--lift)`. `--lift`가 위에서 빛 받은 느낌을 만든다.
- **누름 반응**: 모든 인터랙티브 요소에 `transform:scale(.97~.98)` + `--spring`.
- **배경**: `.bg`의 오로라 3개는 `transform`만 애니메이션(리페인트 없음) + `.grain` SVG 노이즈.
  `filter:blur()`는 모바일에서 비싸서 **안 쓴다** — 부드러움은 radial-gradient 자체로 낸다.
- **`prefers-reduced-motion`** 존중(모든 애니메이션 무력화).

### 큰 화면 대응 — 태블릿을 상 위에 세워두고 다 같이 본다 ★ (v0.14.0)

실제 운영은 **태블릿 1대를 가운데 두고 전원이 그 화면을 보는** 방식이다. 폰을 30cm에서
보는 크기로는 60~100cm 떨어진 사람이 못 읽는다. 그래서 `@media (min-width:760px) and
(min-height:640px)` 에서 `#app` 폭을 880px로 넓히고 **「다 같이 보는 것」만** 키운다
(방 코드 · 타이머 · 스톱워치 · 점수판 · 순위 · 리더보드 · 규칙 전문).

**⚠️ 화면마다 「몇 명이 봐야 하는가」를 먼저 정한다 — 이게 이 대응의 핵심이다.**

| 보는 사람 | 처리 | 예 |
|---|---|---|
| 전원 | 그냥 둔다 → 커진다 | 방 코드 · 타이머 · 순위 · 리더보드 |
| 전원 − 1명 | 그냥 둔다 → 커진다 | 멕썸노이즈 **brief**(술래 빼고 다 같이 읽는다. 바로 앞 단계에서 앱이 「술래는 뒤돌아 눈 감기」를 시켜둔다) |
| **딱 1명** | **`.priv` 를 붙인다** → 폰 크기 유지 + 폭 420px | 몸으로 말해요 **제시어**(맞히는 팀이 보면 그 판이 끝난다) · 멕썸노이즈 **진행 중 하단 미션**(술래가 화면 앞에서 뛰고 있다) |

⚠️ **`.priv` 를 빼먹으면 게임이 조용히 깨진다.** 화면은 멀쩡해 보이는데 맞히는 팀이 답을
   다 보고 있는 상태가 된다. 새 진행 화면을 만들 때 **먼저 이 표를 볼 것.**

⚠️ **가로 폭만으로 미디어쿼리를 걸지 말 것.** `min-width:760px` 만 쓰면 **폰 가로 모드**
   (예: 926×428)가 걸려서 작은 화면에 거대한 글씨가 뜬다. 반드시 `min-height` 를 같이 건다
   (태블릿 가로 1024×768 · 세로 820×1180 통과 / 폰 가로는 높이에서 탈락).

⚠️ **가로로 세운 태블릿은 세로가 모자라다**(768px). 공용 화면은 **스크롤하면 안 된다** —
   뒤에서 보는 사람은 스크롤을 못 한다. `max-height:860px` 구간에서 한 화면에 들어가게 조인다.

### ⚠️ `color-mix()` 쓰지 말 것
게임별 액센트 틴트는 **`gcVars(hex)`** 헬퍼가 `--gc/--gcSoft/--gcLine/--gcGlow`를 rgba로 계산해
인라인 스타일로 내려준다. `color-mix()`는 구형 사파리에서 통째로 무효가 되어 색이 날아간다.
새 액센트 틴트가 필요하면 `gcVars`에 변수를 추가할 것.

### 연출
- `confetti(n)` — 캔버스 파티클. 방 생성 / 팀 편성 / 순위 저장 시 발동.
- `rollReveal(label, pool, finalPid)` — **역할 뽑기 슬롯머신**. 이름을 점점 느리게 순환시키다
  당첨자에서 멈추고 컨페티. 숙소에서 다 같이 폰 하나 쳐다보는 순간이라 연출 가치가 큼.
  ⚠️ 결과(`finalPid`)는 **애니메이션 전에 이미 확정**돼 있다. 연출이 결과를 바꾸지 않는다.
- 진입 애니: 리스트 항목에 `animation-delay:${i*40}ms` 스태거.

## 확정된 기획 방향 (2026-08-13 · 사장님 결정) ★

| 항목 | 결정 | 이유 |
|---|---|---|
| **벌칙(패널티)** | **앱이 다루지 않는다** | 앱은 순위만 챙기고 벌칙은 현장에서 정한다. 앱이 단순해지고, 술 안 마시는 사람이 있어도 그대로 쓸 수 있다. ⚠️ 나중에 추가해도 구조는 안 깨진다 |
| **각자 폰 사용** | **쓰지 않는다** | 각자 폰만 들여다보면 숙소에 둘러앉은 의미가 없다. 전 게임이 **진행자 폰 하나**로 돌아간다. (비밀 정보 배분이 필요했던 크라임씬은 v0.13.0에서 삭제) |
| **사회자(관전) 기기** ★ | **태블릿은 방에 「사회자」로 들어간다 — 선수가 아니다** | v0.15.0. 태블릿이 그냥 참가하면 「사람이 한 명 더 늘어난」 셈이라 팀·술래 뽑기·순위가 전부 어긋난다. `players/{pid}.spec` 로 표시하고 **`playing()`** 이 선수만 센다 |
| **공용 화면** ★ | **태블릿 1대를 가운데 두고 다 같이 본다** | v0.14.0. 사장님 실제 운영 방식이다. 진행자 폰 대신 태블릿을 상 위에 세워두고 전원이 그걸 본다 → 화면을 「전원이 봐도 되는 것」과 「한 사람만 봐야 하는 것」으로 나눠야 한다(아래 「큰 화면 대응」) |
| **규칙 복잡도** ★ | **설명이 1분을 넘으면 안 된다** | v0.13.0 결정. 크라임씬·안 봤어도 들키지마는 도구는 잘 돌았지만 **설명하다가 판이 식었다.** 술 마시며 하는 게임이라 「한 줄 듣고 바로 시작」이 아니면 안 굴러간다. 새 게임을 넣을 때 이 기준을 먼저 볼 것 |
| **점수** | 순위 포인트 통합 리더보드 유지 | 이미 구현됨 |

## 게임별 현황 / 제작 순서

분류 축은 **두 개고 서로 직교한다.** 「사회자 필요」는 개인전/팀전과 나란히 놓는 3번째 통이 아니다.
- `mode` — **개인전(solo) / 팀전(team)**. 누가 겨루는가. 목록 화면의 묶음 기준.
- `mc` — **`'need'`(전담 사회자가 있어야 깔끔) / `'help'`(그때 안 뛰는 선수가 겸해도 된다)**.
  누가 폰을 잡는가. `mcWhy` 에 이유를 한 줄로 적어두면 게임 상세에 그대로 뜬다.

⚠️ **`mc:'help'` 라고 조작자가 필요 없다는 뜻이 아니다.** 전부 「지금 안 뛰는 사람」이
   폰을 잡아야 한다(술래는 문장을 보면 안 되고, 주자는 두 손이 바쁘다). 차이는 그 사람이
   **선수 중 한 명이어도 되느냐**뿐이다. `need` 는 그게 안 되는 경우다.

| 게임 | `tool` | 분류 | 앱이 해주는 것 | 상태 |
|---|---|---|---|---|
| 몸으로/한글자로 말해요 | `deck` | **팀전 · 사회자 전담**(`need`) | 제시어 덱 991개·타이머·팀 점수판 | ✅ v0.3.0 (덱 v0.17.0) |
| **한 단어 릴레이** | `deck` | **팀전 · 3대 3** (`help`) | 같은 덱(**한 단어로 이을 수 있는 12범위**)·타이머·팀 점수판·**방식과 반칙 기준을 화면에 적어줌**. ⚠️ 역할·순서·반칙 판정은 **사람이 한다** | ✅ v0.30.1 |
| 훈민정음 윷놀이 | `yut` | **팀전 · 태블릿 말판 + 각자 폰** | 윷 던지기·말판·업기·잡기·지름길·**「✋ 잠시!」 영어 지적** | ✅ v0.22.0 |
| 노래 맞히기 | `song` | **개인전 · 사회자 전담**(`need`) | **곡 424개(12범위)**·시대별 난이도·**정답은 사회자만(.priv)**·유튜브 뮤직 딥링크·중복 방지 | ✅ v0.20.0 |
| 초성 퀴즈 | `cho` | 개인전 · 먼저 맞히기 (`help`) | **초성 변환**·**답이 유일한 것만 출제(832문제)**·정답 숨김·정답자 탭 집계·되돌리기·점수순 자동 순위 | ✅ v0.17.0 |
| **상식 퀴즈** | `quiz` | 개인전 · 먼저 맞히기 (`help`) | **11범위 1,499문제**·문제만 띄우고 **정답 공개 후 다 같이 판정**·정답자 탭 집계·되돌리기·중복 방지·점수순 자동 순위 | ✅ v0.26.0 |
| 멕썸노이즈 | `noise` | 개인전 · 술래 1명 (`help`) | **미션 문장 자동생성**(술래 몰래 보기)·성공 시간 스톱워치·시간순 자동 순위 | ✅ v0.4.0 |
| 컵 레이스 | `cup` | 개인전 · 기록 (`help`) | 기록 스톱워치·시간순 자동 순위 (`noise`와 race-* 핸들러 공유) | ✅ v0.4.0 |
| 연기 대결 | `act` | 개인전 · 심사 (`help`) | 대사 20종 × 감정 4종 랜덤 카드·리롤 | ✅ v0.4.0 |
| 스마일~! | `smile` | 개인전 · 술래 1명 (`help`) | 사진 10장 카운터·걸린 사람 판정(1명=술래 교대/다수=재진행/0명=재도전)·라운드 로그 | ✅ v0.4.0 |

**10종 전부 진행 도구 완료.** 남은 것 = 실기기 검증.

⚠️ **노래 맞히기는 인터넷이 필요한 유일한 게임이다.** 나머지는 전부 오프라인에서 돈다.

⚠️ **v0.13.0에서 크라임씬(`mafia`)과 안 봤어도 들키지마(`watch`)를 삭제했다.**
둘 다 도구는 완성돼 잘 돌았지만 **규칙 설명에만 몇 분이 걸려** 숙소에서 바로 시작이 안 됐다.
되살릴 일이 있으면 `git log` 에서 v0.11.0 / v0.12.0 커밋을 보면 된다 —
`MAFIA_SITS`(배역 96개) · `CRIME_EXT`(사건 3종) · `WATCH_DECKS`(88작품 99퀴즈) 데이터가
통째로 들어 있다. **다시 붙이려면 데이터만 가져오고 UI는 다시 짜는 게 낫다**(그 UI가 문제였다).

## 사회자(관전) 기기 ★ — `spec` / `playing()`

태블릿을 공용 화면으로 세워두면 **방에 사람이 한 명 더 늘어난 셈**이 된다. 그대로 두면
팀이 홀수로 갈리고, 술래로 뽑히고, 순위표에 0점으로 올라온다. 그래서 「겨루지 않는 기기」를
따로 표시한다.

```
players/{pid} : { name, joinedAt, seen, spec:true }   // spec = 사회자(관전) 기기
```

- **`players()`** = 방에 있는 전부 (대기실 목록 · 하트비트 · 봇 카운트)
- **`playing()`** = `spec` 이 아닌 사람만 ← **팀 편성 · 역할 뽑기 · 도구 순번 · 순위 입력 ·
  리더보드 · 최소 인원 검사는 전부 이쪽을 쓴다**

⚠️ **새로 「참가자 수」를 세는 코드를 쓸 때 `players()` 를 쓰면 태블릿이 선수로 끼어든다.**
   v0.15.0에서 실제로 갈아끼운 자리가 11군데였다. 헷갈리면 `playing()` 이 기본값이다.

⚠️ **팀 배정은 지우는 대신 「보여줄 때」 거른다.** `teams.assign` 은 권위 필드라 호스트만
   쓸 수 있는데, 관전 토글은 **아무 기기나** 누른다. 호스트가 아닌 사람이 토글하면 자기
   배정을 못 지운다 → 그래서 대기실 팀 박스 · `vDeck`의 `members()` · 순위 입력 팀 라벨
   3곳에서 `isSpec` 을 걸러낸다. 새 팀 표시를 만들면 여기도 같이 걸 것.

- 관전 전환은 **자기 `players/{pid}` 노드만** 쓴다(참가자도 쓸 수 있는 경로). 동기화 모델을 안 깬다.
- **점수 기록(`scores`)은 지우지 않는다.** `calcPoints()` 가 선수만 초기화하므로 관전 중엔
  점수가 안 잡히고, 해제하면 그대로 되살아난다 — 되돌릴 수 있는 토글이다.
- **선수가 1명 남으면 관전 전환을 막는다**(전원이 관전이면 아무 게임도 못 한다).
- **`role:'사회자'` 게임(`body`)은 관전 기기가 있으면 「사회자 뽑기」를 아예 숨긴다.**
  안 그러면 선수 중 한 명이 또 사회자로 빠져 이 기능의 의미가 사라진다.

## 진행 도구 (`tool`) 아키텍처 ★

`GAMES[].tool`이 있으면 게임 상세에 **「▶ 진행 도구로 시작하기」** 버튼이 뜬다(호스트만).
없으면 기존 「순위 직접 입력」만.

### ⚠️ 진행 도구 상태는 Firebase에 올리지 않는다
`S.play`는 **이 기기 로컬 전용**이다. 제시어가 다른 사람 폰에 뜨면 게임이 성립하지 않는다.
동기화되는 건 **최종 순위뿐** — `deckToScore()`가 결과를 `S.draft`로 만들어 **기존 순위 입력
화면으로 넘긴다**. 동점·이견은 거기서 손으로 조정하고 저장하면 기존 `scores` 흐름을 탄다.

### 덱 도구(`deck`) 흐름
```
setup(방식·시간·카테고리) → ready(폰 넘기기) → run(타이머+제시어) → turnEnd(결과)
   → 다음 팀 반복 → done(팀 점수) → 순위 입력 화면
```
- `WORD_DECKS` — 카테고리 **14종 1,150개**. `buildDeck()`이 선택 카테고리를 합쳐 셔플(**교차 중복 제거**),
  다 쓰면 자동 재셔플.
  ⚠️ **각 카테고리에 `use:['body','cho']` 태그가 있다.** 두 게임의 요구가 정반대라서다 —
  몸으로 말해요는 *몸짓으로 표현 가능*해야 하고(나라·사자성어는 불가),
  초성 퀴즈는 *답이 하나로 떨어져야* 한다(두 글자 불가).
  화면에는 `deckKeys('body'|'cho')` 가 거른 것만 뜨고, `pl-cat` 핸들러도 한 번 더 막는다.
  **새 카테고리를 넣을 때 `use` 를 안 적으면** 「아르헨티나를 몸으로 표현하세요」가 나온다.
- `beginTurn/endTurn/markWord` — 턴 생명주기. `SFX`(WebAudio 짧은 효과음) + 진동.
- **한글자로 말해요**는 `ready`에서 팀별 한 글자를 입력받아 `run` 화면에 상시 표시.

### 도구별 메모 (v0.4.0)
- **공통**: 각 도구는 `S.play.kind`로 구분, `vPlay()`가 디스패치. 결과는 전부 `toScore(order, mode)`로
  기존 순위 입력 화면에 넘긴다. 도구 액션 핸들러는 **`render(true)`**(강제)를 쓴다.
- **`noise`/`cup`**: 시간 기록형 — `race-run/end/next/save` 핸들러를 **공유**한다(`rec{pid:초|null}`,
  실패=null=꼴찌). 멕썸노이즈 문장 = `jo(술래,'이','가') + noiseSentence()`
  (62% 확률로 다른 참가자 이름이 들어간 상호작용 미션). 스톱워치 = `swBegin()` → `#sw-sec` 직접 갱신.
- **`act`**: `ACT_LINES` 20종 × 감정 4종. 셔플 덱에서 pop, 소진 시 재셔플. 순위는 심사(수동 입력).
- **`smile`**: 판정 3분기 — 1명 걸림=술래 교대 / 다수=걸린 사람들끼리 재진행(pool 축소·술래 유지) /
  0명=같은 술래 재도전. `log`에 라운드 기록. 순위는 수동 입력(술래 순서 역순 규칙이 수동 판단 필요).

### ⚠️ v0.22.0 부터 예외가 하나 있다 — 윷놀이

**훈민정음 윷놀이(`yut`)는 태블릿과 각자 폰이 같이 도는 유일한 게임**이다.
아래 「전부 로컬」 설명은 나머지 7종에만 해당한다.

```
room.yut = { phase, turn, pieces[팀][말4], roll, log, penalty, winner,
             throw,   ← 참가자가 쓴다(자기 차례에 던진 결과)
             halt }   ← 참가자가 쓴다(✋ 잠시! 지적)
```
- 호스트(태블릿)가 진실원. **`pushYut(patch)` 로 하위 경로만** 갱신한다.
- ⚠️ **`pushHostState()` 에 `yut` 을 넣지 말 것** — 통째로 덮으면 던진 결과가 날아간다
  (v0.8.0 마피아 때 똑같이 겪었다).
- 호스트는 스냅샷에서 **`throw`/`halt` 두 경로만** 되받는다. `yutConsume()` 이 반영 후
  **반드시 지운다** — 안 지우면 스냅샷마다 같은 결과가 다시 들어온다.
- ⚠️ **`renderSig()` 에 `r?.yut` 이 있어야 한다.** 빼면 각자 폰이 말판 변화에 안 움직인다.

### ★ 진행 도구는 (윷놀이만 빼고) 전부 로컬이다 (v0.13.0)
`mafia`(크라임씬)가 **유일하게 Firebase를 쓰는 도구**였는데 삭제됐다. 그래서 지금은
**`S.play`가 예외 없이 이 기기 로컬 전용**이고, 방 노드에 올라가는 건 `players`/`teams`/
`scores`/`rotation` 넷뿐이다. 동기화 배선이 그만큼 단순해졌다 —
`pushHostState()`가 권위 필드를 통째로 밀어도 아무것도 안 깨진다.
⚠️ **다시 「각자 폰에 비밀 정보를 뿌리는」 게임을 넣을 거라면** 그 예외를 되살리는 것이므로,
   쓰기 주체 분리(호스트만 쓰는 노드 vs 참가자가 쓰는 노드)를 먼저 설계할 것.
   v0.8.0~v0.11.0 커밋에 그 배선이 통째로 들어 있다.

### ⚠️ 타이머 규칙 (성능과 직결)
- `S.play.left`(남은 초)는 **`renderSig()`에 넣지 않는다.** 넣으면 매 틱 전체 재렌더가 돈다.
- 타이머 표시는 **`renderTimer()`가 DOM을 직접 갱신**(200ms 간격, `#t-bar` / `#t-sec`).
- `go()`가 `play` 이외의 화면으로 갈 때 **`stopPlayTimer()`** 를 부른다. 새 타이머를 만들면
  반드시 정지 경로를 같이 만들 것(누수 = 백그라운드에서 계속 도는 인터벌).

## 게임 추가하기

`index.html`의 **`GAMES` 배열에 항목 하나만 추가**하면 목록·규칙·역할 뽑기·순위 입력·리더보드에
전부 자동 반영된다.

```js
{
  id:'newgame',            // 고유 id (점수 기록의 키 — 나중에 바꾸면 과거 기록과 끊김)
  name:'게임 이름', emoji:'🎲',
  mode:'solo' | 'team',
  weight:1,                // 배점 배수
  role:'술래' | null,      // 있으면 중복 방지 랜덤 뽑기 UI 표시
  tag:'술래 1명',          // 목록에 뜨는 한 줄 설명
  mc:'help' | 'need', mcWhy:'왜 그런지 한 줄',

  /* ★ 설명 구조 — 상세 화면이 이걸 그대로 카드로 그린다. 하나라도 비면 화면이 휑하다 */
  line:'이게 무슨 게임인지 한 줄',     // 45자 이내
  prep:'준비물' | '없음',
  minP:2,                  // ★ 최소 인원 — 화면 표시와 tool-start 조건이 **이 값 하나**를 읽는다
  mins:'10~15분',
  how:['①', '②', '③'],     // 진행 단계 3개 이상, 각 45자 이내
  win:'누가 이기는지 한 줄',

  rules:`...`,             // 자세한 규칙 — 화면에서 접혀 있다. <b> 사용 가능(상수라 raw 삽입)
  todo:'미확정 규칙 메모'   // (선택) 앱에 주황 경고 박스로 표시
}
```

⚠️ **설명을 `rules` 한 덩어리에만 쓰지 말 것.** 예전에 그랬는데, 「무슨 게임인지 · 뭐가
필요한지 · 몇 명인지」를 알려면 긴 글을 읽어야 했다. 술 마시며 보는 화면이라 아무도 안 읽는다.
⚠️ **`minP` 를 핸들러에 하드코딩하지 말 것.** 예전엔 `tool-start` 에 2/3 이 박혀 있어서
화면 표시와 실제 조건이 따로 놀 수 있었다. E2E가 이 일치를 검사한다.

⚠️ `rules`는 `escapeHtml` 없이 삽입된다. **상수에만 쓰고 사용자 입력은 절대 넣지 말 것**
(사용자 입력은 전부 `h()` 경유).

### ★ 새 게임을 고를 때의 기준 (v0.13.0 결정)
1. **규칙 설명이 1분을 넘으면 안 된다.** 이게 첫 번째 관문이다 — 여기서 걸린 게임 2종을
   실제로 들어냈다(위 세션 이력). `rules` 를 쓰다가 화면 두 개를 넘어가면 신호다.
2. **앱이 해줄 일이 있어야 한다.** 종이·말로 되는 걸 앱에 넣으면 폰만 붙잡게 된다.
   좋은 후보: 타이머 · 랜덤 뽑기 · 카운터 · 자동 순위 산출.
   ⚠️ **「있어야 한다」이지 「많아야 한다」가 아니다.** 규칙을 코드로 옮기고 싶어지면 먼저
   **그걸 사람이 못 하는지** 물을 것 — 둘러앉아 다 보고 있으므로 순서 지키기·반칙 판정은
   사람이 훨씬 잘한다. v0.30.1 에서 실제로 만들었다가 들어냈다(역할 뽑기·순서 표시·반칙 버튼).
   앱이 적어주면 되는 것은 **규칙**이고, 앱이 해야 하는 것은 **제시어·시간·점수**다.
3. **진행자 폰 하나로 끝나야 한다.** 각자 폰에 비밀 정보를 뿌리는 구조는 동기화 예외를
   되살리는 일이다(위 「이제 진행 도구는 전부 로컬이다」 참조).
4. **기존 도구 틀을 재사용할 수 있는지 먼저 본다** — 시간 기록형은 `race-*` 핸들러 공유,
   제시어/카드형은 `deck` 또는 `ACT_LINES` 방식, 카운터·판정형은 `smile` 틀.

⚠️ 게임을 **지울 때**는 세션 이력 v0.13.0의 「같이 끊기는 것들」 9항목 체크리스트를 따를 것.
   그리고 지우든 더하든 **`node tools/demo.mjs tools/scenarios/smoke-e2e.js _s.html` 을 먼저 돌린다.**

---

## 코드맵 (index.html)

| 블록 | 내용 |
|---|---|
| `<style>` | 디자인 토큰(`:root`) → 배경 레이어 → 헤더 → 공통 → 화면별 |
| 설정 | `APP_VERSION` · `ROOT='party'` · `firebaseConfig` · `RANK_POINTS` · 색 팔레트 |
| `GAMES` | **게임 레지스트리** (10종) |
| 상태 | `S` 객체 (view/code/pid/isHost/room/draft/…) |
| 동기화 | `pushHostState` · `subscribe` · `startHeartbeat` · `createRoom` / `joinRoom` / `rejoin` / `leaveRoom` |
| 계산 | `calcPoints` · `playedGames` · `pickRole`(중복 방지) · `makeTeams` |
| 연출 | `confetti` · `rollReveal` · `gcVars`(액센트 rgba) · `av`(아바타) |
| 경고창 | `S.ask` · `vAsk()` · `askLeave()` · `ask-yes`/`ask-no` — 되돌릴 수 없는 액션 앞에 |
| 지난 기록 | `snapRoom()` · `histAll`/`histPut` · `histHtml()` · `vHist()` — 방이 사라져도 이 기기에 남는 최종 순위 |
| 렌더 | `render()` → `vHome`/`vLobby`/`vGames`/`vGame`/`vScore`/`vBoard`/`vSettings`/`vHist` |
| 이벤트 | `bind()` + `act(a, dataset)` — `data-act` 위임 방식 |

렌더는 `innerHTML` 통짜 교체 + `data-act` 이벤트 위임. 상태 바뀌면 `render()` 호출.

---

## 현재 상태

**백본 완료** — 방 생성/참여(4자리 코드·링크 공유·세션 복구), 실시간 참가자 목록(오프라인 표시),
팀 랜덤 편성, 게임 목록·규칙 전문, 역할 중복 방지 뽑기, 순위 입력, 종합 리더보드,
PWA(홈 화면 추가·앱 셸 캐시), 화면 꺼짐 방지(Wake Lock).

**게임 10종 전부 전용 진행 도구 보유** (위 「게임별 현황」 표 참조):
멕썸노이즈 · 스마일~! · 몸으로/한글자로 말해요 · **한 단어 릴레이** · 연기 대결 · 컵 레이스 ·
초성 퀴즈 · 상식 퀴즈 · 노래 맞히기 · 훈민정음 윷놀이

### ⏭️ 다음 작업 (우선순위)

1. **실기기(폰·태블릿) 실사용 검증** — 앱만으로 못 하는 유일한 일. 실제 숙소에서 한 판
   돌려보고 글자 크기·버튼 크기·태블릿 세워두는 동선을 조정한다.
2. **「5초 준다」 추가** — 아래 후보 검토 2순위. 주제 덱 60~80개만 새로 쓰면 된다.

~~초성 퀴즈~~ → **v0.16.0에서 완료.**
~~상식 퀴즈~~ → **v0.25.0에서 완료**(후보 표에는 없던 것. 사장님이 직접 요청).

#### 🎮 게임 후보 검토 결과 (2026-08-13 · 사장님 질문 「추가할 만한 게임이 뭐가 있어요?」)

기준은 「새 게임을 고를 때의 기준」 4항목 + **태블릿이 공용 화면이 됐으므로 「다 같이 화면을
보고 맞히는」 게임이 새로 열렸다**는 점(v0.14.0~v0.15.0).

| 순위 | 게임 | 재사용 틀 | 왜 |
|---|---|---|---|
| ~~1~~ | ~~초성 퀴즈~~ | — | **✅ v0.16.0 완료.** 예상대로 새 데이터 0개였다. 단 `deck` 모드 추가가 아니라 **별도 도구(`cho`)** 로 갔다 — 이유는 세션 이력 참조 |
| 2 | **5초 준다** (「초록색 물건 3개, 5초 안에!」) | `deck` + `race-*` | 규칙이 제목에 다 들어있다. 앱이 주제 카드·5초 타이머·성공/실패·자동 순위를 **전부** 한다. 주제 덱 60~80개만 새로 쓰면 됨 |
| 3 | **손병호 게임** (「나는 ~ 해본 적 있다」) | `smile` 카운터 | 핵심이 **문장 덱**이라 앱이 제일 잘하는 일. 접힌 순서가 곧 순위. ⚠️ 문장 수위는 무난하게(술 안 마시는 사람도 그대로 쓴다는 원칙) |
| 4 | **고요 속의 외침** | `deck` 모드 추가 | 제작비 거의 0. ⚠️ 이어폰이 인원수만큼 필요한 게 유일한 걸림돌 |
| 5 | 눈치게임 | `smile` 틀 | 저비용이지만 **앱이 해주는 게 적어** 기준 ②를 아슬아슬하게 넘는다 |

**초성 변환** — 새 데이터 없이 `WORD_DECKS`를 그대로 쓴다(실제로 돌려서 확인함):
```js
const CHO = 'ㄱㄲㄴㄷㄸㄹㅁㅂㅃㅅㅆㅇㅈㅉㅊㅋㅌㅍㅎ';
const cho = w => [...w].map(c => { const i = c.charCodeAt(0) - 0xAC00;
  return (i >= 0 && i < 11172) ? CHO[Math.floor(i/588)] : c; }).join('');
// 삼계탕 → ㅅㄱㅌ · 아나운서 → ㅇㄴㅇㅅ
// 가는 말이 고와야 오는 말이 곱다 → ㄱㄴ ㅁㅇ ㄱㅇㅇ ㅇㄴ ㅁㅇ ㄱㄷ
```

⚠️ **안 넣기로 본 것들과 그 이유** (다시 제안되기 쉬우니 남겨둔다)
- **라이어 게임** — 규칙은 짧지만 「지목 → 투표 → 정산」 골격이 v0.13.0에서 들어낸 두 게임과
  **똑같다.** 같은 이유로 판이 식을 위험이 크다.
- **왕게임 · 벌칙 룰렛류** — 벌칙이 본체라 「벌칙은 앱이 다루지 않는다」 결정과 충돌하고
  순위도 안 나온다.
- **음악 인트로 퀴즈** — 단일 HTML에 음원을 넣을 수 없어 **구조적으로 불가**.
- **텔레스트레이션 · 어몽어스류** — 각자 폰에 비밀 정보를 뿌려야 해서 동기화 예외를 되살린다.

### ⚠️ 미해결 / 결정 대기

- **순위 입력은 호스트 전용**으로 막아둠(동시 입력 충돌 방지). 참가자도 넣게 할지 미결정.
- **스마일~! 카메라**: 웹은 줌 배율 제어가 기기마다 안 먹으므로 "클로즈업 배율" 조건을 앱이 강제 못 한다.
  기본 카메라 앱으로 찍고 앱은 카운터/술래 지정만 하는 현재 방식 유지 권장.
- 실기기(폰) 실사용 미검증 — 실제 숙소에서 한 번 돌려보고 조작감·글자 크기 조정 필요.

---

## 세션 작업 이력

> 새 세션은 이 섹션을 읽어 최근 맥락 파악. 작업 완료 후 갱신할 것.

### v0.32.0 (2026-08-26) — 🏅 개인 랭킹 하나로 + 🗂 지난 기록 ★★★

사장님: "**개인전 점수를 위주로** 진행을 할 건데 팀을 랜덤으로 짤 수 있으니까 **팀이 됐을 때도
팀 점수를 받으면 개인 점수에 반영돼서 랭킹이 맺어지는 시스템**으로 구축하면 될 것 같아.
점수에 따라서 뭘 받거나 하는 거는 나중에 개별적으로 정하면 되는 거니까.
해당 점수 정보들은 기본적으로 방을 만들어서 **같은 공간에 있을 때 유지**되는 걸로 해줘.
그리고 만약 **방에서 모두 나가더라도 해당 점수들은 다시 확인할 수 있도록 메인화면에
시간과 일자 등을 표시**해주는 게 맞을 것 같아."

## ① 팀 점수 → 개인 점수는 **이미 그렇게 돌고 있었다** — 안 보였을 뿐이다

`calcPoints()` 가 처음부터 팀 순위 점수를 팀원 전원에게 똑같이 주고 있었다.
문제는 **화면 어디에도 그게 안 보였다**는 것이다 — 리더보드는 합계만 보여주니
팀전을 이겼는데 내 점수가 왜 올랐는지 알 수가 없다.

→ **`scorePt(sc, pid)`** 를 만들어 게임 기록마다 **「+15」처럼 내가 받은 점수**를 붙였다.
   팀전에는 「팀전」 칩도 같이 붙는다. 점수 방식 카드에도 한 줄 박았다 —
   「**팀전도 개인 점수로 쌓입니다** — 팀 순위 점수를 팀원 전원이 똑같이 받아요」.
⚠️ `scorePt` 는 `calcPoints` 와 **같은 규칙**이어야 한다. 두 벌로 갈라지면 표시와 합계가 어긋난다.
⚠️ **팀전은 그 시점 `assign` 스냅샷으로 판단한다.** 현재 `S.room.teams` 를 보면
   팀을 다시 짜는 순간 과거 점수가 통째로 뒤바뀐다. 🧪 음성 테스트로 확인했다.

## ② 🗂 지난 기록 — **방 노드는 언젠가 사라진다**

이게 이번 작업의 진짜 알맹이다. 점수는 RTDB 방 노드에 있고, 그 방은 모두 나간 뒤
시간이 지나면 **코드가 재사용되면서 통째로 덮인다.** 「모두 나가도 다시 확인」을
방 노드로 풀 수는 없다.

→ 최종 순위를 **`localStorage`(`partygames_rooms`)** 에 따로 갈무리한다. 자세한 배선은
   위 「지난 기록」 섹션. 홈 화면에 **날짜·시각·1등·인원·판 수**가 줄로 뜨고 누르면 상세가 열린다.
⚠️ **`vHist()` 는 `S.room` 을 안 본다** — 저장된 값만으로 그린다. 그게 이 기능의 전부다.
   E2E 가 `S.room = null` 로 만들어 놓고 열리는지 본다.
⚠️ **지금 들어가 있는 방의 기록은 못 지운다.** 살아 있는 값이라 지워도 다음 렌더에 다시 쌓인다
   (검사를 짜다가 실제로 걸렸다). ✕ 를 아예 안 그리고 「지금 방」 칩을 붙였다.

## ③ 방 재사용 12시간 → **48시간**

「같은 공간에 있을 때 유지」를 따져보니 12시간이 모자랐다 — **숙소는 하룻밤을 잔다.**
밤에 놀고 다음 날 오후에 이어 하면 12시간이 쉽게 넘고, 그 사이 남이 같은 코드를
받아가면 그 방 점수가 통째로 날아간다.

## 🐛 그러다 리더보드의 태그 구조가 깨져 있는 걸 찾았다

`vBoard()` 에서 `.two` 를 닫는 `</div></div>` 가 **게임 기록 map 안**에 들어가 있었다.
기록이 1건이라도 있으면 그 뒤 내용이 오른쪽 칸 밖으로 새고, 0건이면 아예 안 닫혔다.
브라우저가 알아서 고쳐 그리는 바람에 **가로 검사(landscape)도 통과하고 있었다.**
⚠️ **`.two` 분할·마감은 항상 최상위 형제 사이에 둘 것** — v0.29.0 에 적어둔 함정을
   내가 그대로 다시 밟았다. 템플릿 리터럴 안이라 편집기도 안 잡아준다.

**검증** — smoke-e2e **283항목**(17항목 신설): 팀 1등 점수를 팀원 전원이 동일 수령 ·
진 팀도 팀 순위대로 · **개인전과 팀전이 한 리더보드에 합산** · **팀을 다시 짜도 과거 점수 유지** ·
한 판이 나에게 준 점수 · **점수가 있으면 기기에 기록이 남음** · 최종 순위 내림차순 ·
진행한 게임 동반 저장 · 날짜·시각 생성 · **홈 화면에 지난 기록** · **날짜와 시간 표기** ·
1등·인원·판 수 요약 · **방이 사라져도(`S.room=null`) 상세가 열림** ·
**지금 있는 방 기록은 못 지움** · 그 줄엔 ✕ 없음 · 나간 뒤엔 경고 후 삭제.
🧪 음성 테스트 3건: 팀 점수를 한 명에게만 → ✕FAIL / 과거 `assign` 대신 현재 팀을 봄 → ✕FAIL /
`snapRoom()` 제거 → exit 1.
landscape 17항목 · yut-verify 70/81항목 회귀 없음.
📸 폰 400×900 · 가로 태블릿 1024×768 — 홈(기록 2줄) · 상세 · 리더보드(`+15` 표시) 실측.

### v0.31.0 (2026-08-26) — ⚠️ 되돌릴 수 없는 것 앞에 경고창 ★★

사장님: "**방 나가기나 게임 나가기를 할 때 어떤 경고창 떴으면 좋겠어. 점수나 이런 게
사라질 수 있다는 경고.**"

## 물어보긴 하고 있었다 — 문제는 **무엇이 사라지는지 안 적혀 있던 것**

`confirm()` 이 다섯 군데 있었는데, 「진행 중인 게임을 그만둘까요?」처럼 **한 줄**이라
뭘 잃는지가 안 보였다. 참가자의 「방 나가기」는 아예 **아무것도 안 물어보고 나갔다.**

## 🐛 그러다 진짜 손실을 하나 찾았다 — 참가자가 나가면 **점수가 영영 끊긴다**

`joinRoom()` 은 들어올 때마다 **새 `pid`** 를 발급하고, `leaveRoom()` 은 `clearSession()` 으로
복구용 세션까지 지운다. 그래서 나갔다 다시 들어오면 **옛 `pid` 의 `scores` 기록이 고아가 되어**
`calcPoints()` 가 못 잡는다 — 방에 데이터는 남아 있는데 그 사람 점수만 0이 된다.
→ 경고에 **지금 몇 점인지 숫자로** 적는다. 「지금까지 쌓은 **내 점수 5점이 사라져요**」

## `confirm()` 을 앱에서 통째로 걷어냈다

PWA 로 띄우면 시스템 창이라 **앱이 깨진 것처럼 보이고**, 여러 줄을 못 적는다.
→ 상태 하나(`S.ask`)로 도는 인앱 경고창. 자세한 배선은 위 「경고창」 섹션.
다섯 군데 전부 옮겼다: 방 나가기 · 게임 그만두기 · 점수 전부 지우기 · 제시어 기록 초기화 · 윷판 정리.

⚠️ **`render()` 가 뷰 HTML **뒤에 이어 붙인다**.** `bind()` 는 `#view` 만 훑으므로 그 밖에 있으면
   버튼이 안 먹고, 뷰 함수 안에 넣으면 `.two` 래퍼 안에 갇힌다. 그래서 `render()` 자리다.
⚠️ **`renderSig()` 에 `S.ask?.go`** — 빼면 창이 뜨지도 닫히지도 않는다.
⚠️ **`await` 하지 않는다.** `act()` 는 switch 를 동기로 들어가므로 `ask-yes` → `act(q.go)` 가
   즉시 상태를 바꾼다. 검증 시나리오가 다음 줄에서 바로 볼 수 있어야 한다.

## ⚠️ 검사를 한 번 고쳤다 — **배경에도 `ask-no` 가 있어서 버튼을 지워도 통과했다**

「취소 버튼이 있다」를 `includes('data-act="ask-no"')` 로 봤는데, **배경(`.askov`)에도 같은
`data-act`** 가 달려 있다(바깥을 눌러 닫는 용도). 버튼을 통째로 지워놓고 돌려보니 그냥 통과했다.
→ `/<button[^>]+data-act="ask-no"/` 로 **버튼인지**를 본다.
⚠️ **문자열 포함 검사는 「그 문자열이 다른 데도 있는지」를 먼저 볼 것.** v0.22.1 의
   「핸들러를 직접 부르는 검사는 UI 존재를 보장하지 않는다」와 같은 병이다.

**검증** — smoke-e2e **266항목**(경고 13항목 신설): 아무것도 안 했으면 그냥 나감 ·
진행 중이면 창이 뜸 · **무엇이 사라지는지 적혀 있음** · 예/아니오 **버튼** 둘 다 ·
취소하면 게임·방이 그대로 · 확인해야 나감 · **진행자에게는 「순위를 저장할 수 없다」** ·
**참가자에게는 「내 점수가 사라진다」** · 화면이 바뀌면 창이 닫힘 · 점수 지우기도 경고 ·
**앱에 `confirm()` 0건**.
🧪 음성 테스트 3건: 경고 없이 바로 나가게 되돌림 → 방이 사라져 exit 1 /
`lose[]` 를 비움 → ✕FAIL / 취소 버튼 제거 → ✕FAIL.
📸 폰 400×900(진행자·참가자·게임) · 가로 태블릿 1024×768 실측 — 태블릿에서는 글자를 키운다
(**누르는 사람만 보는 창이 아니다** — 상 위에 세워둔 화면이라 다 같이 읽는다).
landscape 17항목 · yut-verify 70/81항목 회귀 없음.

### v0.30.1 (2026-08-25) — 🔗 한 단어 릴레이 추가 (9종 → 10종) ★★★

사장님: "새로운 게임을 하나 만들 건데 단어를 맞추는 거고 팀으로 하는 게임이야. **3대 3**이
기본이 되는 거고 서로 팀이 번갈아 가면서 진행하는 게임이고 1팀 3명 중에 **1명은 문제를 맞추는
사람**이고 **2명은 문제를 내는 사람**인데 2명은 문제를 보고 **한 명씩 한 단어로 서로 릴레이**하면서
정답을 설명하면 돼. **단어가 아니거나 두 단어를 말하게 되면 안 되는** 거야."

이어서: "**그냥 문제들만 만들어 주면 돼 또는 규칙을 적어 주거나.** 왜냐하면 **단어를 번갈아
말하는 건 우리끼리 알아서 할 문제**라서."

## ★★ 이 게임의 핵심 교훈 — **앱이 할 일과 사람이 할 일의 경계**

v0.30.0 에서 나는 앱이 **맞히는 사람을 뽑아주고**(`ensureGuesser`), **설명 순서를 그려주고**,
**「✋ 반칙」 전용 버튼**까지 만들었다. 규칙을 코드로 옮기면 좋은 도구가 되는 줄 알았다.
사장님이 바로 잘라냈다 — **그건 둘러앉은 사람들이 이미 하고 있는 일이다.**

> 앱이 해야 할 일 = **제시어 · 시간 · 점수** (사람이 못 하거나 귀찮은 것)
> 사람이 할 일 = **누가 맞힐지 · 누가 먼저 말할지 · 반칙인지** (앉은 자리에서 3초면 정해진다)

⚠️ **「기존 도구 틀을 재사용한다」의 진짜 의미가 이것이다.** 재사용을 잘했다는 건
   `deck` 도구를 불렀다는 뜻이 아니라 **덧붙인 게 없다**는 뜻이다. 새 게임을 넣을 때
   「이 규칙을 앱이 강제해줄까?」가 떠오르면 먼저 **그 규칙을 사람이 못 지키는지** 물을 것.
   순서 지키기·반칙 판정은 다 같이 보고 있으므로 사람이 훨씬 잘한다.

⚠️ **CLAUDE.md 의 「새 게임을 고를 때의 기준」 ②(앱이 해줄 일이 있어야 한다)를
   거꾸로 읽으면 안 된다.** 그건 「앱이 할 일이 **있어야** 한다」이지 「**많아야** 한다」가 아니다.

## 결과 — 덱 도구의 **모드 하나**, 그것도 아주 얇게

흐름이 「몸으로 말해요」와 **완전히 같다**(팀별 턴제 · 제한 시간 · 맞춘 개수 · 팀 순위).
`deck` 도구에 `mode:'relay'` 를 더했고, 그 모드가 하는 일은 **셋뿐**이다:

| | 몸으로 말해요 | 한 단어 릴레이 |
|---|---|---|
| 못 보는 사람 | **상대팀 전원** | **우리 팀의 맞히는 사람 1명** |
| 범위 | `use:'body'` 12종 | **`use:'relay'` 12종** (속담·사자성어 제외) |
| 방식 고르기 | 몸으로/한글자 | **없음(고정)** |

그 외 진행·타이머·패스·점수·순위는 전부 기존 코드 그대로다. 화면에 더한 것은
준비 화면의 **「이 게임 방식」 3줄**과 진행 화면의 **한 줄 알림**뿐 —
사장님이 말한 「규칙을 적어 주거나」가 이것이다.

⚠️ **`.priv` 는 그대로 유지해야 한다.** 「누가 화면을 못 보는가」가 팀 밖에서 **팀 안으로**
   들어왔다 — 맞히는 사람이 우리 팀 옆자리에 앉아 있으므로 태블릿에서 커지면 그 판이 끝난다.
   E2E 에 못을 박았고 🧪 음성 테스트로 확인했다(`.priv` 를 떼니 3항목 ✕FAIL → exit 1).

⚠️ **반칙은 「패스」로 처리한다.** 전용 버튼을 두면 패스와 기능이 똑같은 버튼이 둘이 된다
   (둘 다 「이 문제 넘김·0점」). 판정은 어차피 상대팀이 눈으로 한다.

## 덱 — **새 데이터 0개.** `use` 태그를 하나 더 달았을 뿐

`WORD_DECKS` 14종 중 **12종에 `'relay'`** 를 달았다. 뺀 둘은 **속담·사자성어** —
문장과 한자라 「한 단어씩 번갈아」로 이어지지 않는다(「가는 말이 고와야…」를 한 단어로 설명할 수 없다).
기본 범위는 `RELAY_CATS` 8종(동물·음식·사물·직업·영화·캐릭터·가게·자연) — **손에 잡히는 명사**라야
한 단어 힌트가 붙는다. ⚠️ `pl-cat` 핸들러에도 같은 필터를 걸었다(직접 조작 방어).

## 🐛 스샷으로 잡은 것 — **준비 화면이 가로 태블릿에서 269px 넘쳤다**

역할 카드가 얹히면서 시작 버튼이 화면 밖으로 밀렸다. **시작 버튼이 안 보이면 게임을 시작할
수가 없다.** → v0.29.0 의 `.two` 를 덱 준비 화면에 적용(왼쪽 = 팀원·방식 / 오른쪽 = 안내·버튼).
몸으로 말해요·한글자도 같이 좋아졌다. 역할 카드를 걷어낸 뒤에도 `.two` 는 그대로 뒀다.
⚠️ **덱 도구는 `run` 만 찍어보면 놓친다** — 방식을 알려주는 **준비 화면이 제일 길다.**
   `landscape.js` 에 준비 화면 검사를 따로 추가했다.

## ⚠️ 검사를 한 번 고쳤다 — **한 번만 보면 운으로 통과한다** (지금은 사라진 코드지만 교훈은 남는다)

v0.30.0 의 「맞히는 사람이 그 팀에서 정해진다」를 한 번만 확인했는데, 뽑기를 **전원에서
하도록** 되돌려놓고 돌려보니 **그냥 통과했다**(절반 확률). 전 팀 × 40회를 돌리도록 바꾸고
나서야 `남의 팀 49회` 로 잡혔다.
v0.17.0·v0.18.0·v0.24.0 에 이어 **같은 병이 네 번째**다 — 뽑기가 끼는 검사는 반복하거나 고정할 것.

**검증** — smoke-e2e **253항목**(릴레이 16항목): 등록·최소 6명 · **속담·사자성어 미노출** ·
방식 고르는 칸 없음 · **상세가 「한 단어씩 번갈아」를 설명** · **상세가 반칙 기준을 적어줌** ·
준비 화면이 방식을 다시 알려줌 · **앱이 맞히는 사람을 안 뽑음** · **제시어가 `.priv` 안에만** ·
**반칙 전용 버튼 없음(패스로 처리)** · 맞춘 것만 셈 · 팀 순위로 전환.
landscape **17항목**(준비 화면 1항목 신설) — 1024×768 · 1366×1024 · 1180×820 통과.
yut-verify 70/81항목 회귀 없음.
📸 실측: 가로 태블릿 1024×768 전 단계 스크롤 없음 · 폰 400×900 **가장 긴 제시어**.

### v0.29.0 (2026-08-23) — 📐 화면 전체를 가로로 (윷놀이 말고도) ★★★

사장님: "태블릿으로 했을 때는 웬만하면 가로 화면이니까 **윷놀이 말고도 전체 가로로도**
할 수 있게 해 줘야 돼."

## 먼저 쟀다 — **거의 모든 화면이 넘치고 있었다**

1024×768(실사용 뷰포트 1024×681)에서 화면별 세로 넘침을 전수 측정했다:

| 대기실 | 게임 목록 | 게임 상세 | 도구 설정 | 순위 입력 | 리더보드 | 설정 |
|---|---|---|---|---|---|---|
| **610px** | 511px | 235~558px | 329~453px | 312px | 210px | 287px |

가로는 남는데(#app 880 / 화면 1024~1366) 세로로만 쌓아서 그렇다.

## `.two` — 폰에서는 **아무것도 하지 않는** 2단 틀

```css
@media (min-width:860px) and (min-aspect-ratio:5/4){
  .two{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:0 22px;align-items:start}
}
```
⚠️ `.two` 는 그냥 `<div>` 라 **폰·세로에서는 자식이 순서대로 쌓인다** — 회귀가 없다.
   가로로 놓였을 때만 2단이 된다. 화면마다 `<div class="two"><div>…</div><div>…</div></div>`
   로 감싸기만 하면 된다.

적용: 홈 · 대기실 · 게임 상세 · 순위 입력 · 리더보드 · 설정 ·
진행 도구(초성·상식·노래) · 도구 설정(상식·노래·몸으로).

## 결과 — **다 같이 보는 화면은 전부 한 화면에 들어간다**

| | 전 | 후 |
|---|---|---|
| 진행 화면 9종 | 최대 453px 넘침 | **전부 0** |
| 대기실 | 610px | **0** |
| 순위 입력 · 설정 | 312 · 287 | **0** |
| 리더보드 | 210px | 80px |
| 게임 목록 · 상세 | 511 · 558 | 128 · 127~164 |

⚠️ **혼자 훑는 화면(목록·상세)은 조금 넘쳐도 된다** — 진행자가 손에 쥐고 넘긴다.
   스크롤이 0이어야 하는 건 **뒤에서 보는 사람이 스크롤을 못 하는** 화면뿐이다.

## ⚠️ 밟은 함정 넷

1. **분할 표시를 카드 안쪽에 넣으면 구조가 깨진다.** `</div><div>` 를 `<div class="card">`
   안에 넣었더니 `.two` 의 자식이 **1개**가 되어(디버그로 확인) 2단이 안 먹었다.
   조건식(`${x ? \`…\`}`) 안도 마찬가지 — 조건이 거짓이면 div 가 사라져 무너진다.
   **분할은 항상 최상위 형제 사이에 넣는다.**
2. **좌우 균형을 안 보면 오히려 나빠진다.** 게임 목록을 개인전/팀전으로 나눴더니
   7 vs 2 라 128 → **264px** 로 늘었다. 카드 2열이 정답이었다.
3. **참가자 줄은 2열이 안 된다.** 아바타+이름+칩+점수+✕ 라 폭이 모자라 옆 칸을 침범했다.
   대신 「사회자로 쓰기」를 오른쪽으로 옮겨 균형을 맞췄다.
4. **뒤에 오는 미디어쿼리가 앞을 덮는다.** `max-height:860px` 블록이 `main` 여백을
   되돌려놔서 10px 이 계속 남았다 — 같은 값으로 맞췄다.

**검증** — `tools/scenarios/landscape.js` 신설(15항목): 다 같이 보는 화면 전부 ·
게임 목록 2열 · 상세 2단 · 가로 스크롤 0. 1024×768 · 1366×1024 · 1180×820 통과.
🧪 음성 테스트: `.two` 를 `display:block` 으로 되돌리니 **5항목 실패**.
smoke-e2e 234항목 + yut-verify 72/82항목 회귀 없음. 폰·세로 태블릿도 변화 없음(`.two` 가 무동작).

### v0.28.0 (2026-08-23) — 🎲 던지기가 먼저 + 가로 태블릿 판정 기준 수정 ★★★

사장님: "태블릿에서는 **가로 모드로도 잘 볼 수 있어야** 되는데 세로라서 변경해줘야 될 것 같아.
그리고 **윷이나 모가 나왔을 때 바로 말을 움직일 수 있는 게 아니라 한 번 더 던지는 게 먼저**고
각각 원하는 패로 보낼 수 있도록 하는 게 원래 룰이야."

## 🐛 ① 큰 태블릿을 가로로 놓으면 **2단이 안 걸렸다**

v0.26.2에서 2단 배치를 넣을 때 조건을 **`max-height:860px`** 으로 걸었다.
「가로 태블릿은 세로가 짧다」고 본 것인데 — **큰 태블릿을 가로로 놓으면 세로도 길다.**

| | 세로 | 2단? |
|---|---|---|
| 아이패드 가로 1024×768 | 681 | ✅ |
| **아이패드 프로 가로 1366×1024** | **937** | **❌ → 세로 배치가 그대로** |

→ 기준을 높이가 아니라 **「가로로 놓였는가」**로 바꿨다:
   `@media (min-width:860px) and (min-aspect-ratio:5/4)`.
   세로 태블릿(820×1180 = 0.69)과 폰은 비율에서 자연히 걸러진다.
⚠️ **화면 비율로 판단할 것을 높이로 판단하면 이런 구멍이 난다.** 「가로로 놓였다」가
   조건이면 `aspect-ratio` 로 쓰는 게 맞다.

같이 고침 — 넓은 화면에서 판이 `#app`(880px)에 갇혀 520px 에서 멈췄다.
`min-width:1200px` 에서만 **판만** 좌우로 빠져나가게 했다(다른 화면은 880 유지 — 글줄 길이 때문).
실측: 1366×1024 에서 판 **520 → 620px**, 말 지름 **37 → 44px**. 가로 스크롤 없음(검사 있음).

## 🐛 ② 윷·모가 나와도 바로 말을 옮길 수 있었다

실제 윷놀이는 **던질 것을 다 던진 뒤에** 모아둔 값을 말들에 나눠 준다.
중간에 옮기게 하면 「도로 이 말, 윷으로 저 말」 하는 **배분 자체가 성립하지 않는다.**

→ `yutMoveFrom` 에 `if (m.canThrow) return` 가드를 넣고, 화면에서도 그 동안
   **초록 셀·「새 말 내보내기」를 아예 안 띄운다.** 안내도 「한 번 더 던지세요」로 바뀐다.
⚠️ **잡았을 때(`canThrow=true`)도 똑같이 적용된다** — 잡으면 던지기가 먼저다.
⚠️ 초록 셀이 없다고 「움직일 말이 없어요 — 턴 넘기기」를 띄우면 안 된다(오해를 준다).
   그 문구는 `!canThrow` 일 때만 나온다.

⚠️ **검증기의 자동 대국 봇 3곳을 같이 고쳐야 했다.** 안 고치면 봇이 계속 이동을 시도하고
   앱이 막아서 **무한루프**가 된다(실제로 3000수 상한에 걸려 60판 전부 실패로 잡혔다).
   봇도 「던질 수 있으면 먼저 던진다」로 바꿨다 — **규칙을 바꾸면 검사기의 규칙도 바뀐다.**

**검증** — smoke-e2e **234항목** + yut-verify **72항목**(가로 태블릿 82항목):
윷 나오면 이동 차단 · 초록 셀 미표시 · 안내 문구 · 다 던지면 값 2개 누적 ·
**각각 원하는 말에 배분** · 잡은 뒤에도 던지기 우선 ·
**가로로 놓으면 2단** · 세로면 1단 · 가로 스크롤 없음 · 판 420px 이상.
📸 1366×1024 · 1180×820 · 1024×768 · 820×1180 실측.

### v0.27.1 (2026-08-23) — ✋ 영어 지적에 손이 닿게 ★★★

사장님: "훈민정음 윷놀이라서 서로 팀별로 누군가 **영어를 사용했을 때 벌칙으로 말 하나를
집으로 되돌리는 게 필요**해"

## ⚠️ 기능은 v0.22.0부터 다 있었다 — **손이 닿지 않았을 뿐이다**

지적 → 판정 → 되돌릴 말 고르기 → 집으로. 전 과정이 구현돼 있고 E2E도 통과하고 있었다.
문제는 **버튼까지 갈 수가 없었다**는 것이다:

| | 전 | 후 |
|---|---|---|
| 각자 폰 입구 | **홈 화면에만** 배너 | **홈 + 대기실** 양쪽 |
| 태블릿 | 지적 버튼 **없음** | 판 아래에 **「✋ 잠시! — N팀이 영어를 썼어요」** |

⚠️ **참가자는 입장하면 `lobby` 에 머무른다.** 그런데 입구가 `vHome` 에만 있었으니,
   지적하려면 홈으로 되돌아가야 했다 — 술 마시며 하는 자리에서 아무도 안 한다.
   **기능이 있는 것과 쓸 수 있는 것은 다르다.** 이번 건이 딱 그 사례다.

⚠️ **태블릿 버튼은 「우리 팀은 못 누른다」 가드를 걸지 않는다**(폰의 `yut-halt` 와 다르다).
   공용 화면이라 누른 사람이 곧 상대팀이다 — 진행 중인 팀이 자기를 지적할 이유가 없다.
   대신 판정 화면에서 **이름 대신 팀**을 말한다(`halt.board`).

배너는 `yutBanner()` 헬퍼로 빼서 홈·대기실이 같은 것을 쓴다.
⚠️ `vHome` 은 방에 들어가기 **전에도** 그려진다 → `S.room?.yut` 로 받을 것(v0.22.0에 부팅을 터뜨린 자리).

**검증** — smoke-e2e 233항목 + yut-verify **61항목**(가로 태블릿 69항목), 도달성 7항목 신설:
태블릿 판에서 바로 지적 · **대기실에 배너** · 홈에도 유지 · 배너가 「잠시!」를 안내 ·
태블릿 지적이 진행 팀을 대상으로 · 판정 화면이 팀명으로 말함 · 취소 시 말 안 빠짐.
📸 1024×768 실측 — 판·조작·지적 버튼이 **한 화면**에 들어간다.

### v0.27.0 (2026-08-23) — 🎲 말 고르기 + 3D 말 ★★★

사장님: "**말 고르는 것도** 추가해줘 그리고 말을 **3D나 퀄리티 좋은 걸로** 찾아서 넣어줘"

⚠️ 「말 고르는 것」이 **생김새 고르기**인지 **옮길 말 고르기**인지 갈려서 먼저 물었다.
   답은 **생김새**. (옮길 말은 이미 판에서 칸을 탭해 고르고, 업힌 말은 규칙상 같이 간다.)

## 말 모양 4종 — 색약 대응이기도 하다

`YUT_SKINS` = **바둑돌 · 유리구슬 · 나무말 · 네온**. 설정 화면에서 **팀마다** 고른다.
⚠️ **색만으로 팀을 나누면 색약인 사람이 구분을 못 한다.** 모양이 두 번째 단서다.
   그래서 **안 고르고 시작해도 팀마다 기본 모양이 다르게** 배정된다(`yutSkinOf`).
   대부분은 안 고르고 시작하므로 기본값이 진짜 기본값이어야 한다.

판 위의 말과 설정 화면 미리보기, 팀 박스의 점이 **같은 클래스(`sk-*`)** 를 쓴다 —
한 군데만 고치면 세 곳이 같이 바뀐다.

## 3D는 CSS 로 낸다 (외부 이미지 아님)

⚠️ **이미지를 받아 쓸 수 없다** — 인터넷 없이 도는 앱이고 단일 HTML 이다.
   그래서 그라데이션·하이라이트·그림자로 입체를 만든다. 용량 0, 크기를 키워도 안 깨진다.
- 바둑돌 = 위에서 빛 + 아래 그늘 + 안쪽 그림자
- 유리구슬 = 점 스페큘러 + 아래쪽 반사광
- 나무말 = 모서리 각진 토큰(`border-radius:28%`) + 윗면 하이라이트
- 네온 = 발광(`box-shadow` 글로우) — 앱 컨셉과 같은 결

⚠️ **`color-mix()` 금지 규칙 때문에 톤은 JS 가 계산한다** — `pcVars(hex)` 가
   `--pc / --pcL / --pcD / --pcGlow` 를 rgb 로 내려준다(`gcVars` 와 같은 방식).
⚠️ **말에 인라인 `background` 를 주면 안 된다** — shorthand 라 모양의 그라데이션이
   통째로 지워져 **색만 남는다.** 실제로 그렇게 만들었다가 고쳤고, E2E 에 못을 박았다.
   🧪 음성 테스트: 인라인 background 를 되살리니 exit 1.

**검증** — smoke-e2e 233항목 + yut-verify **54항목**(가로 태블릿 62항목):
모양 4종·키 중복 0 · **기본값이 팀마다 다름** · 설정 화면에 고르는 버튼 · 팀 수만큼 줄 ·
고르면 바뀜 · **방 노드에 저장** · 판 위 말에 모양 클래스 · 팀 박스 미리보기 ·
**인라인 background 없음** · 색 톤 3가지 전달.
📸 4팀 설정 화면 · 4팀 판(1024×768) 실측 — 네 팀이 색과 모양 **둘 다** 다르다.

### v0.26.4 (2026-08-23) — 🎲 말판에서 글씨를 빼고 완성도를 올렸다 ★★

사장님: "넣지말고 **빼** 모두 아는 규칙이야 그리고 **말판 퀄리티 올려줘**"

## 판에서 글씨를 전부 뺐다

v0.26.3 에서 `지름`·`방`·`날` 을 한글로 바꿔 놨는데, 애초에 **설명이 필요 없는 규칙**이었다.
진짜 윷판도 글씨가 없다 — 모서리 4곳과 방은 **칸을 크게 그려서** 구분한다.
→ 라벨 제거. 대신 `.cell.big`(모서리 4 + 방)과 **`.cell.hub`(방, 제일 크게)** 로 나눴다.
⚠️ **검사기에 「판 칸에 글씨가 없다」를 못 박았다** — 누가 다시 넣으면 exit 1.
   🧪 음성 테스트: 「방」을 되살리니 `남아 있는 글씨: 방` 으로 잡았다.

## 판 완성도

이 화면은 상 위에 세워두고 **60~100cm 에서 다 같이 본다.** 그 거리에서 읽히는 게 기준이다.

| 무엇 | 전 | 후 |
|---|---|---|
| 판 바닥 | 단색 `rgba(0,0,0,.3)` | 그라데이션 + 안쪽 그림자(깊이) |
| 길(선) | `stroke-width .7` · `--line2` | **1.2** · 밝기 상향 — 얇으면 「점 무더기」로 보인다 |
| 칸 | 단색 | 그라데이션 + 위쪽 하이라이트 + 그림자(떠 보이게) |
| 모서리·방 | 앰버 테두리만 | 앰버 **글로우** + 방은 한 단계 더 크게 |
| 움직일 수 있는 칸 | 정적인 민트 테두리 | 민트 글로우 + **고동치는 링** |
| 말 | 평평한 원 | 광택(radial-gradient) + 흰 실선 링 + 그림자 |

⚠️ **고동치는 링은 `transform`/`opacity` 만 움직인다.** `box-shadow` 애니메이션은 리페인트라
   비싸다(v0.2.1 성능 원칙). `prefers-reduced-motion` 은 전역 규칙이 이미 꺼준다.
⚠️ **말 색은 인라인 `background-color` 로 준다.** `background` shorthand 로 주면
   CSS 의 광택 `background-image` 가 지워진다 — 색만 나오고 입체감이 사라진다.
⚠️ **흰 실선 링을 두른 이유** — 팀 색이 6가지라 어떤 칸 위에 올라갈지 모른다.
   **4팀 앰버 말이 앰버 모서리에 올라가는 최악 케이스**까지 실제로 그려서 확인했다.

**검증** — smoke-e2e 233항목 + yut-verify **48항목**.
📸 2팀·4팀 · 가로 태블릿 1024×768 실측.

⚠️ 작업 중 `git checkout index.html` 로 되돌리다 **커밋 안 한 작업을 통째로 날렸다.**
   음성 테스트 뒤 원복은 **미리 복사해 둔 파일(`cp`)** 로 할 것 — `git checkout` 은
   그 파일의 커밋 안 된 변경을 전부 지운다.

### v0.26.3 (2026-08-22) — 🎲 말판 글씨가 기기에서 깨지던 것 ★★

사장님: "말판에 **이상한 글씨**도 있고 그래"

지름길 모서리(5·10)에 **`⤢`(U+2922)** 를 라벨로 쓰고 있었다. 「보조 화살표 B」 블록에 있는
**희귀 문자라 폰트에 없다** — Pretendard 에도 없어서 기기·OS 에 따라 **네모(□)나
엉뚱한 글리프**로 나온다. 내 헤드리스 환경에서는 폴백 폰트가 그려줘서 안 보였다.

→ **`지름`** 으로 바꿨다. ⚠️ **판 위 글씨는 한글·숫자·영문만 쓴다.**
   지금 판에 있는 글씨는 `지름`(2곳) · `방`(중앙) · `날`(도착) 넷뿐이고 전부 한글이다.

같이 고침 — **칸 글씨가 9~12px 이라 멀리서 안 읽혔다.** 라벨은 전부 큰 칸(`.cell.big`)에
붙으므로 거기에 크기를 따로 줬다(폰 11px · 큰 화면 **17px**).

⚠️ **검사기에 못을 박았다** — 판 칸의 글씨를 훑어 **한글·숫자·영문이 아닌 코드포인트가
   있으면 실패**시킨다. 어떤 문자가 걸렸는지도 같이 찍는다.
   🧪 음성 테스트: `⤢` 를 되살리니 `깨질 문자: ⤢(U+2922)` 로 잡고 exit 1.
⚠️ 앱 전체를 훑어보니 나머지 기호(`→` `←` `↑` `↔` `≈`)는 기본 화살표·수학 영역이라 안전하고,
   `⬆️`·`↩︎` 는 이모지/텍스트 변형자를 붙여 쓴 것이라 그대로 뒀다.

**검증** — smoke-e2e 233항목 + yut-verify **47항목**(가로 태블릿 기준).

### v0.26.2 (2026-08-22) — 🎲 말판 2단 배치 + 업은 말 개수 표시 ★★★

사장님: "지금 **말이랑 판이 이상해**"

맞는 지적이었고, 원인이 두 개였다. 둘 다 **v0.26.1 에서 내가 만든 것**이거나 키운 것이다.

## 🐛 ① 가로 태블릿에서 말판이 **폰보다 작았다** (272px · 말 지름 11px)

세로가 모자라니(768px) 판을 `44vh → 40vh` 로 계속 줄여서 버튼을 넣었다.
그런데 **판은 다 같이 보는 것**이라 이 트레이드오프 자체가 틀렸다. 실측하면 이랬다:

| | 판 | 말 지름 |
|---|---|---|
| 폰 400×900 | 453px | 22px |
| **가로 태블릿 1024×768** | **272px** | **11px** |

**큰 화면인데 오히려 작았다.** 상 위에 세워두고 60~100cm 에서 보는 화면이 이러면 못 쓴다.

→ ⚠️ **가로 태블릿은 세로가 모자란 대신 가로가 남는다.** 1단으로 쌓으니 판을 줄일 수밖에
   없었던 것이다. `ylayout` 을 **`ytop`(팀·윷짝·결과칩) / `yboard`(판) / `ybot`(버튼·기록)**
   세 덩이로 나누고, `max-height:860px` 구간에서만 **2단**으로 배치했다 —
   왼쪽에 판, 오른쪽에 조작. 폰·세로 태블릿은 그대로 위에서 아래로 쌓인다.

   실측: 판 **272 → 520px**, 말 지름 **11 → 26px**, 버튼 3개 전부 화면 안(뷰포트 681).

⚠️ **「화면 안에 들어간다」만 검사하면 판을 줄여서 통과시킬 수 있다.** 실제로 그렇게 통과했다.
   그래서 검사에 **하한(420px 이상)** 을 같이 박았다.

## 🐛 ② 업은 말이 칸을 통째로 덮었다

같은 칸의 말을 하나씩 다 그리고 있었다(`.pcs` 가 `inset:-14%` 라 칸보다 크다).
말 4개가 업히면 2×2 덩어리가 칸을 덮어 **어느 칸에 있는지조차 안 보였다.**
업기는 흔하게 일어난다(자동 대국 60판에서 979회).

→ 팀별로 묶어 **말 하나 + 개수**로 그린다(빨간 말 + 「4」). 멀리서도 바로 읽힌다.
⚠️ 두 팀 말이 같은 칸에 있는 상태는 원래 없다(잡기가 상대를 집으로 보낸다) —
   그래도 팀별로 묶으므로 그런 상태가 와도 안 깨진다.

**검증** — smoke-e2e 233항목 + yut-verify **50항목**(레이아웃 4항목 신설):
판 420px 이상 · 업은 말이 개수로 표시 · 말이 칸보다 작음 · 버튼 3개 화면 안.
📸 가로 태블릿 1024×768 · 세로 820×1180 · 폰 400×900 실측.

### v0.26.1 (2026-08-17) — 🎲 윷놀이 전면 재검증 — 버그 2건 + 검증기 신설 ★★★

사장님: "이거 윷놀이 확인해줘 **제대로 검증해서 문제없이 게임할 수 있도록**"

v0.24.1 리뷰는 **코드를 읽어서** 버그를 찾았다. 이번엔 **돌려봐서** 찾았다 —
읽기로는 안 보이던 것 두 개가 나왔고, 둘 다 실제 게임에서 걸린다.

## 🧰 `tools/scenarios/yut-verify.js` 신설 (46항목)

smoke-e2e 의 윷 항목은 「규칙이 맞는가」를 본다. 이 검증기는 **읽기로 못 잡는 것**을 본다:

| 무엇 | 어떻게 |
|---|---|
| 판 밖으로 나가는 칸이 없나 | **이동 조합 150가지 전수**(모든 칸 × 1~5칸) |
| 경로에 순환이 없나 | 모든 칸에서 도(1)만 반복 → 반드시 완주하는지 |
| 판이 멈추지 않나 | **랜덤 자동 대국 60판**(약 5,700수) + 3팀 6판 |
| 상태가 깨지지 않나 | 매 수마다 말 4개·위치 유효성 검사 |
| **화면이 멈추지 않나** | 한 판을 실제로 그리며 **매 순간 누를 것이 있는지** |
| 레이아웃 | 태블릿 크기로 돌리면 판·조작 버튼이 화면 안에 있는지 |

```bash
node tools/demo.mjs tools/scenarios/yut-verify.js _y.html
node tools/shot.mjs --dom "/_y.html?demo=1"                      # 규칙·대국
node tools/shot.mjs --dom "/_y.html?demo=1" -w 1024 -h 768       # + 레이아웃
```

**결과: 규칙·판·데드락은 전부 깨끗했다.** 60판이 모두 승자가 나왔고(평균 93수),
잡기 357회·업기 979회·지름길 796회가 실제로 일어났다. 판이 멈추는 상태 0, 상태 깨짐 0.

## 🐛 ① 턴이 넘어가도 참가자 폰에 **이전 팀의 윷 결과가 그대로 남았다**

`yutTurnEnd()` 가 `m.last = null` 로 지우는데 `pushYut(...)` 패치에 **`last` 가 빠져 있었다.**
호스트는 로컬 상태를 쓰니 멀쩡하고, **참가자만** 서버 값을 그대로 그린다 →
다음 팀 폰에 앞 팀의 「윷!」이 떠 있다. 정작 **지금 던질 사람이 보는 화면**이다.

⚠️ **로컬에서만 지우고 patch 에 안 담는 필드가 있으면 참가자 화면만 옛 값을 본다.**
   호스트=진실원 구조에서 제일 나기 쉬운 버그다. 검사도 그 관점으로 짰다 —
   `pushYut` 을 감싸 **서버로 나가는 patch 를 모아서** 본다.

## 🐛 ② 가로 태블릿에서 「🎲 여기서 대신 던지기」가 **화면 밖으로 밀려 있었다**

기록 카드 뒤에 있어서 1024×768 에서 828~875px — 뷰포트(681)를 한참 넘겼다.
**폰이 없거나 배터리가 나간 사람 대신 던져주는 버튼**이라, 안 보이면 그 사람은 아예 못 던진다.
「🏁 새 말 내보내기」도 623~681 로 걸쳐 잘렸다(집의 말을 내보내는 **유일한** 수단이다).

→ 조작 버튼을 **판 바로 아래(`.ybtns`)** 로 올리고, 가로 태블릿에서 판 40vh·`.sec` 여백을 조였다.
   세로 태블릿(820×1180)도 40px 넘쳐서 큰 화면 판 상한을 56vh → **51vh** 로 낮췄다.
   실측: 가로 판 572 · 새말 622 · 대신던지기 671 (뷰포트 681) — **셋 다 스크롤 없이 보인다.**

## 🧰 `shot.mjs --dom` 이 `-w/-h` 를 무시하고 있었다

레이아웃을 재려 했더니 어떤 크기를 줘도 **780×493** 이 나왔다 — `--dom` 경로에만
`--window-size` 가 없었다. 재는 족족 같은 값이라 **측정이 통째로 무의미**했다.
→ **`-w`/`-h` 를 명시했을 때만** 창 크기를 준다(안 그러면 기존 E2E 렌더 환경이 바뀐다).

**검증** — smoke-e2e 233항목 + yut-verify 46항목.
🧪 음성 테스트 3건: `last` 수정 되돌림 → exit 1 / 모서리 지름길 규칙 깸 → smoke 2항목 실패 /
판을 옛 크기로 되돌림 → 레이아웃 검사가 「대신던지기 716 > 681」로 exit 1.
⚠️ 그 과정에서 **뽑기에 따라 흔들리는 검사 하나를 잡았다** — 참가자 폰 검사가 그 봇의
   팀이 뭐냐에 따라 반반으로 통과했다. 차례를 고정하고 **5회 연속 동일** 확인.

### v0.26.0 (2026-08-17) — 🧠 상식 퀴즈 문제 3배 (502 → 1,499) ★★

사장님: "여기서 한 **세 배 정도 추가**해서 **문제가 중복되는 일이 없도록** 해 줬으면 좋겠다."

11범위 모두 **90개씩** 보태 502 → **1,499문제**. 범위마다 132~140개로 고르게 찼다.
기본 범위(6종)만으로 **821문제 = 15문제짜리 54판**, 전 범위면 **99판**을 안 겹치고 돈다.

## ⚠️ 문제를 대량으로 늘리면 **완전 일치 검사만으로는 중복을 못 잡는다**

이게 이번 작업의 핵심 교훈이다. 1,500개까지 늘리면 사람이 쓰는 이상
「일본의 수도는?」과 「일본의 수도 **이름**은?」 같은 쌍이 반드시 생긴다.
문자열이 다르니 기존 검사(`new Set(...).size === 길이`)는 **통과시킨다.**

→ **유사 중복 검사를 E2E 에 새로 박았다.** 공백·기호·흔한 어미(`은/는/이/가/이름/것/무엇/
   뭐라 하나…`)를 떼고 비교해서 같으면 중복으로 본다.
   🧪 음성 테스트: 「일본의 수도 이름은?」을 넣으니 `★★어미만 다른 같은 질문이 없다: ✕FAIL` → exit 1.

## ⚠️ 그리고 **정답이 같은 것과 「같은 질문」은 다르다**

합치기 전에 「정답이 같은 쌍 중 문제 글자가 50% 이상 겹치는 것」을 전부 뽑아 눈으로 봤다(56쌍).
대부분은 **정상**이다 — 「에펠탑이 있는 도시」/「노트르담 대성당이 있는 도시」는 둘 다 파리지만
서로 다른 사실을 묻는다. 진짜 문제는 **같은 사실을 두 번 묻는** 12쌍이었고 전부 갈아끼웠다:

| 걸린 것 | 왜 |
|---|---|
| 「천지창조를 그린 사람」 / 「시스티나 성당 천장화를 그린 사람」 | **천지창조가 곧 그 천장화다** |
| 「가장 작은 소수」 / 「가장 작은 짝수 소수」 | 답이 똑같이 2 |
| 「종이/화약/나침반을 처음 만든 나라」 3개 | 전부 중국 — 한 판에 둘 나오면 두 번째는 공짜 |
| 「올림픽/월드컵/아시안게임은 몇 년마다」 + 「윤년은 몇 년마다」 | 4년짜리가 **넷** |
| 「수제비의 주재료」 / 「칼국수의 주재료」 | 둘 다 밀가루, 형태까지 같다 |
| 「동로마 제국의 수도」 / 「오스만이 무너뜨린 동로마의 수도」 | 같은 질문 |

⚠️ **정답이 겹치는 것 자체를 막으려 들지 말 것** — 그러면 「파리」가 답인 문제를 하나밖에
   못 낸다. 봐야 하는 것은 **묻는 사실이 같은가**이고, 그건 기계가 못 하므로 **후보를 뽑아
   사람이 본다.** 뽑는 기준(같은 정답 + 글자 50% 겹침)은 위 스크립트 그대로 쓰면 된다.

**검증** — smoke-e2e **233항목**(3항목 신설): 1,400문제 이상 · 범위마다 100문제 이상 ·
**어미만 다른 같은 질문 0** · **20문제씩 20판(400문제)을 돌려도 재출제 0**.
📸 설정 화면 실측 — 범위별 개수(132~140)와 기본 합계 821이 그대로 뜬다.

### v0.25.0 (2026-08-17) — 🧠 상식 퀴즈 추가 (8종 → 9종) ★★★

사장님: "퀴즈 문제 하나 넣고 싶은데 **상식 퀴즈** 문제 맞추는 거 해줬으면 좋겠어
**여러 가지 카테고리**로 말이야."

## ★ 흐름을 초성 퀴즈에서 **한 군데 뒤집었다** — 그게 이 게임의 핵심이다

틀은 초성 퀴즈(`cho`)를 그대로 썼다(setup → run → reveal → done, `log` 하나로 점수·취소·
중복 방지). **딱 한 가지가 달라야 했다: 판정을 누가 하느냐.**

| | 초성 퀴즈 | 상식 퀴즈 |
|---|---|---|
| 답의 성격 | 하나로 딱 떨어진다 | **자유 문장** — 표현이 갈린다 |
| 그래서 | 맞힌 사람을 **바로** 탭 | **정답을 먼저 열고** 다 같이 보고 탭 |

```
run(문제만 뜬다) → 「✅ 정답 공개」 → reveal(정답 + 누가 맞혔나) → 다음
```

**왜 이렇게 했나 — 대안 두 개가 다 나빴다**
- ① 노래 맞히기처럼 정답을 진행자만 보기(`.priv` + `mc:'need'`) → **진행자가 게임에서 빠진다.**
  노래는 「곡을 틀려면 제목을 볼 수밖에 없어서」 어쩔 수 없었지만 퀴즈는 그럴 이유가 없다.
- ② 정답을 아무에게도 안 보여주기 → **판정 자체가 불가능.**
→ 순서를 뒤집으니 둘 다 해결된다. 진행자도 정답을 미리 모르니 **같이 맞힐 수 있고**(`mc:'help'`),
  판정은 정답이 뜬 뒤 전원이 함께 하니 옥신각신할 일이 없다.

⚠️ **`.priv` 를 붙이면 안 된다**(초성 퀴즈와 같다 — 전원이 보는 화면이라 커져야 한다).
⚠️ **run 단계에 정답 문자열이 있으면 게임이 조용히 성립하지 않는다.**
⚠️ **정답 공개 전에는 아무도 고를 수 없어야 한다**(`quiz-hit` 의 `phase !== 'reveal'` 가드).
   이게 뚫리면 정답 없이 판정하게 된다. **세 가지 다 E2E 에 못으로 박았다.**
⚠️ 「방금 취소」는 `reveal` 로 되돌린다(초성 퀴즈는 `run`). 실수는 「다른 사람을 눌렀다」가
   대부분이라 **정답이 보이는 상태로 돌아가야** 다시 고를 수 있다.

## 데이터 — **11범위 502문제** (`QUIZ_DECKS`, `[문제, 정답]` 쌍)

한국 상식 46 · 세계지리 46 · 과학자연 46 · 음식생활 46 · 영화대중문화 45 · 잡학숫자 46 ·
세계사 45 · 예술문학 47 · 스포츠 45 · 인체건강 45 · IT브랜드 45.
기본 범위는 `kor·world·sci·life·pop·trivia`(275문제).
⚠️ **세계사·예술문학·스포츠·IT 는 일부러 기본에서 뺐다** — 사자성어를 뺀 것과 같은 이유로,
   아는 사람만 아는 쪽으로 쏠려서 첫 판부터 켜두면 한 사람이 독식한다.
⚠️ **문제를 넣을 때의 기준을 `QUIZ_DECKS` 주석에 적어뒀다** — ①답이 하나로 떨어질 것
   ②5초 안에 아는지 갈릴 것 ③한 명은 알 법할 것 ④문제 문장 중복 0.
⚠️ 위인·작가·브랜드 이름은 **게임 내용(정답)** 이라 「이름 원칙」 대상이 아니다
   (v0.19.0 에서 곡 제목·가수명에 대해 이미 같은 판단을 했다).
⚠️ 문제를 `w` 에 담는다 — 중복 방지 헬퍼(`freshPick`/`markUsed`)가 `w` 를 보기 때문이다.
   노래 맞히기가 곡 제목을 `w` 에 담는 것과 똑같은 이유.

## 🐛 스샷으로 잡은 것 2개 (E2E 는 통과하는데 화면이 틀렸다)

**① 종료 화면에 정답만 있고 문제가 없었다.** 「60세」·「차하로」만 나열되니 **뭘 물었는지 알 수 없다.**
초성 퀴즈는 제시어가 곧 답이라 문제없던 자리인데, 이 게임은 문제와 답이 따로다.
→ 정답 아래에 문제를 한 줄로 같이 적는다(`.sumrow .qa`). E2E 에도 검사를 추가했다.

**② 가로 태블릿(1024×768)에서 reveal 화면의 조작 버튼이 화면 밖으로 밀렸다.**
정답 블록(`.wans`)이 얹히면서 초성 퀴즈보다 ~110px 커진 탓이다. 문제·정답은 보이지만
**진행자가 매 문제마다 스크롤**하게 된다. → `revealing` 일 때 `.wcard` 에 `rv` 를 붙이고
`max-height:860px` 구간에서만 조인다(문제·정답은 **이미 다 같이 읽은 뒤**라 줄여도 된다).
⚠️ **가로 태블릿은 세로가 모자라다** — 새 진행 화면을 만들면 1024×768 로 반드시 찍어볼 것.

**검증** — smoke-e2e **230항목**(상식 퀴즈 36항목 신설): 502문제·11범위 · **문제문 중복 0** ·
전 문제 정답 보유 · 기본 범위에 어려운 쪽 미포함 · **run 에 정답 없음** · **`.priv` 미사용** ·
**공개 전 「누가 맞혔나」 미노출** · **공개 전 탭 차단** · 공개 후 정답 표시 · +1 후 자동 진행 ·
**취소 시 reveal 복귀** · 못 맞힘 `by:null` · 자동 종료 · 점수순 1등 · 개인전 전환 ·
**종료 화면에 문제도 표시** · 판 종료 시 `used` 기록 · **다음 판에 이미 나온 문제 없음**.
🧪 음성 테스트 2건: run 에 정답을 노출시키니 exit 1 / `reveal` 가드를 빼니 9항목 ✕FAIL 로 exit 1.
📸 실측: 폰 400×900 · 태블릿 1024×768(**스크롤 없이 한 화면**) · **가장 긴 문제(37자)** 양쪽.
⚠️ **정답 누출 검사는 카드를 고정해서 본다** — 정답이 「M」 처럼 짧으면 화면 아무 데나 걸려
   뽑기에 따라 실패하는 검사가 된다(v0.24.0 의 태연 「I」 사건과 같은 병).

### v0.24.1 (2026-08-14) — 🎲 리뷰에서 잡은 버그 2건 ★★

사장님: "방금 만든 훈민정음 윷놀이 잘 만들었는지 검토 좀 해줘봐" → 코드를 다시 읽으며
**E2E 가 놓친 것**을 찾았다. 치명 1건 + 실질 1건.

**① [치명] 새 말을 내보내면 집의 말이 전부 한꺼번에 나왔다.**
`yutMoveFrom(-1)` 이 `p === pos` 로 필터하는데 집(-1)엔 말 4개가 다 있다 —
**첫 도에 4개가 통째로 나와 업힌 채** 다녔다. 잡히면 전멸이고, 말을 나눠 쓰는
윷놀이의 핵심이 사라진다. → `pos === -1` 이면 하나만(`slice(0,1)`). **업기는 판 위에서만.**
⚠️ **E2E 가 이 버그를 정상이라 못 박고 있었다** — `filter(x=>x===1).length === 4` 를
「업고 함께 간다」라며 통과시켰다. 검사를 쓸 때 **기대값이 규칙에서 나온 게 맞는지**를
먼저 물어야 한다. 구현이 뱉는 값을 그대로 기대값으로 적으면 버그가 명세가 된다.

**② [실질] 「잠시!」 판정 중에도 말을 움직일 수 있었다.**
판정 화면이 판(`board`)을 같이 그리는데 초록 셀의 `yut-move` 가 살아 있었고
`yutMoveFrom` 에 `halt` 가드도 없었다. → 핸들러 가드 + 판정 중 `hot=[]` 이중 방어.
음성 테스트에서 이 버그는 **뒷 검사 6개를 연쇄로 오염**시켰다 — 실전에서도 판정 중
이동 한 번이면 판이 그렇게 꼬였을 것이다.

**검토했지만 문제 없음(근거 포함)**: 판 그래프(경로 길이 수치 검증) · 쓰기 주체 분리 ·
잡기/승리/턴 · 연출 재진입 가드(`pushYut` 에코 스냅샷으로 밀린 throw 재처리됨) ·
같은 팀 둘이 동시에 던지는 경합(마지막 쓰기만 반영 — 허용).
📝 참고: **사회자(관전) 기기도 「잠시!」를 누를 수 있다** — 상대팀만은 아니지만
심판 역할이라 의도된 동작으로 남겨뒀다.

**검증** — smoke-e2e **194항목**. 음성 테스트 2건 각각 exit 1.

### v0.24.0 (2026-08-14) — 🎲 윷 던지는 연출 ★★

사장님: "그 사람 순서에서 윷을 던질 때 **진짜 윷놀이처럼** 해줘. 던지는 것도 표현되게."

`yutThrowAnim(r, who)` — 전체 화면에서 **윷짝 4개가 굴러 올라갔다가 하나씩 착지**하고,
마지막에 「걸 · 3칸」처럼 결과가 터진다. 윷·모면 「한 번 더 던져요!」 + 컨페티.
기존 `rollReveal`(역할 뽑기 슬롯머신)과 같은 오버레이 패턴을 그대로 따랐다.

⚠️ **결과는 연출 전에 이미 확정돼 있다.** 연출은 보여주기만 한다(`rollReveal` 과 같은 원칙).

**세 경로 모두에 연결했다** — 어디서 던지든 다 같이 본다:
| 던지는 곳 | 연출이 도는 곳 |
|---|---|
| 참가자 폰 | 내 폰에서 즉시 → 결과를 서버에 올림 |
| 태블릿 「대신 던지기」 | 태블릿 |
| 참가자가 던진 걸 태블릿이 수신 | **태블릿**(`yutConsume`) |

⚠️ **연출이 끝난 뒤에 판에 반영한다.** 먼저 반영하면 연출 중에 말이 움직여 결과가 미리 보인다.
⚠️ **`_yutAnim` 재진입 가드가 필요하다.** 스냅샷이 연달아 오면 연출이 겹친다 —
   연출 중이면 `yutConsume` 이 그냥 돌아가고 다음 스냅샷에서 처리한다.

**🧰 검증 도구도 같이 손봤다**
- `yutConsume()` 이 **프로미스를 돌려준다** → E2E 가 `await` 로 기다린다.
  (안 그러면 「참가자 던지기 반영」 검사가 연출 때문에 항상 실패한다. 실제로 3개가 깨졌다.)
- `shot.mjs --dom` 의 가상시간 예산 9초 → **20초**. 연출이 들어가자 시나리오가 중간에 잘렸다.
⚠️ **연출을 추가하면 E2E 시간이 늘어난다** — 예산을 같이 올릴 것.

🐛 **랜덤하게 실패하던 검사도 잡았다.** 노래 맞히기의 「`.priv` 밖에 곡 제목이 없다」가
   **뽑힌 곡에 따라** 가끔 깨졌다 — y10b 에 태연의 「**I**」가 있어서, 한 글자 제목이 걸리면
   화면 아무 데나 매칭된다. → 검사에서 곡을 **고정**했다(「벚꽃엔딩」).
   ⚠️ **데이터를 랜덤으로 뽑는 검사는 값을 고정할 것.** 랜덤 실패는 사람을 길들여
   진짜 실패까지 무시하게 만든다(v0.12.0 `crime-e2e` 와 같은 병이 또 나왔다).

**검증** — smoke-e2e **192항목**, **5회 연속 동일**(연출 대기 반영).
📸 연출 중간(구르는 중)과 착지 후를 각각 캡처해 확인 — 배 3·등 1 → 「걸 · 3칸」.

### v0.23.0 (2026-08-14) — 🎲 윷놀이 **전면 재작성** (진짜 규칙으로) ★★★

사장님: "지금 기본적인 윷놀이랑 **완전 안 맞아.** 윷놀이 게임을 제대로 만들어줘
(**말판도 이상하고** 문제 많아)"

맞다. v0.22 는 「윷놀이 비슷한 것」이었지 윷놀이가 아니었다. 세 군데가 근본적으로 틀렸다.

## ① 말판이 진짜 윷판이 아니었다

| | v0.22 | v0.23 |
|---|---|---|
| 칸 | 27 (대각선이 Y자) | **29 — 외곽 20 + 대각선 8 + 방 1** |
| 대각선 | 하나로 합쳐짐 | **X자로 둘 다** (둘 다 실제로 지나간다) |
| 5의 지름길 | 바로 날 자리로 | **반대 모서리(15)로 나간다** (표준) |

## ② 「방을 지나갈 때」와 「방에 멈췄을 때」가 달라야 한다 ★핵심

이게 윷놀이 규칙의 핵심인데 v0.22 엔 아예 없었다.
- 방을 **지나가는 중**이면 **타고 온 대각선을 그대로** 빠져나간다(A로 왔으면 15 쪽).
- 방에 **정확히 멈췄다가** 출발하면 **날 쪽 지름길**(28→29→날)로 빠진다.

→ 이동 계산에 **「지금 어느 대각선을 타고 있나(`via`)」** 가 필요하다. `yutMove` 가 이를 추적한다.
경로 길이도 맞췄다: 바깥 한 바퀴 20 · 5 지름길 11 · 10 지름길 6 · 방에서 3
(두 지름길 모두 **4칸 절약** — 실제 윷판과 같다).

## ③ 던진 값을 **모아서 배분**해야 한다 ★핵심

실제 윷놀이는 윷·모가 나오면 계속 던지고, **「도로 이 말, 윷으로 저 말」** 처럼 나눠 쓴다.
v0.22 는 던질 때마다 즉시 이동해서 **그 전략이 통째로 없었다.**
→ `pending[]` 에 쌓고, 화면에서 결과 칩을 골라 말에 배분한다. 잡으면 `canThrow` 가 다시 켜진다.

## 그 밖에
- **윷짝 4개를 그린다** — 배(밝은 면)가 몇 개 위로 왔는지 보인다. 훨씬 윷놀이답다.
- `YUT_HOME` 을 20 → **99** 로 바꿨다. 20 은 외곽 칸 번호와 겹쳐서 위험했다.
- 🗑 `yutDist` 제거 — `yutMove(p,1)` 을 반복해서 **모서리마다 지름길을 타는** 버그가 있었다.
  (이제 되돌릴 말은 그 팀이 고르므로 거리 계산 자체가 필요 없다.)
- ⚠️ **백도(뒤도)는 넣지 않았다.** 전통 규칙엔 없고, 넣으면 윷짝 표시가 하나 더 는다.

⚠️ **검증할 때 `yutMove(p,1)` 을 반복하지 말 것.** 매번 첫 걸음이 되어 모서리마다 지름길을
   탄다(실제로 그렇게 재다가 「한 바퀴 11칸」이라는 잘못된 결과를 봤다). **한 번에 n 칸**으로 부를 것.

**검증** — smoke-e2e **192항목**(윷놀이 35항목): 29칸 · **바깥 20칸** · 모서리 통과/정지 ·
**방 통과 시 대각선 유지** · **방 정지 시 날 쪽** · 각 지름길 길이 · **던진 값 누적** ·
윷 나오면 추가 던지기 · **값 골라 배분** · 업기 · 잡기 후 추가 던지기 · 나기 승리 ·
「잠시!」 전 과정 · 쓰기 주체 분리 · renderSig · 폰/집 버튼 존재.
📸 태블릿 1024×768 — 판·윷짝·결과 칩·버튼이 **한 화면**에 들어간다.

### v0.22.1 (2026-08-14) — 🎲 되돌릴 말을 **그 팀이 고른다** + 대기 말 내보내기 ★★

사장님: "말 하나를 다시 집을 보내야 하는 건 **영어를 한 그쪽 팀에서 고를 수 있어야** 해.
방금 출발한 친구를 되돌릴 수도 있고 이미 말판에 여러 마리가 나가 있을 수 있으니까."

**맞는 지적이다 — 앱이 골라주면 전략이 통째로 사라진다.**
v0.22.0은 「가장 앞선 말」을 자동으로 뺐다. 그러면 벌칙이 그냥 손해일 뿐인데,
직접 고르게 하면 **방금 출발한 말을 버리고 앞선 말을 지키는** 판단이 생긴다.

- 인정 → `yut.pick = { team }` 단계로 간다. 말판에서 **그 팀 말만** 초록으로 집힌다.
- 그 팀이 태블릿에서 직접 고른다(다 같이 둘러앉아 있으니 자연스럽다).
- ⚠️ 고르는 중에는 이동을 막는다(`yutMoveFrom` 가드).
- 영어 횟수는 **인정 시점**에 기록한다(어떤 말을 고르든 영어를 쓴 건 사실이니까).
- 업혀 있으면 그중 **하나만** 돌아간다.

## 🐛 같이 발견 — 대기 말을 내보낼 방법이 아예 없었다

**판에는 「대기」 칸이 없다.** 그래서 말이 전부 집에 있으면 **한 발도 못 움직였다.**
E2E 가 `act('yut-move', {pos:'-1'})` 를 **코드로 직접 불러서** 통과하고 있었다 —
화면에 그 버튼이 있는지는 아무도 안 본 것이다.
→ 「🏁 새 말 내보내기 (대기 N개)」 버튼을 넣고, **버튼이 화면에 있는지**를 검사에 추가했다.
⚠️ **핸들러를 직접 호출하는 검사는 UI 존재를 보장하지 않는다.** 화면 문자열도 같이 볼 것.

**검증** — smoke-e2e **183항목**: 인정 → 고르기 단계 · 아직 아무 말도 안 빠짐 ·
고르는 중 이동 차단 · **앞선 말(7) 대신 뒤의 말(3)을 골라 되돌리기** · pick 정리 ·
**대기 말 버튼이 화면에 존재**.

### v0.22.0 (2026-08-14) — 🎲 훈민정음 윷놀이 (7종 → 8종) ★★★

사장님: "훈민정음 윷놀이. **영어를 했을 때 말 1개를 빼야** 하는 게임. 다 같이 **태블릿으로
말판**을 보고, **각자 핸드폰으로 윷**을 던질 수 있게. 영어를 하면 상대팀이 **「잠시!」**를
외칠 수 있는 버튼을 만들어서 **말을 되돌리게** 하는 구조도."

## ★ v0.13.0 이후 처음으로 「각자 폰」을 되살린 게임이다

문서에 적어둔 대로 **쓰기 주체 분리를 먼저 설계**했다(위 「윷놀이 예외」 참조).
호스트=태블릿이 진실원, 참가자는 `yut/throw` 와 `yut/halt` **두 경로만** 쓴다.

## 판과 규칙 — 검증하면서 버그 두 개를 잡았다

- 외곽 20칸 + 지름길 2개. 좌표는 **%** 라 폰·태블릿에서 같은 코드가 그대로 맞는다.
- 🐛 **지름길이 「지나가기만 해도」 꺾였다.** 그래서 바깥 한 바퀴가 20칸이 아니라 **11칸**이었다.
  → `yutStep(pos, first)` 의 `first` 로 **모서리에 정확히 멈춘 다음 이동**에서만 타게 했다.
  경로 길이를 실제로 재보고 잡았다(0→도착 20칸 · 지름길 6칸).
- 🐛 **「잠시!」가 가장 앞선 말이 아니라 가장 뒤처진 말을 뺐다.** `yutDist` 는 「도착까지
  남은 칸」이라 **작을수록 앞선 말**인데 내림차순으로 정렬했다. E2E가 잡았다.
- 같은 칸의 우리 말은 **업고 함께** 움직인다(칸 단위로 옮기면 업기가 자연히 구현된다).
- 잡으면 상대 말은 대기로, **잡은 쪽이 한 번 더**. 윷·모도 한 번 더.

## ✋ 잠시! — 판정은 앱이 못 한다

영어를 썼는지는 **사람만 안다.** 그래서 상대팀이 폰에서 「✋ 잠시!」를 누르면 태블릿에
**판단 화면이 최우선으로** 뜨고, 다 같이 판단한 뒤 진행자가 인정/취소를 누른다.
인정되면 **가장 앞선 말 1개가 처음으로** 돌아가고 영어 횟수가 기록된다.
- ⚠️ **우리 팀은 지적할 수 없다**(자해 방지). E2E에 있다.

## 🐛 부팅이 통째로 죽었던 것

`vHome` 에 윷놀이 배너를 넣으며 `S.room.yut` 을 썼는데, **`vHome` 은 방에 들어가기 전에도
그려진다** — `S.room` 이 `null` 이라 모듈 전체가 죽었다. 앱이 아예 안 뜨는데
**시나리오도 안 돌아서 조용히 넘어갈 뻔했다** — v0.21.0에서 넣은 `shot.mjs` 의
「합격 표시가 없으면 실패」가 바로 잡아냈다. 넣어두길 잘했다.

**검증** — smoke-e2e **177항목**(윷놀이 26항목 신설): 판 깔기 · **바깥 20칸** ·
지나가면 지름길 안 탐 / 멈추면 탐 · 중앙 출구 · 업기 · **잡기와 추가 던지기** · 완주 승리 ·
**「잠시!」 인정 시 가장 앞선 말 회수** · 우리 팀 지적 차단 · **참가자 던지기 소비 후 throw 삭제** ·
남의 차례 던지기 폐기 · **renderSig 에 yut 반영** · 폰 화면 버튼.
📸 태블릿 1024×768 실측 — **스크롤 없이 한 화면**(vh 상한). 선은 SVG 로 그렸다 —
div 회전은 칸과 좌표가 안 맞았다. ⚠️ **그리는 선 = 실제로 갈 수 있는 길**로 맞췄다.

### v0.21.0 (2026-08-14) — 🗣 한글자 자동 지급 + 사회자 안내 정리 ★★

사장님: "**한글자로 말해요는 랜덤으로 한 글자를 줘야** 할 것 같아. 그리고 사회자가 필수로
잡혀 있는데, **태블릿이 사회자 역할을 하면 되는 거라 괜찮을 것 같은데 확인 좀 해줘**"

## ① 사회자 — 사장님 말이 맞았고, 앱도 이미 그렇게 동작하고 있었다

관전(사회자) 기기가 있으면 `body` 의 사회자 뽑기는 v0.15.0부터 이미 숨겨진다.
**문제는 화면이 모순돼 있었던 것**: 위 박스는 「겨루지 않는 사회자가 있어야 깔끔해요」라고
**요구**하는데 바로 아래에서 「태블릿 기기가 사회자예요」라고 **해결**됐다고 했다.
이미 갖췄는데 아직 모자란 것처럼 보인다. (게임 **목록** 화면은 이미 이걸 처리하고 있었고
**상세** 화면만 안 고쳐져 있었다.)

→ 두 박스를 **하나**로 합치고 상태를 반영한다: 관전 기기가 있으면 ✅ 초록 「충족」,
   없으면 📺 파랑 「요구 + 대기실에서 지정하는 법」.
- ⚠️ **`privScreen:true`** 를 `body`·`song` 에 넣었다 — **사회자 화면에 답이 있는 게임**이다.
  이 경우 충족 박스에 「그 화면을 가운데 두지 말고 사회자가 들고 보세요」를 같이 띄운다.
  `.priv` 는 큰 화면에서 **커지지 않게 할 뿐 화면에서 지워주지 않는다.**

## ② 한글자로 말해요 — 앱이 뽑아준다

예전엔 팀이 입력창에 직접 쳤다. 아무도 안 정해서 시작이 지연되고, 팀마다 같은 글자를 골라
뒷 팀이 앞 팀 방식을 그대로 베끼기도 했다.
→ `ONE_LETTERS` 30개에서 **팀마다 다른 글자**를 자동으로 준다. 「🎲 다른 글자로 바꾸기」로 재추첨.
- ⚠️ 아무 글자나 넣으면 안 된다 — **억양·길이만으로 표현되는 소리 흉내 글자**라야 굴러간다
  (짜·뿅·쿵·삐…). "의"·"례" 같은 건 입에 안 붙어서 그냥 막힌다.
- ⚠️ **글자는 렌더에서 뽑으면 안 된다.** `render()` 는 스냅샷마다 불리므로 매번 바뀐다.
  턴이 바뀌는 시점(`pl-start`/`pl-next`)에 `ensureLetter()` 로 준비해 둔다.

## 🧰 검증 도구 두 곳을 고쳤다 — **조용히 통과하던 구멍**

이번에 시나리오에 변수 충돌(`t1` 중복 선언)을 냈더니 **사본 전체가 파싱되지 않아
시나리오가 통째로 안 돌았는데 `exit 0`** 이 나왔다. 「다 통과한 줄 알았는데 애초에 안 돌았다」 —
v0.12.0에 CDN 문제로 똑같은 일을 겪고도 판정 쪽이 남아 있었다.

- **`tools/demo.mjs`** — 사본을 만든 뒤 **스스로 `node --check`** 한다. 깨졌으면 즉시 exit 1.
- **`tools/shot.mjs --dom`** — 「아무 표시 없음」을 통과로 보지 않는다.
  시나리오는 끝에 반드시 `✅` 또는 `‼` 를 찍으므로, **둘 다 없으면 안 돈 것**으로 판정한다.
⚠️ **판정은 「실패 표시가 없다」가 아니라 「합격 표시가 있다」로 해야 한다.**

🐛 같이 고침: 템플릿 안 **HTML 주석에 UI 문구를 그대로 적어** 문자열 검사가 오판했다.
   주석은 DOM 으로 나간다 — UI 문구를 복사해 넣지 말 것.

**검증** — smoke-e2e **151항목**(한글자 7 + 사회자 3 신설): 자동 지급 · 목록 안의 글자 ·
입력창 없음 · 재추첨 · **팀끼리 안 겹침** · 관전 기기 있으면 요구 문구 사라짐 · 없으면 다시 뜸 ·
답이 있는 화면 안내.
🧪 도구 음성 테스트 2건: 시나리오에 문법 오류 → `demo.mjs` 가 exit 1.
빈 시나리오 → `shot.mjs` 가 「실행되지 않았습니다」로 exit 1.

### v0.20.0 (2026-08-14) — 🎵 노래 대폭 확장 + **사회자 전담 확정** ★★

사장님: "노래 훨씬 많이 넣어야 할 것 같고 **연도별 카테고리도 다양하게**, 대신 **유명한
노래**들로. 그리고 이건 노래를 틀 때 **어쩔 수 없이 정답을 볼 수밖에 없기 때문에 무조건
사회자 필요 게임**으로 해야 할 것 같아. **노래 제목을 못 보게 할 수 있다면 확인해줘봐**"

## ★ "제목을 숨길 수 있나" — 실제로 띄워서 확인했다 (결론: 못 한다)

헤드리스로 임베드를 두 벌 띄워 스크린샷으로 비교했다.

| 시도 | 결과 |
|---|---|
| 기본 `youtube.com/embed/ID` | 상단에 **제목·채널명 그대로** |
| `controls=0&modestbranding=1&showinfo=0&rel=0&iv_load_policy=3` | **똑같이 다 보인다** |
| 썸네일 | **영상 안에 곡 정보가 박혀 있다** (옵션과 무관) |

`showinfo` 는 이미 폐기된 파라미터고, 제목을 끄는 지원 옵션은 **없다.**
0px 플레이어로 소리만 빼는 것은 기술적으로 가능하지만 채택하지 않았다 —
① 유튜브 정책상 플레이어를 숨기는 것은 위반 ② **광고가 먼저 나오면 게임이 깨진다**
③ 공식 뮤비 상당수가 임베드 차단 ④ 영상 ID가 삭제·차단되면 **숙소에서 조용히 먹통**이 된다.
⚠️ **다시 시도하지 말 것.** 위 표가 근거다.

→ 그래서 **사회자 전담(`mc:'need'`, `minP:3`)으로 확정**했다. 트는 사람은 답을 다 본다.
⚠️ **이 게임만은 태블릿을 가운데 두면 안 된다** — 정답이 그 화면에 있다.
   `.priv` 가 크기를 줄여줄 뿐 화면에서 지워주지는 않는다. 설정 화면에 경고 박스를 넣었다.

## 데이터 — 142곡 → **424곡**, 7범위 → **12범위**

시대를 잘게 쪼갰다: **1980년대 30 · 1990년대 44 · 2000년대 초반 34 · 2000년대 후반 34 ·
2010년대 초반 36 · 2010년대 후반 45 · 2020년대 48** + 팝 명곡 30 · 최신 팝 32 · 트로트 31 ·
드라마·영화 OST 30 · 만화·동요 30.
기본 범위는 `y10b·y20·ost·ani`(세대가 섞여도 다 같이 맞힐 수 있는 조합).
⚠️ **유명한 곡만 넣는다** — 마이너한 곡은 아무도 못 맞혀서 판이 죽는다.

**검증** — smoke-e2e **141항목**: 시대 7분할 · 300곡 이상 · **같은 곡이 두 범위에 없음** ·
`mc:'need'` · 정답이 `.priv` 안에만.
🧪 음성 테스트: `.priv` 를 떼니 2항목 ✕FAIL → exit 1.
⚠️ **검사 두 개가 무의미했던 것을 고쳤다.**
   ① 「같은 곡이 두 번 없다」가 **`songPool()` 을 자기 자신과 비교**하고 있었다 —
     `songPool()` 은 이미 중복을 걸러 돌려주므로 **항상 통과**한다.
     원본 `SONG_DECKS` 배열로 바꾸니 곧바로 실제 중복 5건(테스형·밤편지·타이타닉 등)을 잡아냈다.
   ② 「정답이 `.priv` 안에만」의 정규식이 `[\s\S]{0,400}?[^<>]*` 라 **아무거나 통과**했다.
     「블록 안에 있고, 블록을 지우면 밖엔 없다」로 정확히 바꿨다.

### v0.19.0 (2026-08-14) — 🎵 노래 맞히기 (6종 → 7종) ★★★

사장님: "노래 듣고 맞추는 게임을 만들고 싶은데, **유튜브 뮤직이랑 연동**해서 잘 되도록
해주는 게 포인트" + "그냥 노래를 맞추면 너무 빡세니까 **연도별처럼 기준을 만들어서**"

## ★ 왜 앱이 직접 재생하지 않나 — 실측해서 결정했다

| | 결과 |
|---|---|
| `music.youtube.com` 임베드 | **`X-Frame-Options: SAMEORIGIN` → 원천 불가** |
| `youtube.com/embed/…` | 가능(200). **하지만 플레이어에 영상 제목이 뜬다 = 정답 노출** |
| 플레이어 가리기 | 유튜브 정책 위반 |

노래 맞히기는 **제목이 곧 정답**이라 유튜브 임베드와 근본적으로 안 맞는다.
→ **재생은 유튜브 뮤직 앱에 맡기고 앱은 문제·정답·점수만 맡는다.**
   진행자가 「▶ 유튜브 뮤직에서 열기」를 누르면 그 곡 검색 화면이 새 탭(앱)으로 열린다.
   `ytMusicUrl(제목, 가수)` → `music.youtube.com/search?q=…` (딥링크, 앱 설치 시 앱으로).
⚠️ **이걸 임베드로 "개선"하려 들지 말 것.** 위 표가 이유다. 정답이 새거나 정책을 어긴다.
⚠️ 부수 효과 — **이 앱에서 인터넷이 꼭 필요한 유일한 게임**이 됐다(나머지는 다 오프라인).

## 데이터 — 연도별이 곧 난이도 손잡이

`SONG_DECKS` **7범위 142곡**: 1990년대 20 · 2000년대 20 · 2010년대 22 · 2020년대 22 ·
팝송 20 · 트로트 18 · 만화·동요 20.
사장님 말대로 **범위가 난이도**다 — 세대가 섞인 자리에서 90년대만 켜면 어린 사람이 한 곡도
못 맞힌다. 설정 화면에 그 안내를 넣었고, 기본값은 `y10·y20·ani`(가장 넓게 통하는 조합).

## 정답 보호 — `.priv` 를 **반드시** 붙인다

초성 퀴즈와 정반대다. 초성은 전원이 봐야 해서 `.priv` 를 **빼는** 게 규칙이었는데,
노래는 **진행자만** 봐야 곡을 틀 수 있다 → `.priv` 를 **붙이는** 게 규칙이다.
태블릿 공용 화면에서 이게 커지면 그 자리에서 게임이 끝난다.
⚠️ E2E에 「`.priv` 밖에는 곡 제목이 없다」를 못으로 박았다(블록을 지우고 남는지로 검사).

- 진행 구조는 초성 퀴즈와 **일부러 똑같이** 맞췄다(`log` 하나로 점수·되돌리기·중복 방지).
  배우는 규칙이 하나면 두 게임을 다 할 줄 알게 된다.
- v0.18.0의 중복 방지(`room.used`)를 그대로 재사용 — 같은 곡이 다음 판에 안 나온다.

**검증** — smoke-e2e **138항목**(노래 19항목 신설): 범위가 연도별로 나뉨 · 곡마다 제목·가수 ·
중복 0 · **정답이 `.priv` 안에만** · **딥링크에 제목과 가수가 함께** · 되돌리기 ·
자동 종료 · 중복 방지 기록 · 순위.
🧪 음성 테스트 2건: `.priv` 를 떼니 2항목 ✕FAIL → exit 1. 딥링크에서 가수를 빼니 ✕FAIL → exit 1.
📸 태블릿 1024×768 실측 — **정답 카드만 폰 크기로 남고** 버튼·명단은 커진다.

⚠️ **곡 제목·가수는 「이름 원칙」의 대상이 아니다.** 그 원칙이 막는 것은 예시·더미·주석에
   쓰는 사람 이름이고, 여기 가수명은 **게임 내용(정답)** 이다. 기존 `music` 제시어 덱도 같다.

### v0.18.0 (2026-08-14) — 🔁 제시어 중복 방지 + 📋 설명 구조화 ★★★

사장님: "중복방지 기능 넣어줘. 다만 새롭게 시작할 수 있게 **게임 리셋 버튼**을 따로 만들면
다음에 다시 시작할 수 있는 느낌으로. 그리고 전반적으로 **정보나 방식들이 산개해 있는데
좀 더 직관적으로 설명될 수 있도록** 바꿔줘."

## ① 중복 방지 — `room.used`

덱을 아무리 늘려도(v0.17.0에서 832문제) **판마다 새로 섞으면 재탕이 난다.**
`rotation`(술래 중복 방지)과 **똑같은 자리·똑같은 방식**으로 `room.used/{게임id}` 를 만들었다.

- `usedWords(gid)` / `markUsed(gid, words)` / `resetUsed(gid)` / `freshPick(gid, pool, need)`
  — **두 덱 게임(초성 퀴즈·몸으로 말해요)이 같은 헬퍼를 쓴다.** 같은 문제를 게임마다
  다르게 처리하면 그것도 「산개」다.
- 판이 끝날 때 그 판에 **실제로 화면에 뜬** 제시어만 기록한다(안 넘긴 카드는 안 쓴 것).
- **남은 게 모자라면 자동으로 비우고 처음부터** 돈다. 끝이 없는 게임에서 "낼 게 없어요"로
  멈추면 안 되기 때문. 이때 토스트로 알린다.
- **「🔄 초기화」 버튼**(`used-reset`) — 사장님이 말한 "다음에 다시 시작하는 느낌".
  설정 화면의 `stockBox()` 안에 있다: 「아직 안 나온 문제 348개 / 지금까지 120개 사용」.

### 🐛 같이 잡은 진짜 버그 2개 (몸으로 말해요)
1. **팀마다 덱이 되감겼다.** `beginTurn()` 이 매 턴 `buildDeck()` 을 불러서
   **2팀이 1팀과 똑같은 제시어를 받을 수 있었다.** → 덱은 `pl-start` 에서 **한 판에 한 번만**.
2. **턴이 바뀔 때마다 제시어가 하나씩 증발했다.** `beginTurn()` 의 `nextWord()` 가
   지난 턴 끝에 이미 뽑아둔 카드를 버리고 있었다. → 첫 장은 `pl-start` 에서 뽑아둔다.
⚠️ **`beginTurn()` 에서 덱을 건드리지 말 것.** 두 버그가 다 거기서 났다.

## ② 설명 구조화 — 상세 화면이 정해진 순서로 그린다

예전엔 설명이 `rules` 한 덩어리에만 있었다. 「무슨 게임인지 · 뭐가 필요한지 · 몇 명인지」를
알려면 긴 글을 읽어야 했고, 최소 인원은 아예 **핸들러에 하드코딩**돼 있어 화면과 따로 놀 수 있었다.

→ `GAMES` 에 **`line` · `prep` · `minP` · `mins` · `how[]` · `win`** 을 넣고,
   상세 화면을 **무슨 게임 → 뭐가 필요(인원·시간·준비물 3칸) → 어떻게(번호 단계) → 누가 이김**
   순서로 고정했다. **자세한 규칙은 `<details>` 로 접었다** — 술 마시며 보는 화면이라
   길면 아무도 안 읽는다.
- ⚠️ **`minP` 는 이제 한 곳에서만 읽는다.** `tool-start` 의 하드코딩된 2/3 을 걷어내고
  게임별 시작 분기도 표 하나로 합쳤다(`{deck:…, cho:…}[g.tool]?.()`).

**검증** — smoke-e2e **119항목**(중복 방지 11 + 설명 구조 7 신설):
판 사이 미중복 · **같은 판에서 팀 간 미중복** · 덱을 이어 씀 · 자동 순환 · 초기화 ·
전 게임 설명 6필드 · 상세 화면이 실제로 그림 · minP 일치.
🧪 음성 테스트 4건 전부 exit 1(필터 제거 / 팀 되감기 부활 / `win` 비우기 / minP 우회).
⚠️ **검사를 두 번 고쳤다** — 판 사이·팀 사이 중복 검사가 처음엔 **뽑힌 몇 개만** 비교해서
   87개 풀에서는 버그가 있어도 27% 확률로 통과했다(운으로 통과하는 검사). 덱 전체를
   비교하도록 바꾸고 나서야 음성 테스트가 5회 연속 걸렸다.
   ⚠️ 그 과정에서 **정상본이 실패**해 코드 쪽 버그(제시어 증발)를 찾아냈다 —
   검사를 결정적으로 만들면 이런 게 딸려 나온다.

### v0.17.0 (2026-08-14) — 📚 제시어 덱 대확장 (416 → 1,150 · 9종 → 14종) ★★

사장님: "중복이 잘 안 나오도록 문제를 훨씬 더 많이 만들어 줬으면 좋겠어.
카테고리가 늘어나도 괜찮을 듯?"

**왜 필요했나** — v0.16.1 직후 「몇 판이나 되냐」를 계산해보니 기본 범위 117문제 =
15문제짜리 7판이 한계였고, **판마다 덱을 새로 섞으므로 3판째부터 재탕이 나왔다**
(4판째 15문제 중 평균 5개). 숙소에서 연달아 두세 판 하는 게 이 앱의 전제라 치명적이다.

**늘린 것**

| | 이전 | 지금 |
|---|---|---|
| 카테고리 | 9종 | **14종** |
| 총 단어 | 416 | **1,150** |
| 초성 출제 가능 | 271 | **832** |
| 기본 범위 | 117 (7판) | **468 (31판)** |
| 4판째 중복(15문제) | 5.0개 | **0.8개** (전 범위) |

- 새 카테고리 5종: **나라·도시(83) · 사자성어(77) · 스포츠·취미(51) · 자연·날씨(59) ·
  가게·장소(61)**. 기존 9종도 전부 확장(동물 60→108, 음식 54→114, 영화 44→87 …).
- 초성 퀴즈 기본 범위를 4종 → **7종**으로 넓혔다(영화·노래·캐릭터·동물·가게·음식·나라).
  ⚠️ **사자성어는 일부러 기본에서 뺐다** — 문제로는 좋지만 난이도가 확 뛰어서
  첫 판부터 켜두면 아무도 못 맞히는 판이 나온다.

**★ `use` 태그 — 한 덱을 두 게임이 쓰는데 요구가 정반대다**
몸으로 말해요는 **몸짓으로 표현 가능**해야 하고, 초성 퀴즈는 **답이 하나로 떨어져야** 한다.
「아르헨티나」는 초성 퀴즈엔 완벽하지만 몸으로는 불가능하고, 「사자」는 그 반대다.
→ 각 카테고리에 `use:['body','cho']` 를 달고 `deckKeys(tool)` 로 화면을 거른다.
   `pl-cat` 핸들러에서도 한 번 더 막는다(직접 조작 방어).
⚠️ **새 카테고리에 `use` 를 안 적으면 양쪽 모두에 뜬다**(옛 데이터 호환 때문).
   나라·사자성어가 몸으로 말해요에 새면 그 판은 그냥 망한다 — E2E에 못으로 박아뒀다.

**품질 정리** — 초안에 섞였던 것들을 실측 중에 걷어냈다:
「잠수함벌레」(존재하지 않는 말) · 「팔각형링」·「줄넘기대회」(억지) ·
**「보람」·「나나」(사람 이름처럼 읽힘 — 이름 원칙 위반)** · 「노래방」 중복(행동/노래 양쪽).

**검증** — smoke-e2e **101항목**(덱 정합 8항목 신설): `use` 태그가 게임 사이로 안 샘 ·
전 카테고리 태그·이름·이모지 보유 · **덱 전체 원문 중복 0** · 초성 출제 600문제 이상 ·
기본 범위 300문제 이상.
🧪 음성 테스트 2건: 나라를 `body` 에도 열어보니 ✕FAIL → exit 1.
같은 단어를 두 카테고리에 넣어보니 `중복: 호랑이` 로 잡고 exit 1.
📸 두 게임 설정 화면 실측 — 몸으로 말해요 12종(나라·사자성어 없음) / 초성 퀴즈 14종.

⚠️ **판 사이 중복은 「덜」 나는 것이지 0이 아니다.** 판마다 덱을 새로 섞는 구조는
   그대로다(전 범위 기준 6판째에도 15문제 중 1.3개). 완전히 없애려면 이미 낸 문제를
   누적해두고 빼야 한다 — `rotation`(술래 중복 방지)과 같은 방식이면 된다.

### v0.16.1 (2026-08-14) — 🔠 초성 퀴즈: **답이 여러 개인 문제를 안 낸다** ★★

사장님: "초성 퀴즈가 좀 어이없는 게, ㄱㄹ에 동물 이렇게 나왔는데 기린도 되고 고래도
되고 그렇지 않아?"

**맞는 지적이고, 게임이 성립하지 않는 결함이었다.** 진행자가 정답을 모르는 채로
「누가 맞혔나」만 탭하는 구조라(그게 이 게임의 장점이다) **답이 둘이면 판정을 못 한다.**

**감으로 고치지 않고 덱 416개를 실측했다 — 원인은 덱 중복이 아니라 「단어 길이」였다**

| 글자 수 | 개수 | 초성 충돌 |
|---|---|---|
| 1글자 | 15 | **27%** |
| 2글자 | 115 | **19%** |
| 3글자 | 137 | 1% |
| 4글자 | 85 | 2% |
| 5글자↑ | 64 | 0% |

카테고리 안 충돌은 416개 중 30개(7.2%)뿐인데, **짧은 단어는 덱에 없는 답까지 쏟아진다.**
「ㅂ · 동물」은 덱에 뱀만 있어도 벌·붕어·백조가 다 답이다. 그래서 덱 중복만 없애는 걸로는
못 고친다.

**고친 방법 — `choWords()` 로 두 겹 거른다**
1. 공백 뺀 길이 **3글자 이상**만
2. 그러고도 초성이 겹치면 **양쪽 다** 뺀다
   (하나만 남겨도 사람은 다른 답을 외친다 — 애초에 그 초성을 안 내는 게 맞다)

→ 416개 중 **282개 생존, 카테고리는 전부 23개 이상** 남는다(기본 범위 117문제).
⚠️ **4글자로 올리면 안 된다** — 동물 5개·음식 7개·캐릭터 10개로 반토막 난다. 실측값이다.

- ⚠️ `nextWord()` 의 **재고갈 재빌드 경로도 같이 갈았다**(`p.kind === 'cho'` 분기).
  여기서 `buildDeck()` 을 부르면 두 글자 단어가 슬그머니 섞여 들어와 결함이 되살아난다.
- 설정 화면 개수를 **원래 덱 수(416)가 아니라 실제 출제 가능 수**로 바꿨다.
  걸러진 걸 모르면 "왜 이 단어가 안 나오지?" 가 된다.
- 규칙 전문에도 적었다 — 「ㄱㄹ·동물 같은 문제는 아예 안 나온다」.

**검증** — smoke-e2e **93항목**(초성 충돌 7항목 신설): 두 글자 이하 미출제 · 덱 내 초성
중복 0 · **기린/고래 짝 제거 확인** · 전 범위를 켜도 충돌 0 · 카테고리마다 20개 이상 생존 ·
재빌드 후에도 규칙 유지.
🧪 음성 테스트 2건: 필터를 옛 `buildDeck()` 으로 되돌리니 3항목 ✕FAIL → exit 1.
재빌드 경로만 되돌린 것도 ✕FAIL → exit 1.
⚠️ **검사기 자체를 한 번 고쳤다** — 재빌드 검사가 처음엔 **단어 하나만** 봐서
   필터를 벗겨놔도 우연히 3글자가 걸리면 통과했다(운으로 통과하는 검사). 덱 전체를
   보도록 바꾸고 나서야 음성 테스트가 제대로 걸렸다.

### v0.16.0 (2026-08-14) — 🔠 초성 퀴즈 추가 (5종 → 6종) ★★

문서의 「다음 작업」 1순위를 그대로 구현. 예상대로 **새 데이터가 0개**였다 —
`WORD_DECKS` 416개를 초성으로 바꾸는 `cho()` 3줄이면 끝난다.

**⚠️ 계획을 한 군데 바꿨다 — `deck` 모드 추가가 아니라 별도 도구(`cho`)로 갔다**
`deck` 은 **팀별 턴제**(팀이 돌아가며 제한시간 안에 몇 개)라 구조가 정반대다.
초성 퀴즈는 **전원이 동시에 보고 먼저 외치는** 게임이라 턴도 팀도 타이머도 없다.
`deck` 에 모드를 하나 더 넣으면 `beginTurn/endTurn/results/letters` 가 전부 무의미해진다.
대신 **덱 생성은 그대로 재사용**했다 — `buildDeck()`/`nextWord()` 를 그냥 부른다.

**이 게임의 비밀은 「화면」이 아니라 「정답 문자열」이다 ★**
v0.14.0의 `.priv` 는 *한 사람만 봐야 하는 화면*을 가리는 장치인데, 초성 퀴즈는
**전원이 같은 화면을 봐야 한다.** 가릴 화면이 없다. 대신 지켜야 할 것은 하나다 —
**정답은 맞히기 전까지 어떤 화면에도 렌더하지 않는다.**
→ 그래서 `.priv` 를 안 붙였고(붙이면 태블릿에서 안 커진다), 폰을 잡은 진행자도 같이
   맞힐 수 있다(`mc:'help'` 인 이유). 정답은 ① 맞힌 뒤 「직전」 줄 ② 「아무도 못 맞힘」
   을 눌렀을 때만 나온다.
⚠️ **E2E에 이 두 가지를 못으로 박아뒀다** — `★★진행 화면에 정답이 없다` ·
   `★.priv 를 안 붙였다`. 화면을 고칠 때 이 둘을 깨면 게임이 조용히 성립하지 않는다.

**설계**
- 흐름: `setup(문제 수·범위) → run(초성) → reveal(정답 공개) → done(점수)` — 타이머 없음.
  규칙 설명이 한 줄이라 v0.13.0의 「1분」 기준을 여유 있게 통과한다.
- **점수를 따로 안 들고 `log` 에서 매번 센다**(`choScores()`). 상태가 하나뿐이라
  「방금 취소」가 `log.pop()` 한 줄이고, `renderSig()` 의 `log.length` 가 점수 변화를
  자동으로 잡는다 → 서명 필드를 안 늘려도 된다(늘린 건 목표 문제 수 `n` 하나).
- 「방금 취소」는 점수만 되돌리는 게 아니라 **그 문제로 되돌아간다**(`p.cur` 복원).
  실수 탭은 「다른 사람을 눌렀다」가 대부분이라 문제째 다시 하는 게 맞다.
- 기본 범위는 `CHO_CATS = movie·music·char·animal` — 고유명사가 초성 퀴즈에 제일 잘 붙는다.
- 글자 크기: 초성은 정보가 적은 대신 **모두가 그것만 노려본다** → 제시어보다 크게
  (폰 48px / 태블릿 110px / 가로 태블릿 84px). ⚠️ `.wtxt.cho` 가 `.wtxt.long` **뒤에**
  선언돼 같은 특정도에서 이기므로 `.cho.long`/`.cho.xlong` 을 다시 적어야 한다 —
  안 적으면 속담 초성(20자)이 화면을 넘친다. 실제로 `cat=proverb` 로 찍어 확인했다.
- 동점이 흔한 게임이라(0점 다수) 종료 화면에서 **동점이 있으면 안내**하고
  순위 화면에서 조정하게 한다.

**🐛 같이 고친 것 — `shot.mjs` 의 합격 판정이 틀려 있었다**
`/문제|ERR |‼/` 로 판정하고 있어서 **라벨에 "문제"라는 낱말이 들어가자 전부 통과인데도
exit 1** 이 됐다. v0.12.0에 시나리오 쪽은 같은 이유로 이미 고쳤는데 도구 쪽이 남아 있었다.
지금은 시나리오가 찍는 표시(`‼` · `✕FAIL` · 맨 앞 `ERR `)로만 판정한다.
⚠️ **판정에 평범한 낱말을 넣지 말 것** — 항상 실패하는 검사기는 사람을 길들여 진짜
   실패까지 무시하게 만든다(랜덤 실패가 문제였던 v0.12.0의 `crime-e2e` 와 같은 병).

**검증** — smoke-e2e **86항목**(초성 20항목 신설): 변환 정확도(겹자음 `ㅉ`·공백 유지·
비한글 통과) · **정답 미노출** · `.priv` 미사용 · 맞히면 +1 후 즉시 다음 · 직전 줄에 정답 ·
되돌리기(점수·문제·카운터 3가지 복구) · 못 맞힘은 `by:null` · 목표 수 도달 시 자동 종료 ·
점수순 1등 · 개인전으로 넘어감 · 선수 전원 순위 포함.
🧪 음성 테스트 2건: 진행 화면에 정답을 노출시키니 `★★진행 화면에 정답이 없다: ✕FAIL` →
exit 1. `.priv` 를 붙여보니 `★.priv 를 안 붙였다: ✕FAIL` → exit 1.
📸 실측: 태블릿 가로 1024×768(스크롤 없음) · 폰 400×900 · **속담 초성 최악 케이스** 양쪽.

### v0.15.0 (2026-08-13) — 📺 사회자(관전) 기기 + 게임 분류 ★★★

사장님: "각자 핸드폰으로 접속하는데, 몸으로 말해요 같은 건 제시어를 태블릿으로 보여주는 게
잘 보이니까. **근데 이러면 태블릿이라는 사람이 하나 더 추가되는 거잖아** → 관전자로 들어갈 수
있게 해주거나… 차라리 사회자라고 해도 되겠다. 게임을 잘 분류해서 문제가 없는지 검토해 봐."

**사장님이 짚은 게 정확했다 — 태블릿이 「사람 한 명」으로 세어지는 게 진짜 문제다**
코드를 훑어보니 `players()` 호출이 17군데인데 그중 **11군데가 「선수 수」를 세는 자리**였다.
태블릿이 그냥 참가하면 팀이 홀수로 갈리고, 술래로 뽑히고, 도구 순번에 끼고, 순위표에 0점으로
올라온다. → `players/{pid}.spec` 플래그와 **`playing()`** 헬퍼를 만들어 11군데를 갈아끼웠다.
자세한 규칙은 위 「사회자(관전) 기기」 섹션.

**검토하다 걸린 것 3개** (전부 고쳤다)
1. 🐛 **`body` 의 「사회자 뽑기」가 그대로 떠 있었다.** 태블릿이 이미 사회자인데 선수 중에서
   또 뽑으면 **선수가 한 명 빠진다** — 이 기능으로 없애려던 바로 그 문제가 되살아난다.
   → 관전 기기가 있으면 뽑기 UI를 숨기고 「태블릿 기기가 사회자예요 · 5명 전원이 팀에 들어갑니다」로 대체.
2. 🐛 **호스트가 아닌 사람이 관전으로 바꾸면 팀 배정이 안 지워진다.** `teams.assign` 은 권위
   필드라 호스트만 쓸 수 있는데 관전 토글은 아무 기기나 누른다. → **지우는 대신 「보여줄 때」
   거르도록** 바꿨다(대기실 팀 박스 · `vDeck.members()` · 순위 입력 팀 라벨 3곳).
3. 🐛 **리더보드 필터가 검사에 안 걸렸다.** `calcPoints()` 만 검사하면 통과해버린다 —
   `vBoard` 는 행을 따로 만들어서 0점짜리 사회자 줄이 남는다. 음성 테스트로 발견해서
   **렌더된 줄 수를 세는 검사**를 추가했다.

**게임 분류 — 축은 두 개고 서로 직교한다**
사장님 말("개인전이나 팀전 혹은 사회자가 필요한 게임")을 그대로 3분류로 만들면 안 된다.
「사회자 필요」는 개인전/팀전과 나란한 세 번째 통이 아니라 **다른 축**이다.
- `mode` = 개인전 / 팀전 → 목록 화면의 묶음 기준
- `mc` = `need` / `help` + `mcWhy` → 게임 상세에 「누가 폰을 잡는가」로 표시

⚠️ **따져보니 5종 전부 「지금 안 뛰는 사람」이 폰을 잡아야 한다.** 술래는 미션 문장을 보면
   안 되고, 주자는 두 손으로 휴지를 당기고 있고, 연기 대결은 심사가 필요하다. 차이는 그
   사람이 **선수 중 한 명이어도 되느냐**뿐 — `body` 만 안 된다(설명자는 두 손이 바쁘고
   맞히는 팀은 화면을 보면 안 되므로 제3자가 필요). 그래서 `need` 는 `body` 하나다.
   → **사회자 기기의 실질 이득이 가장 큰 게임도 `body`다**(선수가 한 명도 안 빠진다).

**검증** — smoke-e2e 64항목(관전 관련 15항목 신설): 관전 켜면 선수·팀·술래 뽑기·도구 순번·
스마일 풀·순위 입력 후보·리더보드에서 전부 빠짐 · 방 목록에는 남음 · 해제하면 점수까지 복귀 ·
선수 1명 남으면 전환 차단 · 사회자 뽑기 숨김/복원 · 유령 팀원 0.
🧪 음성 테스트 3건: `makeTeams` 를 `players()` 로 되돌림 → exit 1,
`vBoard` 행을 `players()` 로 되돌림 → (처음엔 안 잡혀서 검사를 보강한 뒤) exit 1.

### v0.14.0 (2026-08-13) — 📺 태블릿 공용 화면 대응 ★★

사장님: "보통 사회자나 이 화면을 모두 다 같이 봐야 하는 경우라면 **태블릿 PC를 하나 이용해서
거기서 보여줄 수 있는 상태를 만들 거야**."

**실측부터 했다 — 두 가지가 걸렸다**
① 1024×768 태블릿에서 `#app{max-width:520px}` 때문에 **가운데 520px 기둥만 쓰고 좌우 500px이
   비어 있었다.** 글자도 폰을 30cm에서 보는 크기라, 상 위에 세워두고 60~100cm에서 보면 안 읽힌다.
② **더 중요한 것** — 공용 화면에 띄우면 안 되는 정보가 섞여 있었다. 몸으로 말해요의 제시어는
   지금도 화면에 「나머지는 화면을 보면 안 됩니다」라고 떠 있다. 태블릿에 크게 띄우면
   맞히는 팀이 다 봐서 **그 판이 그냥 끝난다.** ①만 고치면 이게 새 버그가 된다.

**그래서 화면을 「몇 명이 봐야 하는가」로 나눴다** (자세한 표는 위 「큰 화면 대응」)
- 전원 / 전원−1 → 큰 화면에서 커진다
- **딱 1명 → `.priv`** → 폰 크기 유지 + 폭 420px로 좁혀 「가까이서 혼자 보는 것」으로 보이게
- ⚠️ 멕썸노이즈는 **화면마다 다르다** — `brief`(술래 빼고 다 같이 읽는다, 앞 단계에서 술래를
  뒤돌려놨다)는 **크게**, `run` 하단의 미션 리마인더(술래가 화면 앞에서 뛰고 있다)는 `.priv`.
  같은 게임 같은 데이터라도 **그 순간 누가 화면 앞에 있느냐**로 갈린다.

**미디어쿼리 함정 2개** (둘 다 실제로 밟고 고쳤다)
- 🐛 **가로 폭만으로 걸면 폰 가로 모드가 걸린다.** `min-width:760px` 만 쓰면 926×428 폰에
  74px 글씨가 뜬다. → `and (min-height:640px)` 를 같이 건다. 926×428로 찍어 확인.
- 🐛 **가로 태블릿(768px 높이)에서 아래 버튼이 잘렸다.** 공용 화면은 스크롤하면 안 된다 —
  **뒤에서 보는 사람은 스크롤을 못 한다.** → `max-height:860px` 구간을 따로 만들어 조였다.

**검증** — 1024×768(가로) · 820×1180(세로) · 926×428(폰 가로) · 400×900(폰 세로) 4종 실측.
폰은 완전 무변화(회귀 0), 폰 가로는 큰 화면 모드에 안 걸림, 가로 태블릿은 스크롤 없이 한 화면.
smoke-e2e 48항목 통과.

⚠️ **CSS만 고쳤다.** 진행 도구 상태는 여전히 이 기기 로컬 전용이고, 태블릿은 「진행자 기기」
   자리에 그대로 들어간다(터치도 태블릿에서 한다). 태블릿=화면 / 폰=리모컨으로 쪼개는 건
   `S.play` 를 두 기기간 동기화해야 해서 별개의 큰 작업이다 — 지금은 안 했다.

### v0.13.0 (2026-08-13) — 🗑 복잡한 게임 2종 삭제 (7종 → 5종) ★★

사장님: "아까 하던 크라임씬 게임 그냥 없애주고 방금 업데이트 하려고 했던 안 봤어도 들키지 마도
없애 줘. **너무 복잡해서** 다른 좀 더 간단한 게임들로 채울 예정이야."

**교훈 — 도구가 잘 돌아가는 것과 게임이 굴러가는 것은 다른 문제다**
두 게임 모두 도구는 완성돼 있었고 E2E도 전부 통과했다. 문제는 그게 아니라 **규칙 설명에만
몇 분이 걸린다**는 것. 술 마시며 하는 자리에서 진행자가 규칙을 읊는 동안 판이 식는다.
v0.12.0에서 「안 봤어도」의 정산을 두 줄로 줄인 것도 그 방향이 맞긴 했지만, 애초에
**지목 → 문제 → 판정 → 정산이라는 골격 자체가 한 줄로 설명되지 않는다.**
→ 「확정된 기획 방향」에 **규칙 복잡도** 항목을 새로 못 박았다: **설명이 1분을 넘으면 안 된다.**

**지운 것** (약 2,050줄)
- `GAMES`에서 `mafia`(크라임씬) · `watch`(안 봤어도) 항목
- 데이터: `MAFIA_SITS`(배경 8종 × 배역 12개 = 96개) · `CRIME_EXT`(사건 3종) ·
  `CRIME_CASES` · `WATCH_DECKS`(4카테고리 88작품 99퀴즈) · `WATCH_CATS`
- 도구: `startMafiaTool`/`crimeBalance`/`pushMafia`/`MF`/`cOrder`/`cRevealed`/`cTally`/
  `cSearchLeft`/`cBoard`/`cNextGrill`/`cRevealPlace`/`cVoteAdj`/`cResolveVote` ·
  `startWatchTool`/`watchBuildDeck`/`watchDraw`/`watchTally`/`watchTop`/`watchSettle`/`watchTimerStart`
- 화면: `vMafiaTool` · `vWatch` · `mfActionPanel` · **`vRole`(참가자 「내 역할」 화면 통째로)**
- 핸들러: `wa-*` · `mf-*` · `cr-*` · `go-role`
- CSS: `.holdbox/.secret/.role-team/.story/.rolelist/.rolerow/.guide/.gstep/.truthbox/
  .infoblk/.balbox/.linelist/.evbox/.actbox/.astep/.pgrid/.pbtn/.howto/.turnbig/.readbox/
  .grillbox/.putdown/.myturn/.stepbtn` (약 130줄)
- 검증 시나리오 4개(`crime.js`/`crime-e2e.js`/`watch.js`/`watch-e2e.js`)
- 죽은 코드: `fmtClock()`(카운트다운 표시. `watch` 말고 쓰는 데가 없었다)

**⚠️ 한 파일 앱에서 게임을 들어낼 때 같이 끊기는 것들** (이번에 실제로 다 걸렸다)
`index.html`이 단일 파일이라 게임 하나를 지우면 **공용 배선이 조용히 끊긴다.** 순서대로 훑을 것:
1. `GAMES` 항목 · 데이터 상수 · 도구 함수 · 뷰 함수 · `act()` 핸들러
2. **`vPlay()` 디스패치**(`case 'watch'` 등)
3. **`renderSig()`** — 그 도구만 쓰던 필드(`r.mafia`, `p.pts`, `p.votes`…)
4. **`emptyRoom()` / `pushHostState()` / `subscribe()`** — 방 노드 필드(`mafia:null`)
5. **`clearBots()`** — 봇을 지우던 부수 경로(`mafia.roles`에서도 제거하고 있었다)
6. **`bind()`** — 그 화면 전용 DOM 바인딩(`#role-hold` 꾹 누르기)
7. **뷰 라우팅 3곳** — `render()`의 뷰 맵 · 헤더 타이틀 맵 · 뒤로가기 대상 목록(`'role'`)
8. **`pl-quit`의 진행 중 판정** — 지운 도구의 필드를 보고 있었다
9. **CSS** — 클래스가 통째로 고아가 된다. ⚠️ 단, `.m1/.p1` 처럼 **`'m'+(i+1)` 로 조립되는
   클래스는 정적 검색으로 안 잡힌다.** 지우기 전에 조립 패턴(`${...}`)을 반드시 확인할 것

**🛡 지워진 게임의 옛 점수 기록**
방에 `gameId:'mafia'` 기록이 남아 있을 수 있다. 이미 폴백이 있었다 —
`sc.weight`를 저장해두므로 `G()`가 없어도 점수는 계산되고, 리더보드는 🎲 + 원래 id로 표시한다.
`gcVars(undefined)` 가 `--gc:undefined` 를 내던 것만 기본색 폴백으로 고쳤다.
**이 폴백을 깨지 말 것** — 음성 테스트로 `g?.name` 의 `?.` 를 빼보니 리더보드가 즉시 죽었다.

**➕ `tools/scenarios/smoke-e2e.js` 신설 (48항목)**
시나리오 4개를 다 지우면 검증 도구에 돌릴 게 하나도 안 남는다. 대신 **남은 5종 + 백본을
전부 도는** 스모크를 만들었다 — 레지스트리 정합 · 전 게임 상세 렌더 · deck 한 판(팀 순위 저장까지) ·
noise/cup 기록형(실패=꼴찌 · 빠른 순 정렬) · act 카드/리롤 · smile 판정 3분기 ·
**술래 뽑기 중복 방지 로테이션** · **삭제된 게임 기록이 섞인 리더보드** · 봇 정리.
🧪 음성 테스트 2건: `g?.name`의 `?.` 제거 → 리더보드 예외로 exit 1,
smile 다수-걸림 분기 훼손 → 해당 항목만 ✕FAIL로 exit 1.
⚠️ `S._lastPick` 은 `{gameId: pid}` **맵**이다(통째로 읽으면 안 된다). 그리고 `act('pick')` 은
`rollReveal` 을 await 하는 async라, 누가 뽑혔는지는 동기적으로 쌓이는 `rotation` 배열로 확인한다.

⚠️ **되살릴 일이 생기면** `git log` 의 v0.11.0 / v0.12.0 커밋에 데이터가 통째로 있다.
   **데이터만 가져오고 UI는 다시 짜는 게 낫다** — 문제는 데이터가 아니라 그 UI가 요구한 절차였다.

### v0.12.0 (2026-08-13) — 🎬 「안 봤어도 들키지마」 전면 재설계 ★★★

사장님: "안 봤어도 들키지 마 게임을 좀 더 쉽고 깔끔하게 만들어 줬으면 좋겠어.
코인이나 이런 부분 말고 충분히 승패가 나뉠 수 있도록 말이야. **그리고 문제도 만들어줘야 돼**"

**진단 — 이 게임은 시작조차 못 하는 게임이었다**
막힌 곳이 두 군데였고 둘 다 앱이 손을 놓고 있었다.
① **출제자가 「본 사람만 아는 문제」를 직접 만들어야 했다.** 술 마시다가 그걸 지어내는 사람은
   없다. 첫 라운드에서 "뭐 내지?" 하다가 흐지부지된다.
② **코인 정산이 두 갈래로 갈렸다**(맞추면 지목받은 수만큼 / 틀리면 지목한 사람들과 출제자가 1개씩).
   계산이 헷갈리는 데다 앱은 **± 버튼만 주고 손으로 세게** 했다. 아무도 안 센다 = 승패가 안 난다.
   게다가 원문 규칙이 `"…아무도 못 맞추면 출제자의 ___"` 에서 잘려 `todo` 경고 박스가 박혀 있었다.

**바꾼 것**

| | v0.11.0까지 | v0.12.0 |
|---|---|---|
| 문제 | 출제자가 직접 만든다 | **앱이 작품·퀴즈·정답을 준다** (88작품 99퀴즈) |
| 출제자 자격 | 그 작품을 본 사람이어야 함 | **안 봤어도 된다** — 정답을 앱이 알려준다 |
| 지목 | 손가락, 1인 2표, 몰아주기 가능 | **1인 1표**로 단순화. 앱이 「누가 누구를」 받는다 |
| 정산 | 두 갈래 코인 규칙 · 수동 ± | **앱이 자동.** 규칙 두 줄 |
| 승패 | 코인 세다가 흐지부지 | 라운드마다 결과 · 총점 순위 자동 |

- **점수 규칙은 딱 두 줄이다. 늘리지 말 것.**
  `맞히면 → 답한 사람 +2` / `틀리면 → 그 사람을 지목한 사람들이 각각 +1`
  이 두 줄이 전략을 다 만든다 — 안 봤으면 눈에 띄지 말고, **봤으면 일부러 의심을 사서 +2를 노린다**
  (원 규칙의 「맞출 자신이 있으면 일부러 의심을 사라」는 팁이 그대로 살아 있다).
  잘렸던 출제자 규칙은 **자연 소멸**했다 — 앱이 문제를 주므로 출제자에게 난이도 책임이 없다.
  출제자는 지목에 참여하되(누가 봤는지는 모르니까) 지목 대상은 아니다. `todo` 경고 박스 제거.
- **흐름**: `setup → ready(폰 넘기기) → card(출제자만) → talk(토론) → vote(지목) →
  answer(⭕/❌) → result → 다음 라운드 → done`. 전원이 한 번씩 출제자를 하면 끝.
- ⚠️ **`votes`를 개수가 아니라 `{지목한사람: 지목당한사람}`으로 받는다.** 크라임씬은 개수만
  받아도 되지만(정산이 없다), 여기서는 **누가 지목했는지 모르면 자동 정산이 성립하지 않는다.**
  "하나 둘 셋" 동시 지목은 그대로 하고, 진행자가 한 명씩 탭해 6명이면 6탭 · 30초면 끝난다.
- **`WATCH_DECKS`** — 한국 영화 22 / 외국 영화 22 / 드라마 22 / 애니 22. 작품당 퀴즈 1~2개.
  ⚠️ 배우·실존 인물 이름은 안 쓴다(이름 원칙). 등장인물은 「주인공」·「여동생」처럼 역할로 부른다.
  `watchDraw()`가 `used`로 한 게임 안 작품 중복을 막고, 다 쓰면 비워 재활용.
  너무 유명해서 안 봐도 아는 퀴즈가 걸리면 「다른 문제」로 리롤.

**검증** (6인 헤드리스 E2E · `tools/scenarios/watch-e2e.js`, 44항목)
덱 정합(작품·퀴즈문 중복 0, 정답 누락 0) · **★정답이 홀드 밖으로 안 샘**(card·answer 두 화면) ·
**★토론 화면에 퀴즈문·정답 안 뜸** · 지목 대상에서 출제자·본인 제외 · 되돌리기 ·
오답 정산(지목자 5명 각 +1 · 지목만 받아선 0점 · 헛다리 0점) · **정답 +2** ·
**★동점이면 최다 득표자 전원이 각자 답한다** · 작품 중복 0 · 출제자 전원 1회씩 · 6라운드 완주 ·
점수 내림차순 순위 반영 · 타이머 누수 0 · 3명 미만 차단.
🧪 **음성 테스트 2건**: `+2`를 `+1`로 바꾸니 exit 1, 토론 화면에 정답을 노출시키니 exit 1.

### 🧰 같이 고친 것 — 검증 도구 (이번 세션에서 실제로 물렸다)

- 🐛 **`crime-e2e.js`의 `★order[0]≠범인` 단언이 틀려 있었다.** v0.11.0에서 `order`를
  제대로 셔플하도록 고쳤으므로 7명이면 **범인이 맨 앞에 오는 게 1/7 확률로 정상**이다.
  그런데 검사는 "이번 판에 맨 앞이 아니어야 한다"고 요구했다 → **40회 중 6회 랜덤 실패**.
  랜덤하게 실패하는 검사기는 사람을 길들여서 진짜 실패까지 무시하게 만든다.
  → **40판 배정해 범인 자리 분포를 본다**(옛 버그면 40판 전부 0). 30회 연속 통과 확인,
  일부러 `order = pool`로 되돌리니 `맨앞 40회 / 1가지`로 exit 1.
  ⚠️ 재배정 루프는 **`const M = S.room.mafia` 를 잡기 전에** 끝낼 것 — `mf-deal`이 노드를 새 객체로 간다.
- 🐛 **`demo.mjs`가 CDN이 막힌 환경에서 조용히 아무것도 검사하지 않았다.**
  `firebase-database`만 스텁으로 갈고 `firebase-app` import는 남겨뒀는데, 원격 컨테이너처럼
  `gstatic.com`이 막힌 곳에서는 그 import가 실패해 **`<script type="module">`이 통째로 안 돈다.**
  시나리오가 실행조차 안 됐는데 에러도 안 난다 — **"다 통과한 줄 알았는데 애초에 안 돌았다"**가
  제일 나쁜 실패다. → `initializeApp`/`getDatabase`/`ref`까지 스텁으로 갈아 **네트워크 없이 돈다.**
  `set`/`update`/`remove`/`initializeApp` 중 하나라도 못 막으면 **중단**하도록 가드도 넣었다.
- 🔧 **`shot.mjs`가 Edge만 찾았다.** 맥·리눅스 크롬, `CHROME` 환경변수, 그리고 Playwright
  크로미움(`PLAYWRIGHT_BROWSERS_PATH`)까지 찾는다. 플래그는 크로미움 공통이라 그대로 돈다.
- 🔧 **E2E 합격 판정을 출력 텍스트 검색에서 `ck()` 실패 카운트로 바꿨다**(watch 시나리오).
  기존 방식(`/문제|false/`)은 라벨에 "문제"라는 단어가 들어가는 순간 **항상 실패**로 뜬다.
  이 게임은 라벨에 "문제"가 안 들어갈 수가 없어서 즉시 물렸다 → 라벨은 「퀴즈」로 쓰고 판정은 카운터로.
- ➕ `tools/scenarios/watch.js` — 화면 스냅샷용(`v=rules setup ready card talk vote tally
  answer result done`). 크라임씬의 `crime.js`와 같은 형식.

### 도구 정비 (2026-08-13) — 🧰 다른 기기에서 그대로 이어지도록 ★

사장님: "다른 컴퓨터 또는 핸드폰에서 이어해도 문제 없게 해줘" (= 개발 작업 이어하기)

리포 위생은 이미 깨끗했다(추적 누락 0, 빌드 0, 비밀키 0). 진짜 마찰은 **검증 도구가
리포에 없다는 것**이었다 — 매 세션 임시 폴더에 파이썬 스크립트를 새로 써서 쓰고 버렸다.
다른 기기에는 그게 통째로 없다. 게다가 이 문서의 스크린샷 레시피는 **틀려 있었다**
(`--no-sandbox --user-data-dir` 누락 → 헤드리스가 그냥 멈춘다. 오늘도 2분 타임아웃을 맞았다).

- `tools/check.mjs` — 문법 + **끊어진 참조**(지운 함수를 아직 호출하는 것) + 죽은 코드.
  단일 HTML이라 `node --check` 가 안 되는 문제를 흡수하고, **원본 줄번호로 되돌려** 준다.
- `tools/demo.mjs` — Firebase 쓰기를 막은 검증 사본 생성.
  ⚠️ 예전 방식(호출부 7군데를 문자열로 패치)은 화면을 고칠 때마다 그 문자열이 사라져
  **조용히 무력화**됐다. 지금은 `firebase-database` **import 한 줄**을 가로채 스텁으로
  바꾼다 — 호출부가 어떻게 바뀌어도 막힌다. 호출부는 다시는 패치하지 말 것.
- `tools/shot.mjs` — 헤드리스 스샷/DOM 덤프. Edge 경로 자동 탐색, 필수 플래그 내장,
  `--grid` 로 여러 화면을 한 장에(우측 잘림 아티팩트가 없다).
- `tools/scenarios/crime.js` · `crime-e2e.js` — 화면 스냅샷 / 전 구간 자동 점검.
  E2E는 **과거에 실제로 터진 것들**만 모아 두었다(order 누수·정체 누수·참가자 폰 조작버튼 등).

**검증**
① 도구 음성 테스트 — 일부러 `const order = pool` 버그를 심으니 E2E가 `order[0]≠범인: false`
  로 잡고 exit 1. 문법을 깨니 `index.html:2411` 로 짚음(실제 파손 위치 2410).
② **새 기기 흉내** — 추적 파일만 빈 폴더에 복사하고 이 문서의 명령을 그대로 실행 →
  설치 없이 check / 사본 / E2E / 스샷 전부 통과.
③ 라이브 RTDB 방 목록 확인 → **유령 방 0건**(사본이 실제로 쓰기를 못 한다).

⚠️ `index.html` 은 건드리지 않았다. `APP_VERSION` 은 v0.11.0 그대로.

⚠️ **이 문서를 스크립트로 고칠 때**: bash `python -c "..."` 안에 백틱을 넣으면 셸이
명령 치환으로 먹어 **코드 스팬이 통째로 사라진다**(이 항목도 그렇게 한 번 깨졌다).
반드시 `.py` 파일로 저장해서 `python 파일.py` 로 돌릴 것.

### v0.11.0 (2026-08-13) — 🍽 크라임씬을 **테이블 게임**으로 ★★★

사장님: "각자 핸드폰 보면서 증거 찾는 건 좀 별로인 것 같은데?
같은 숙소에 있으면서 연기하고 증거 찾고 하는 걸 어떻게 풀어갈 수 있지?"

**진단** — v0.10.0은 조사도 투표도 각자 폰이었다. 그러면 7명이 둘러앉아 각자 화면만
들여다보는 그림이 된다. 정보 비대칭(찾은 증거를 나만 봄)은 이론상 재밌지만,
숙소에서 실제로 벌어지는 건 **침묵**이다. 말하게 만드는 장치가 아니라 말을 막는 장치였다.

**바꾼 것 — 폰 하나를 돌려본다**

| | v0.10.0 | v0.11.0 |
|---|---|---|
| 배역 확인 | 각자 폰 | 각자 폰 (유지 — 비밀이니까) |
| 자기소개 | 없음 | **앱이 한 명씩 이름을 띄운다** (조용한 사람도 반드시 말함) |
| 증거 조사 | 각자 폰에서 몰래 | **진행자 폰을 넘겨받아 모두 앞에서 소리 내어 낭독** |
| 심문 | 없음 | **앱이 「누가 누구에게 무엇을」을 지시** (`GRILL_Q` 10종) |
| 투표 | 각자 폰 | **"하나 둘 셋" 손가락 지목** → 진행자가 개수만 ±로 입력 |

- 단계: `brief → intro → search → grill → final → vote → ended`
  (`search ↔ grill` 왕복 가능 — 심문 중 「현장 더 뒤지기」)
- **증거는 전원 공개**가 됐다. 대신 조사 횟수가 자원이다(기본 3곳, 「한 번 더」로 증설).
  못 뒤진 현장은 끝까지 미공개 → 범인 흔적이 안 나올 수도 있다(그게 범인의 운).
- 참가자 폰에는 **누르는 버튼이 하나도 없다.** 지금 뭘 할지 안내 + 「📵 폰을 내려놓고
  진행자 화면을 보세요」 + 내 차례 배지(`myturn`)만.
- 데이터: `order`(발언 순서) · `turn`(커서) · `revealed[{p,by}]` · `maxSearch` ·
  `grill{q,t,hint}` · `tally{pid:개수}`. `searched`/`votes`/`round`는 제거.
  ⚠️ **`order` 는 `pool` 을 그대로 쓰면 안 된다** — `pool[0]`이 범인이라 순서가 정답을 흘린다.
  반드시 `shuffle(pool.slice())`.
- **참가자는 이제 `mafia` 노드에 아무것도 쓰지 않는다** → `subscribe()`의 `acts/votes`
  되받기 제거. 호스트가 유일한 진실원.
- 순위: 개인 투표 기록이 없으므로 **범인 자리만 확정**(잡히면 꼴찌 / 도망가면 1등),
  나머지는 의심 많이 받은 순으로 깔고 진행자가 순위 화면에서 손으로 조정.
- 범인 흔적은 `push`가 아니라 **무작위 위치에 `splice`** — 항상 마지막 줄이면 외워버린다.

**검증**(7인 헤드리스 E2E): 배역 중복 0 · 피해자 배역 제외 · `order[0]≠범인` ·
범인 흔적만 배치 · 자기소개 커서 · **내 차례 배지가 당사자에게만** · 낭독 화면 증거 노출 ·
중복 조사 차단 · 조사 소진 → 심문 전환 · **참가자 폰에 조작 버튼 0개** · 심문 자문자답 0 ·
집계 ±·정답/오답/**동점** 3판정 · 순위 정렬 · **홀드 밖 범인 단서 0 · 배역별 HTML 구조 동일**.
🎨 같이 고침: `.howto b`가 인라인 `<b>`까지 블록으로 만들어 문장이 토막 나던 것 →
`>b:first-child`. 낭독 중 같은 증거가 아래 게시판에 중복 노출되던 것 → 접음.
증거 공개 방식이 바뀌었으므로 「찾은 증거는 숨겨도 된다」 문구 3곳 정리.

### v0.10.0 (2026-08-13) — 🔎 마피아 → **크라임씬(추리 상황극)** 전환 ★★★

사장님: "마피아 느낌이 전혀 안 든다. 차라리 크라임씬처럼 스토리를 얹고 증거를 모아
범인을 추리하는 형태로 가는 게 낫지 않나?"

**왜 크라임씬이 이 앱에 더 맞나**
마피아는 밤마다 사람이 죽어 **탈락자가 생겨 남은 시간을 구경만** 해야 하고,
상황극이 붙을 자리가 낮 토론 몇 분뿐이었다. 크라임씬은 **아무도 탈락하지 않고**,
증거라는 구체적 이야깃거리가 계속 나와 연기가 살아난다. 숙소 한 판(30~60분)에 딱 맞다.

**게임 구조** — 브리핑 → [조사 → 토론] × 2~3 → 최종 변론 → 투표 → 진상 공개
- 범인 1명 + 나머지는 **무고한 용의자**. 끝까지 전원이 연기·추리한다.
- **조사**: 한 사람이 현장 한 곳씩. **찾은 증거는 본인 폰에만** 보인다 →
  말할지 숨길지가 선택이 되어 정보 비대칭이 생긴다(이게 토론을 살린다).
- **판정**: 최다 득표자가 범인이면 용의자 승 / 못 맞히거나 동점이면 범인 승.
- 순위: 범인이 이기면 범인 1등, 잡히면 범인을 지목한 사람들이 상위.

**데이터 — 기존 자산 재활용**
`MAFIA_SITS`의 배역 96개(알리바이·비밀·연기팁·대사)를 **그대로 두고**,
`CRIME_EXT`에 사건별 확장만 얹었다: `victim`(피해자 배역·배정에서 제외) · `brief` · `found` ·
`places[]`(현장 5곳 × 고정 증거 2개) · `ext[배역]`= `rel`(피해자와의 관계) · `mot`(동기) ·
`trace`(**그 사람이 범인일 때만** 현장에 추가되는 결정적 증거).
현재 사건 **3종**: 왕궁 연회 · 심야의 크루즈 · 예능 촬영장 (각 용의자 11명까지).
⚠️ 나머지 5개 배경도 `CRIME_EXT`에 같은 형식으로 추가하면 바로 선택지가 된다.

**⚠️ 유지보수 주의**
- 내부 노드 이름은 **여전히 `room.mafia`** 다(동기화 배선 공유). 게임만 바뀌었다.
- 쓰기 주체 분리는 그대로: 호스트=`pushMafia()`로 phase/round/result,
  참가자=`searched/{round}/{내pid}`·`votes/{내pid}`.
- 마피아 루프(밤/조사/보호/처형)와 관련 함수는 **전부 제거**했다.
- **정체 누수 금지 원칙 유지**: 항상 보이는 영역은 전원 동일, 범인 여부·흔적은 홀드 박스 안에서만.
- 진행자가 참가 중이면 **정답(범인·전체 증거) 보기 잠금**.

**검증**: 7인 풀게임 — 범인 1명·피해자 배역 제외·배역 중복 0 · 전원 관계/동기/알리바이 보유 ·
**범인 흔적만 현장 배치**(무고한 사람 흔적 미배치) · **증거가 본인에게만 노출** ·
홀드 밖 범인 단서 0 · 안내문구 전원 동일 · 2차 누적 증거 · 정답/오답 판정 양쪽 · 순위 정렬.
🐛 같이 고침: `vScore`가 draft 없이 렌더되면 죽던 것 → 빈 상태 화면으로 방어.

### v0.9.1 (2026-08-13) — ⚖️ 마피아 인원 밸런스 + 진행자 참가

사장님: "6명이면 역할이 가능한가?" → 확인해보니 **6명에서 밸런스가 무너져 있었다.**

- 🐛 **`suggestMafia()`가 「전체 인원」 기준이었다.** 6명이면 마피아 2명을 뽑는데,
  진행자가 빠져 실제 역할은 5명 → 5명에 마피아 2명. **첫 투표에서 시민을 한 명만
  잘못 지목하면 2:2가 되어 그 자리에서 마피아 승.** 한 라운드 만에 끝난다.
  → 기준을 **실제 역할 인원(pool)** 으로 바꾸고 통상 비율 적용: `3~6=1 · 7~9=2 · 10+=3`.
- 🙋 **진행자도 기본 참가**(`incHost:true`). v0.8.0부터 앱이 진행을 맡으므로 사람 사회자가
  필요 없어졌는데 기본값이 그대로 「구경」이었다. 6명이면 6명 다 플레이하는 게 맞다.
- 🔒 **진행자가 참가 중이면 「정체 보기」 잠금.** 안 그러면 본인이 전원 정체를 보는 치트가 된다.
  구경 모드일 때만 열람 가능.
- ⚖️ **설정 화면에 실시간 밸런스 판정**(`mafiaBalance`) — 「✓ 균형이 좋아요 / 자객 1명 · 시민 5명」,
  범인이 절반 이상이면 **딜 버튼 자체를 비활성화**. 인원이 바뀌면 손대지 않은 범인 수는
  권장값을 자동 추종(`cntAuto`).
- 검증: 3~12명 전 구간 밸런스 표 · 6인 실게임(범인1/시민5, 첫 오판 처형 후에도 게임 계속) ·
  진행자 참가 시 정체 배지 미노출 · 범인 과다 시 딜 차단.

### v0.9.0 (2026-08-13) — 🕵️ 정체 누수 차단 + 전원 「비밀」 부여 ★★

사장님 지적 2건. 첫 번째는 **내가 v0.7.0에서 만든 실제 누수**였다.

**① 화면만 봐도 마피아인 걸 알 수 있었다**
`vRole` 최상단 truthbox가 팀에 따라 색·문구가 달랐다
(시민=초록 「거짓말 금지」 / 마피아=빨강 「거짓말 해도 됩니다」). **꾹 누르지 않아도 보인다.**
낮 행동 패널도 마피아만 빨간색 + 동료 이름이 떠 있었다.
→ **원칙 수립: 항상 보이는 영역은 전원 100% 동일해야 한다. 팀 정보는 전부 홀드 박스 안으로.**
  - truthbox 문구를 팀 무관 단일 문구로 통일
  - 동료 목록·조사 결과를 홀드 박스 안으로 이동
  - 낮/투표 패널의 색 클래스 제거(전원 동일)
  - **밤에는 시민에게도 같은 모양의 「메모」 지목판**을 준다(`mf-hunch`, 아무 데도 전송 안 함)
    — 겉모습을 맞춰 흘끗 보기로 구분 못 하게
  - 검증: 마피아/시민/조사자 3화면의 홀드 밖 텍스트에서 '자객·마피아·동료·거짓말을 해도'
    노출 0건, 안내문구·패널 색상 동일 확인

**② 연기할 거리의 밀도가 낮았다**
근본 원인은 **시민에게 숨길 게 없다**는 것. 시민은 사실만 말하면 되니 마피아만 어색해진다.
→ **배역 96개 전부에 `s` 「비밀」 추가** — 살인과 무관하지만 들키면 곤란한 것.
  (근위대장 "잠시 자리를 비웠다. 말하는 순간 목이 날아간다" / 재상 "국고에서 돈을 빼돌리고 있다"
   / 광대 "왕을 흉내 내는 공연을 몰래 준비하고 있었다")
  **모두가 얼버무릴 이유가 생기므로 모두가 수상해 보인다.**
- 규칙 보강: **배역·알리바이는 사실대로 / 비밀은 숨겨도 반칙 아님 / 범인만 알리바이도 거짓말 가능.**
- 비밀 96개 전부 고유(중복 0) 확인.

### v0.8.0 (2026-08-13) — 🤖 마피아 게임 루프 자동화 (앱이 사회자를 대신한다) ★

사장님: "사회자가 하는 것들을 애초에 사이트가 알아서 해주는 로직으로 짜야 할 것 같다."
→ 진행표를 사람이 읽고 손으로 돌리던 것을, **앱이 직접 게임을 진행**하도록 재작성.

**루프**: 낮(토론) → 투표(처형) → 밤(지목) → 아침(결과) → 반복 → 승패 자동 판정
- `room.mafia`에 `phase`('day'|'vote'|'night'|'ended') · `round` · `alive` · `acts` ·
  `votes` · `checkRes` · `log` · `winner` 추가.
- **밤 행동은 각자 폰에서 비밀 지목** — 마피아(대상)·조사(정체 확인)·보호(살리기).
  사회자는 "누가 제출했는지"만 보고 결과는 못 본다.
- 조사 결과는 **그 사람 폰에만** 표시. ⚠️ 낮에도 계속 보이게 해야 한다
  (정작 써먹는 시점이 토론 중 — 초기 구현에서 낮 분기가 먼저 반환돼 안 보이는 버그가 있었음).
- 마피아는 **동료가 누구인지 앱이 알려준다**(앱 진행이라 밤에 눈뜨는 절차가 없으므로 필수).
- 투표는 각자 폰 → 사회자 화면에 실시간 집계. **동점이면 아무도 처형하지 않는다.**
- 승패 자동 판정: 마피아 0명=시민 승 / 마피아 ≥ 나머지=마피아 승. 종료 시 전원 정체 공개.
- 「🏆 순위 입력하기」가 **승리 진영을 상위로 자동 정렬**해 순위 화면으로 넘긴다.

### ⚠️ 쓰기 주체 분리 (이 기능의 핵심 제약)
참가자가 밤 행동·투표를 직접 쓰므로, 호스트가 `mafia`를 통째로 덮으면 다 날아간다.
- `pushHostState()`에서 **`mafia`를 제외**했다. 호스트는 `pushMafia(patch)`로 하위 경로만 갱신.
- 참가자는 `mafia/acts/{round}/{slot}`, `mafia/votes/{round}/{내pid}`만 쓴다.
- 호스트는 스냅샷에서 **`mafia.acts`/`mafia.votes`만 병합**해 받는다(나머지는 호스트가 진실원).
- 딜/회수는 `set`/`remove`로 노드 전체를 다룬다.
**새 mafia 필드를 추가할 때 이 분리를 깨지 말 것.**

- 검증: 7인 풀 게임 시뮬 — 처형·킬·보호 성공(생존 유지)·조사 판정 정확·사망자 화면·
  마피아 동료 표시·시민 승/마피아 승 양쪽 종료·순위 승리진영 우선 정렬.
  능력명↔배역명 충돌 0 확인(예: 왕궁의 능력은 점성술사인데 배역 목록엔 없음).

### v0.7.0 (2026-08-13) — 🎭 배역별 알리바이·연기 팁·대사 + ★거짓말 규칙

사장님 두 가지 지시:
1. "연기 팁·대사·알리바이를 본인에게 알려주는 게 핵심. **배역 선에서 서로 엉키지 않게** 잡아줄 것"
   (본인이 제대로 연기를 못 하는 경우가 많으므로)
2. "**마피아는 거짓말 가능, 다른 사람은 배역·알리바이에 대해 거짓말 불가.**
   이게 마피아 게임과 잘 연동되어야 한다"

- **배역 96개 전부**(8배경 × 12배역)에 필드 3종 추가:
  - `a` **알리바이** — 그날 밤 어디서 뭘 했는지 한 문장
  - `t` **연기 팁** — 말투·태도 지시("말끝을 흐리고 사장 쪽을 슬쩍 보며")
  - `l` **대사 2개** — 막혔을 때 그대로 읽으면 되는 문장
- ⚠️ **같은 배경 안에서 알리바이가 절대 겹치지 않게** 장소를 하나씩 배분했다
  (크루즈=조타실/라운지/특실/갑판/주방/카지노/선미/복도/객실/계단/엔진실/발코니).
  겹치면 "나도 거기 있었는데?"로 판이 엉킨다. 검증 스크립트로 중복 0 확인.
- **교차 검증 훅**을 일부러 심었다 — 왕자 "발코니에 있었다, 근위대장이 봤을 것" ↔ 근위대장
  "정문을 지켰다, 나간 사람 없다" 처럼 서로 확인 가능한 진술이 있어야 추리가 성립한다.
- ★ **거짓말 규칙을 게임의 대전제로 명시**(이게 없으면 알리바이 카드가 무의미):
  - 참가자 화면 상단 `truthbox` — 시민=초록 "거짓말 금지" / 마피아=빨강 "거짓말 해도 됨"
  - 꾹 누른 정체 화면 하단에도 재차 명시
  - 사회자 화면 최상단 + 진행표 첫 줄 + 게임 규칙 전문에 반영
  - **거짓말 금지는 배역·알리바이에만 적용**, 말투·태도 연기는 자유임을 명시
- 검증: 96배역 알리바이 중복 0·팁/대사 누락 0 · 시민/마피아/능력자 3종 화면 · 사회자 진행표.

### v0.6.0 (2026-08-13) — 🎭 상황극 마피아 몰입 강화 (이름표 → 실제 상황극)

사장님: "상황극에 몰입할 수 있게 스토리나 직업이 잘 들어가 있나?" → **아니었다.**
v0.4.0은 배경·배역이 **이름표뿐**이었고, 왕궁 연회에 「경찰」·「의사」가 등장해 몰입을 깨고 있었다.

- **`MAFIA_SITS` 전면 재작성** — 배경 8종 각각에:
  - `intro` — 사회자가 소리 내어 읽는 **사건 발단** 3문장
  - `mafia`/`cop`/`doc` — **세계관 언어로 바꾼 진영·능력 이름**(기능은 동일)
    예: 왕궁 연회 = 자객 / 점성술사 / 어의 · 좀비 대피소 = 감염자 / 정찰병 / 군의관
  - `chars[].d` — **배역별 연기 힌트 한 줄**("모든 방을 드나든다. 소문을 전부 안다.")
  - 배역 **12개씩**(참가자 상한과 동일 → 중복 배정 없음)
- **참가자 화면**: 사건 내레이션 → 내 배역 + 연기 힌트(공개) → 꾹 눌러야 보이는 정체
  (세계관 이름 + 능력 설명). 시민도 "말과 눈치로 찾아내라"는 안내를 준다.
- **사회자 화면**: 사건 내레이션 크게(읽어주기용) + **접이식 진행 순서표**
  (밤 → 진영별 지목 순서 → 아침 → 투표 → 승리 조건). 마피아를 처음 진행하는 사람도 가능.
  로스터 배지도 세계관 이름으로.
- 설정 화면에서 배경을 바꾸면 **내레이션·진영·능력 이름이 즉시 미리보기**된다.
- 좀비 대피소 배역 '의사' → '약사'(능력명 군의관과 혼동 방지).
- 검증: 8배경 데이터 정합(intro·진영·능력·배역12·설명 전부) · 배정 후 능력자 이름이
  세계관 명칭인지(「경찰/의사」 문자열 부재) · 마피아/능력자/시민 3종 참가자 뷰 · 진행표 노출.

### v0.5.0 (2026-08-13) — 🧪 테스트 봇 (혼자서 게임 도구 돌려보기)

사장님: "팀원이 없어서 도구를 못 돌려본다" → 가짜 참가자를 넣을 수 있게.

- `addBots(n)` / `clearBots()` — 봇 pid는 `bot_` 접두. **실제 `players` 노드에 쓴다.**
  로컬에만 두면 다음 스냅샷에서 호스트가 `S.room.players = v.players`로 덮어써 사라진다.
  실제 노드에 쓰므로 팀 편성·역할 배정·순위·랭킹이 **전부 기존 코드 그대로** 동작한다.
- `isOn(p)`가 `p.bot`이면 항상 온라인 취급(하트비트가 없어 회색 점이 뜨는 것 방지).
- `clearBots()`는 players뿐 아니라 **teams.assign · mafia.roles · rotation에서도 제거**한다.
- 진입점 2곳: 대기실(3명 미만일 때 안내 카드) · 설정 → 진행자 도구(+1/+3/+5, 전부 내보내기).
  상한 12명.
- 검증: RTDB 경로 키 PATCH/삭제 실측 · 봇 추가·스냅샷 덮어쓰기 생존·팀 편성·로테이션·
  마피아 배정·정리 후 잔여 0 · 12명 상한.
- ⚠️ **봇 이름을 사람 이름 → 동물 이름으로 교체**(사장님 지시, 위 「이름 원칙」).
  `jo()` 주석 예시도 사물 단어로 교체. 리포 전체 사람 이름 스윕 완료.

### v0.4.1 (2026-08-13) — 📲 PWA 아이콘 수정 + 앱 설치 유도

사장님이 크롬의 "앱으로 설치" 안내를 보고 질문 → 점검 중 **아이폰 아이콘이 깨지는 것**을 발견해 수정.

- 🐛 **iOS 아이콘 깨짐**: `apple-touch-icon`을 SVG로 걸어놨는데 iOS는 SVG를 지원하지 않는다.
  → `icon.svg`를 Edge 헤드리스로 512px 렌더 후 Pillow로 축소해 `apple-touch-icon.png`(180)·
  `icon-192.png`·`icon-512.png` 생성. manifest에 PNG(any+maskable) 추가, `apple-mobile-web-app-title` 지정.
  마스커블 안전영역 검사(주사위가 중앙 80% 원 안) 통과.
- 📲 **앱 설치 배너**: 홈·대기실에 「앱으로 설치하기」. 크롬은 `beforeinstallprompt`를 잡아 앱 안에서
  띄우고, iOS는 「공유 → 홈 화면에 추가」 3단계 안내 모달. 설치됨/불가/✕끔 상태는 렌더 안 함.
- 🐛 **중첩 data-act 버그**: 배너 ✕가 부모 버튼 안에 있어 클릭 시 설치 팝업까지 같이 떴다.
  → `bind()`에 `stopPropagation()` 추가(전역 적용, 다른 곳도 버블링 의존 없음).
- `sw.js` 셸 캐시에 PNG 3개 추가 + 캐시 버전 v2→v3.
- 검증: 배너 표시/✕(프롬프트 미발동)/본체 클릭(프롬프트 1회)/설치 후 숨김/설치불가 환경 숨김.

### v0.4.0 (2026-08-13) — 🎮 나머지 6개 게임 진행 도구 전부 구현

사장님: "게임들에서 꼭 필요한 것들 검토하고 추가해줘" → 게임별 필수 도구를 감사해 6종을 한 번에 구현.
**7게임 전원 진행 도구 보유.** 상세는 위 「도구별 메모」.

- 멕썸노이즈(`noise`): 미션 문장 자동생성(참가자 이름 조합 13종 + 솔로 18종, `jo()` 조사 처리) +
  "술래는 눈 감기" 단계 + 성공 시간 스톱워치 + 시간순 자동 순위.
- 컵 레이스(`cup`): 스톱워치 기록 → 자동 순위. `noise`와 race-* 핸들러 공유.
- 연기 대결(`act`): 대사 20종 × 감정 4종 랜덤 카드 + 리롤.
- 스마일~!(`smile`): 사진 10장 카운터 + 걸린 사람 판정 3분기 + 라운드 로그.
- 안 봤어도(`watch`): 토론 카운트다운 + 코인 집계판(규칙 미완 구간은 수동 ±로 우회).
- 상황극 마피아(`mafia`): **각자 폰 비밀 역할** — 배경 8종·캐릭터 풀, 마피아 수·경찰·의사 설정,
  사회자 제외 옵션, 대기실 알림 → 꾹 눌러 확인. `room.mafia` 노드 신설(pushHostState 포함).
- `renderSig()`를 도구 전반 대응으로 확장(kind별 필드 + `r.mafia`).
- E2E: 6개 도구 전 흐름 통과(문장 생성·기록·타이머 만료·정지, 카드 리롤, 스마일 3분기,
  코인 클램프·1등 산출, 마피아 배정 4명/마피아1/경찰1/사회자 제외/캐릭터 중복 없음/회수).
- ⚠️ 데모가 실제 RTDB `party/rooms/4821`에 쓴 테스트 데이터는 삭제 완료.

### v0.3.0 (2026-08-13) — 🕺 「몸으로 말해요 / 한글자로 말해요」 진행 도구

기획 방향 3건 확정(위 「확정된 기획 방향」) 후 첫 게임 도구 구현. **덱 틀을 만들어
멕썸노이즈·연기 대결이 그대로 재사용하도록** 설계했다.

- `WORD_DECKS` 9종 **416개** 제시어(동물·직업·행동·사물·음식·영화드라마·노래가수·캐릭터·속담). *(v0.17.0에서 14종 1,150개로 확장)*
  카테고리 내 중복 0, 교차 중복은 `buildDeck()`이 제거.
- `S.play` 로컬 상태 + 5단계 흐름(setup → ready → run → turnEnd → done).
- `SFX`(WebAudio 정답/패스/카운트다운/종료음) + 진동. 시끄러운 숙소에서 귀로 알 수 있게.
- 한글자 모드는 팀별 글자를 받아 진행 화면에 상시 표시.
- 결과는 `deckToScore()`가 **기존 순위 입력 화면으로 넘겨** 저장 흐름을 재사용(동점 조정 가능).
- 게임 상세에 `tool` 있으면 「▶ 진행 도구로 시작하기」 노출(호스트만).
- **E2E 검증**: 2팀 한 판 완주 — 덱 219개 중복0 / 마킹 4회 집계 / 2초 타이머 자동 만료 →
  turnEnd·타이머 정지 / 조기 종료 / 순위 [2팀,1팀] 자동 산출 / 저장 후 점수
  **15·15·10.5·10.5**(10×1.5, 7×1.5) 정확 / 타이머 누수 없음.
- 5개 단계 화면 + 20자 속담 표시 렌더 확인.

### v0.2.1 (2026-08-13) — ⚡ 깜빡임 · 스크롤 끊김 수정 ★성능 원칙

사장님 실기기 제보(PC 1대 + 폰 1대 접속): "누군가 어떤 행동을 할 때 사이트가 깜빡거리고,
스크롤이 부드럽지 않다." 원인 두 가지 모두 v0.2.0에서 내가 만든 것.

**① 깜빡임 = 재렌더 폭주** — `render()`가 Firebase 스냅샷마다 호출되는데,
각 기기가 **20초마다 하트비트(`seen`)를 쓰므로** 아무도 아무것도 안 해도 수 초마다
화면 전체가 `innerHTML` 교체 + `viewIn` 페이드를 다시 재생하고 있었다.
→ **렌더 게이팅 도입**:
  - `renderSig()` — 화면에 실제로 보이는 값만 뽑아 서명을 만들고, 같으면 `render()`가 즉시 반환.
    ⚠️ **`seen`(타임스탬프)이 아니라 `isOn()`(불리언)을 넣는 것이 핵심.** 원본을 넣으면 매번 바뀐다.
  - `viewIn` 애니메이션은 **화면 전환일 때만**(`main.enter`). 제자리 갱신은 `main.noanim`으로
    목록 진입 애니메이션(`.pl/.g/.lb-r/.rslot/.pod`)까지 억제.
  - `grabFocus`/`restoreFocus` — 재렌더가 입력 포커스·커서·입력 중인 값을 날리지 않게 보존.
  - 접속 끊김 표시는 시간 경과로 바뀌므로 15초 타이머가 `render()`를 호출(서명 같으면 무동작).
  - **계측 검증**: 하트비트 15회 → 재렌더 **0회** / 참가자 입장·이름변경·팀편성·점수저장·
    접속끊김·오프라인 전환 → 각각 **정확히 1회**. (22 calls → 7 paints)

**② 스크롤 끊김 = 전체화면 합성 비용** — 아래 둘이 스크롤마다 화면 전체를 재합성시키고 있었다.
  - sticky 헤더의 `backdrop-filter: blur(18px) saturate(1.6)` → **모바일에서 제거**,
    불투명 배경(`#0E0C18`). 블러는 `@media (min-width:700px) and (pointer:fine)`에서만.
  - `.grain`의 `mix-blend-mode: overlay` → **제거**(opacity .035만으로 충분).
  - `#toast` backdrop-filter 제거 + 평소 `visibility:hidden`.
  - `#fx`(컨페티 캔버스) 평소 `display:none`, 쓸 때만 켬. DPR은 2로 상한.
  - 좁은 화면에서 오로라 3겹 → 2겹(`.aur.c` 숨김).

> ### ⚠️ 앞으로 지킬 성능 원칙 (다음 작업자 필독)
> 1. **`render()`는 Firebase 스냅샷마다 불린다.** 렌더 결과에 영향을 주는 새 상태를 추가하면
>    **반드시 `renderSig()`에도 넣을 것.** 안 넣으면 화면이 갱신되지 않는다.
>    반대로 **타임스탬프 같은 상시 변하는 값은 넣지 말 것** — 깜빡임이 되돌아온다.
>    강제 갱신이 필요하면 `render(true)`.
> 2. **스크롤되는 화면 위에 `backdrop-filter` / `mix-blend-mode`를 쓰지 말 것.**
>    필요하면 데스크탑 전용 미디어쿼리로 격리.
> 3. **`filter: blur()`도 모바일에선 금지.** 부드러움은 radial-gradient로 낸다.
> 4. 상시 존재하는 전체화면 레이어(캔버스·오버레이)는 **안 쓸 때 `display:none`**.
>
> ⚠️ **검증 함정**: 헤드리스 스크린샷 하네스(iframe)는 **반드시 같은 출처(localhost)에서 열 것.**
> `file://`에서 `http://localhost` iframe을 열면 가상 시계가 전파되지 않아 애니메이션 도중
> 캡처되고, 멀쩡한 화면이 깨진 것처럼 보인다(실제로 이번에 오진할 뻔했음).

### v0.2.0 (2026-08-13) — 🎨 디자인 전면 재작업

사장님: "라이브 들어갔는데 디자인이 좀 별로야, 엄청나게 퀄리티 있게." → v0.1.0의 색·타이포·간격이
제각각이라 정체성을 **「심야 파티 / 네온 사인」** 하나로 잡고 재설계. **로직은 그대로, 표현만 교체.**

- **디자인 토큰 체계 도입**(위 「디자인 시스템」 섹션) — 잉크빛 자정 배경 + 코랄→앰버 히어로,
  게임 7종 각각 고유 액센트 컬러 부여(`GAMES[].color` 신설).
- **폰트 Pretendard Variable**(동적 서브셋 CDN) — 한글 굵기·자간이 확 정리됨. `sw.js`가 캐시.
- **배경**: 오로라 3겹(transform만 애니메이션) + SVG 그레인 노이즈. `filter:blur()` 미사용.
- **화면별 재설계**: 홈 히어로 + **4칸 코드 입력 박스**(투명 input 오버레이 방식) /
  대기실 방코드 히어로 카드 / 게임 카드 액센트 바+틴트 아이콘 / 게임 상세 히어로 배너 /
  **리더보드 시상대(1·2·3위 단상)** / 빈 상태 디자인.
- **연출 신설**: `confetti()` 캔버스 파티클, **`rollReveal()` 역할 뽑기 슬롯머신**(이름 순환 →
  감속 → 당첨 팝 + 컨페티 + 햅틱), 리스트 스태거 진입, 누름 스프링.
- **`color-mix()` 제거 → `gcVars()` rgba 계산**으로 교체. 구형 사파리에서 게임 액센트가 통째로
  날아가는 것을 방지(⚠️ 재발 주의).
- **`sw.js` v2**: 폰트 CDN 캐시-우선 추가, Firebase 요청은 가로채지 않도록 명시적 제외,
  캐시 이름 v2로 올려 옛 캐시 정리.
- **`.gitignore`**: 검증 하네스용 `_*.html` 추가. ⚠️ **gitignore는 줄 끝 인라인 주석을 지원하지 않는다**
  (`_*.html  # 설명` 은 패턴 일부로 먹힘) — 주석은 반드시 별도 줄에.
- 검증: `node --check` 통과 · **390/360px 고정폭 iframe 하네스**로 실제 레이아웃 확인
  (헤드리스 `--window-size`만으로는 우측 잘림 아티팩트가 생겨 판단 불가) · 6화면 렌더 확인.

### v0.1.0 (2026-08-13) — 프로젝트 신설 + 백본 구현 + GitHub 세팅

- **기획 결정**(사장님과): 앱 vs 웹 → **웹(PWA)** 확정. 이유 = 친구 5명에게 링크 하나로 3초 접속,
  아이폰/안드 동시 지원, 심사 없이 즉시 배포, 비용 0. 대신 PWA로 "앱처럼" 보이게.
- **접속 방식**: `AskUserQuestion`으로 **「처음부터 방 코드 접속」** 선택받음.
  인터넷 끊김 리스크는 **호스트=진실원 + RTDB 미러** 구조로 방어(위 동기화 모델).
- **범위**: **「백본만 먼저」** 선택받음 → 게임별 전용 도구는 미구현.
- **구현**: `index.html` 단일 파일 + `manifest.json`/`icon.svg`/`sw.js`/`serve.mjs`/`README.md`.
  게임 7종 규칙 전문 입력, 순위 포인트 환산(마리오카트식), 역할 중복 방지 로테이션.
- **검증**: 인라인 스크립트·`sw.js`·`serve.mjs` `node --check` 통과 · manifest JSON 유효 ·
  **Firebase `/party` 노드 REST 읽기/쓰기 실측 성공** ·
  Edge 헤드리스로 6화면(홈/대기실/게임목록/게임상세/순위입력/리더보드) 렌더 확인.
- **GitHub**: `SOHADA2/party-games` public 신설 + Pages 배포. `.gitignore`는 aram식
  전체무시+화이트리스트가 아니라 **일반 방식**(node_modules/에디터/임시만 무시) → `git add -f` 불필요.
